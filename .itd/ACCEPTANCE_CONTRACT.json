{
  "version": 2,
  "purpose": "Acceptance contract for the portable review gate and GPG-003 unified mandatory keyless independent-review workflow.",
  "sourceRequest": "Preserve the approved nine-point plan and evidence-first exact-candidate machine oracle while simplifying the mandatory pre-PR semantic check to one fresh opposite GPT session: Sol maker to Terra reviewer, Terra maker to Sol reviewer. Anthropic, GitHub Copilot, Antigravity and paid API transports remain optional and cannot block unrelated projects.",
  "createdAt": "2026-07-30",
  "criteriaSchema": {
    "requiredFields": [
      "id",
      "criterion",
      "source",
      "evidence",
      "verificationCommand",
      "status"
    ],
    "allowedStatus": [
      "pending",
      "passed",
      "failed",
      "recovery_required"
    ],
    "evidenceFirstRequiredField": "reviewEvidence"
  },
  "criteria": [
    {
      "id": "GPG-001-AC1",
      "criterion": "Candidate acquisition uses complete GitHub compare pagination plus bounded full base/head blobs, treats merge_base_commit as an independently validated ancestor rather than the current base, verifies decoded size and Git blob SHA, and emits one deterministic canonical diff without trusting patch fields or truncating; merge-group PR composition is explicitly paginated to a hard bound.",
      "source": "Approved plan items 2 and 5.",
      "evidence": "Orchestration mutations accept a valid behind-base PR, reject incomplete commit/component pagination, missing/mismatched/oversized blobs, unsafe paths, binary content, stale coordinates, and over-limit candidates before provider dispatch.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_review_broker.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC2",
      "criterion": "The selected Responses API reviewer is derived from verified maker provenance, receives only the clean bounded final diff, uses a strict structured verdict, and settles only primary token usage against the frozen integer-microusd budget. Diffs above the direct-call bound are scrubbed in full, partitioned without truncation into a bounded deterministic review plan, and can pass only after every unit plus an integration verdict agree.",
      "source": "Approved plan items 2, 3, and 7.",
      "evidence": "Route, request-bound, invalid-output, missing-usage, outage, overspend, exhaustion, same-identity, missing-unit, forged-plan, over-limit-line, and partial-integration mutations fail closed without CLI/OAuth substitution; ambiguous hierarchical calls charge observed usage plus one call cap.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_review_broker.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC3",
      "criterion": "The broker creates the App-owned Check Run on the policy-selected SHA and external ID and requires an exact durable preparation before every terminal PATCH: full cross-bound review evidence for reviewed outcomes or closed typed failure evidence for early fail-closed outcomes. It observes and atomically finalizes each publication; interrupted publications are recoverable without another model call.",
      "source": "Approved plan items 2 and 6.",
      "evidence": "Mutations reject stale head/base/check SHA, forged App identity, wrong external ID, missing preparation/observation, non-clean verdict, and unavailable reviewer; every success/action_required/failure PATCH proves matching prior evidence, and lost GitHub responses for success and failure are recovered without repeating provider review.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_review_broker.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC4",
      "criterion": "The HTTP service reads a bounded body, verifies the exact-byte webhook HMAC and signed event/action shape before state change, uses the broker-owned public provenance keyring, and returns explicit fail-closed HTTP outcomes.",
      "source": "Approved central GitHub App/broker security boundary.",
      "evidence": "Server tests cover health/readiness, overload, malformed/oversized/signature-mismatched events, replay, clean enqueue, worker failure, and graceful shutdown.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_review_broker_server.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC5",
      "criterion": "Runtime configuration loads reviewer/App/webhook secrets only from bounded permission-checked files, never from repository content or OPENAI_API_KEY, and never exposes credentials to candidate execution.",
      "source": "Approved plan items 2 and 7.",
      "evidence": "Environment/bootstrap mutations reject missing, relative, permissive, malformed, oversized, and user-environment secret sources; the broker never checks out or executes candidate code.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_review_broker_server.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC6",
      "criterion": "Accepted trust primitives remain green while the executable layer uses their closed schemas, strict JCS/I-JSON, enrollment, provenance, budget, App authentication, and immutable review APIs without compatibility bypasses.",
      "source": "GPG-001 accepted trust-foundation dependency.",
      "evidence": "Policy and primitive regression suites pass unchanged together with orchestration/server suites.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_review_broker_primitives.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC7",
      "criterion": "This bootstrap slice stages (but does not activate) the machine-oracle workflow template and server-side ruleset machinery while the legacy gate remains active. After a separate protected-anchor merge, the template is installed and pinned by repository ID, path, and release SHA; it loads the protected runner/v2 contract, binds verifier dependencies, isolates Python, executes shell-free argv over the exact candidate, and emits a bound receipt without reviewer secrets.",
      "source": "Approved plan items 5 and 6.",
      "evidence": "Oracle/workflow/ruleset mutations cover bootstrap order, pinned source and drift, same-name status rejection, protected contract/verifiers, exact-tree isolation, Python -I, schema/artifact/output/receipt binding, and pull_request plus merge_group.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_machine_oracle.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC8",
      "criterion": "Windows and WSL global pre-push hooks block protected-branch pushes, unregistered GitHub checkouts, missing maker provenance, and stale or foreign machine receipts while allowing only an explicitly guarded exact-HEAD PR push.",
      "source": "Approved plan item 8.",
      "evidence": "Cross-platform hook and installer mutations prove fail-closed behavior, explicit repository binding, no plugin-cache edits, and separate native host installation targets.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_git_gate_hooks.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC9",
      "criterion": "The global itd CLI creates Draft PRs only through the guarded exact-candidate path. The canonical user gates.json is a versioned profile registry: local-review requires a current exact independent adjudication without GitHub App authority and may carry it across only one clean single-parent commit whose parent, tree, and binary diff reproduce the reviewed candidate; a second or foreign commit invalidates the review. App profiles retain signed maker provenance and live App-owned checks, and the canonical doctor reports only the weakest verified claim. Legacy v1 organization-workflow registries remain fail-closed readable but are never silently rewritten as v2. The CLI also manages the canonical no-bypass ruleset, registers App adoption only after live validation, observes closed enrollment metadata, and inventories controlled repositories in doctor --all.",
      "source": "Approved plan items 4, 6, 8, and 9.",
      "evidence": "CLI/registry/hook mutations cover v2 closed-schema registration, stale/foreign/missing local adjudication, local no-App/no-network routing, guarded exact-HEAD push, weakest-claim doctor aggregation, explicit legacy-v1 compatibility, transient check availability, forged integration identity, live ruleset/workflow drift, invalid App permissions/events, registry write ordering, organization inventory, rejection of local-only bootstrap contracts, and protected status.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_itd_cli.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC10",
      "criterion": "The broker has a reproducible least-privilege deployment package with pinned dependencies, unprivileged read-only execution, bounded loopback exposure, explicit health/readiness, and credential-file isolation.",
      "source": "Approved plan items 2 and 7.",
      "evidence": "Deployment tests validate image/runtime boundaries, Compose/systemd/Caddy configuration, private file modes, readiness, and secret-source rejection.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_review_broker_deployment.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC11",
      "criterion": "The portable profile contract permits maker/maintainer/deployer role overlap while requiring an independent reviewer, distinguishes local-submission, self-hosted-app, and managed-app deployments from protection claims, and allows a dedicated GitHub App to be created for user or organization owners with only profile-valid private/public visibility. Every App uses the exact HTTPS broker webhook, review-only least privilege, only pull_request and merge_group events, CSRF-bound loopback conversion, atomic private credential persistence outside the repository, and no retained OAuth client secret.",
      "source": "Approved plan items 2 and 6.",
      "evidence": "Manifest/profile tests reject invalid origins, callbacks, owners, account types, profile/visibility combinations, codes, conversion fields, write permissions, PROTECTED overclaims, and overwrite attempts while checking the documented GitHub API version and 0600/0700 persistence.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_github_app_manifest.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC12",
      "criterion": "Adoption and operator documentation name the central App/broker as the required authority, retain separately configured provider CLIs only as advisory transports outside the mandatory local route, require live enforceability before protected status, and never direct operators to edit an installed plugin cache or persist or expose a runtime reviewer API key.",
      "source": "Approved plan items 3, 4, and 7.",
      "evidence": "Release, host-adapter, methodology meta-review, and deployment documentation checks pass with the legacy repository gate retained through trust-anchor bootstrap; removal is a separate post-deployment cutover only after protected doctor and negative canaries pass.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_external_reviewer_release.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC13",
      "criterion": "The broker operator adds public provenance keys and exact live enrollment receipts through schema-validated preview/apply commands, uses private atomic files, rejects overwrite or mutation of active enrollment, and never prints credential material.",
      "source": "Approved plan items 2, 7, and 9.",
      "evidence": "Operator mutations cover preview, apply, private modes, invalid records, duplicate key IDs, exact App client IDs, immutable enrollment, and status lookup.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_review_broker_operator.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC14",
      "criterion": "Codex and Claude host adapters remain transport-only and expose the same vendor-neutral methodology, contracts, hooks, and review authority after the new global gate artifacts are added.",
      "source": "Approved plan item 4 and the host-adapter contract.",
      "evidence": "Host-adapter verification passes against the staged methodology source without editing either installed plugin cache.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_host_adapters.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC15",
      "criterion": "The primary independent-review route uses an isolated fresh different-model and different-session producer without paid API, network tools, repository mutation, inherited development context, or model access to reviewer credentials; it emits a signed two-phase exact-candidate receipt that the GitHub App revalidates against live PR and check coordinates before publishing an App-owned Check. Provider API review remains an optional explicitly authorized per-operation route and is never dispatched automatically.",
      "source": "Approved nine-point plan items 2, 3, 6, 7, 8, and 9.",
      "evidence": "PASSED on implementation tree 45e1e376e8ee056e1763bc382e67891963dc4b2f: producer 53 checks, broker 721 checks, server 50 checks, deployment 25 checks, zero-redaction scrub, tool-trust inventory, cold-start, meta-review, host adapters, and quick suite all passed. Fresh gpt-5.6-terra session 019fbea8-4089-7043-98cd-e261498e6c44 returned findings=[] and unverified=[] with zero tool calls; bound adjudication is .itd-memory/verification-loop/receipts/c7525d99a8d5a1d0/GPG-001-general-review-adjudication-a1.json. Mutations cover untrusted executable/proxy/auth, paid-route consent, same-session/context, target replay, foreign signing scope/maker provenance, PR publication races, interruption recovery, findings, and model unavailability.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_free_reviewer_producer.py",
      "status": "passed"
    },
    {
      "id": "GPG-001-AC16",
      "criterion": "The GPG-001 candidate preserves the existing MEM-8 prompt-supply-chain trust boundary: closed tool registry schema and validator, provider/version/prompt-surface evidence, allow/ask/abstain disposition with unknown prompt-bearing providers defaulting fail closed, read-only adoption inventory, security-audit coverage, and mutation tests.",
      "source": "GPG-001 plan zero condition and existing HE-001 trust dependency.",
      "evidence": "PASSED in commit 43e65b0 / tree 92fd0590a80d0234d829a13c5f572b28f6c7de4b: targeted tool-trust mutations, nested schema/validator closure, local unreviewed-allow rejection, full adoption no-mutation wording, Linux/Windows CI wiring, semantic-navigation binding, cold-start, meta-review, host adapters, and quick suite passed; fresh gpt-5.6-terra full report had findings=[] and unverified=[]; adjudication .itd-memory/verification-loop/receipts/22764b59bfd656f6/GPG-001-general-review-adjudication-a1.json.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_tool_trust_inventory.py",
      "status": "passed"
    },
    {
      "id": "GPG-002-AC1",
      "criterion": "Native Windows local-adjudication validation on a UNC checkout receives a bounded 180-second outer cold-start budget while ordinary paths retain 30 seconds; receipt/path/candidate binding, child probe limits, stale/foreign failure and all protection claims remain unchanged.",
      "source": "User-authorized post-v1.95.0 Windows UNC doctor follow-up.",
      "evidence": "PASSED. RED reproduced the hard 30-second child termination. Standard/extended Windows UNC receive 180 seconds while local, device and incomplete paths retain 30 seconds; focused WSL/native-Windows tests pass 25/25. PR #180 merged as ed33169e8cc48f6a5da314586548ee0c5e2389cd; v1.95.1 release PR #181 merged as 1422c94d28f7a6821f0038a766437b7618cc22f1 and is installed on WSL/Windows Codex and Claude with zero clean-archive drift. Windows-bound high-risk receipt 0dabd737b3a711c96c53f598529611708234cb790db03e59b535d46984438d09 made the real UNC doctor return LOCAL_REVIEWED in 32.57 seconds with drift=[] and protected=0. Durable index: .itd/GPG-002_COMPLETION_EVIDENCE.json.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_gate_profile_doctor.py",
      "status": "passed"
    },
    {
      "id": "GPG-003-AC1",
      "criterion": "One shared mandatory pre-PR producer is authoritative for /review, /cross-review and Verification Loop; no parallel advisory path may satisfy or bypass it.",
      "source": "User-authorized GPG-003 clarification after a parallel session substituted missing OPENAI_API_KEY with local review consent.",
      "evidence": "PASSED focused contract oracle: 93 checks prove /review, /cross-review and Verification Loop name itd_free_reviewer_producer.py, one fixed route, viable null coordinates before initial PR creation, actionable negative-report persistence, runtime-observed model provenance, conservative provider/model-family alias comparison, and no caller bypass. Verification Loop 76/76 proves a generic checker cannot satisfy mandatory publication and a signed foreign-base diff cannot borrow the candidate, while repository-bound phase-one/prompt/report/keyring/machine evidence does; local doctor 27/27 requires the route and selected repository before guarded push. Guarded-CLI mutations additionally prove that an amended existing Draft uses an observed-SHA force-with-lease under the same receipt/maker environment, while an already current Draft performs no push. The actual pre-push timeout regression is closed by carrying the existing CLI budget through a bounded 300..3600-second range. PR lookup binds the exact current branch/repository and only its exact no-PR diagnostic permits Draft creation.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_mandatory_keyless_review.py",
      "reviewEvidence": {
        "claim": "All review entry points converge on one route-bound adjudication authority.",
        "impactClasses": [
          "correctness",
          "repository-hygiene"
        ],
        "oracleIds": [
          "mandatory-keyless-review-regression",
          "verification-loop-commit-bridge-regression"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-003-AC2",
      "criterion": "The shared producer runs exactly one fresh opposite-GPT session through ChatGPT subscription auth: Sol maker selects Terra and Terra maker selects Sol. The default route contains no provider API-key requirement and never automatically dispatches Anthropic, GitHub Copilot, Antigravity, paid overage or a paid API request.",
      "source": "User-selected ordered independent-review route.",
      "evidence": "Mandatory-route and producer mutations require the one-entry OpenAI subscription route, exact Sol/Terra alternation, fresh runtime session/model telemetry, no tools or inherited context, strict structured output and no provider fallback. Optional adapter regression suites remain separate compatibility evidence and cannot satisfy the selected route.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_mandatory_keyless_review.py",
      "reviewEvidence": {
        "claim": "The keyless provider route preserves isolation, compatibility and paid-dispatch denial.",
        "impactClasses": [
          "compatibility",
          "security"
        ],
        "oracleIds": [
          "mandatory-keyless-review-regression",
          "free-reviewer-producer"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-003-AC3",
      "criterion": "Findings, unverified contours, malformed output, tool use, same model/session, inherited context, missing exact coverage, stale evidence, unknown maker model or mandatory reviewer UNAVAILABLE block PR publication with no caller/user/force bypass and no automatic provider fallback.",
      "source": "Fail-closed independent-review invariant from the nine-point plan.",
      "evidence": "Mutations cover one mandatory call, BLOCKED/UNVERIFIED/UNAVAILABLE terminal behavior, exact opposite-model selection, same-model rejection, missing/mismatched runtime telemetry, caller off-switch rejection and a signed closed one-entry attempt ledger. Existing optional adapter and phase-one v3 compatibility tests remain non-authoritative for the selected route.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_mandatory_keyless_review.py",
      "reviewEvidence": {
        "claim": "Typed failures, findings and evidence gaps remain terminal across producer, consumer and broker.",
        "impactClasses": [
          "correctness",
          "error-handling",
          "security"
        ],
        "oracleIds": [
          "mandatory-keyless-review-regression",
          "free-reviewer-producer",
          "broker-orchestration",
          "verification-loop-commit-bridge-regression"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-003-AC4",
      "criterion": "Every provider receives only a scrubbed self-contained exact-candidate direct packet or every unit of one deterministic complete hierarchical plan in fresh non-persistent sessions with no repository, mutation, shell, network-tool, inherited-history, model-visible credential or arbitrary environment access; oversized candidates require all units plus integration. Every call places its trusted RFC 8259 closed-output contract after all untrusted material, and successful output uses one closed verdict/findings/unverified schema with real provider/model/session provenance.",
      "source": "Existing free-producer isolation boundary extended without weakening.",
      "evidence": "PASSED on 2026-08-04: the 106-check producer, 49-check Copilot adapter, 744-check broker and 50-check broker-server suites passed after replacing the unavailable Google transport with GitHub Copilot Free/auto. Direct, unit and integration prompts end with the trusted output contract; strict parsing rejects fenced or prose-prefixed JSON. Dual-host semantic efficacy retained every seeded blocker, passed the clean control and bound current producer/corpus hashes.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_free_reviewer_producer.py",
      "reviewEvidence": {
        "claim": "Bounded hierarchical review retains all findings and covers scale/performance contours without truncation.",
        "impactClasses": [
          "bounded-output",
          "performance",
          "scale"
        ],
        "oracleIds": [
          "free-reviewer-producer",
          "independent-review-efficacy"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-003-AC5",
      "criterion": "The same route, typed outcomes and evidence semantics work in WSL and native Windows; host adapters document only transport differences and cannot change order or acceptance authority.",
      "source": "User-required Windows and WSL operation plus host-adapter parity contract.",
      "evidence": "PASSED pre-release host-parity acceptance on 2026-08-04: current focused route/producer/adapter suites passed on WSL and native Windows; host adapters registered the same 28 shared hooks and all 11 hard gates; native live Copilot probes used distinct sessions, allowed auto model gpt-5-mini, zero tools/file changes and included quota only. Release installation and dual-host rollout remain separate completion gates.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_host_adapters.py",
      "reviewEvidence": {
        "claim": "The evidence-first review contract and transport adapters stay host-neutral.",
        "impactClasses": [
          "host-parity"
        ],
        "oracleIds": [
          "host-adapter-regression",
          "independent-review-efficacy"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-003-AC6",
      "criterion": "Paid Responses API review, if retained, is a separately named operation requiring separate explicit consent and budget; it is not part of the mandatory default fallback chain and cannot be selected by missing subscription auth or provider outage.",
      "source": "User rejection of API-key substitution and existing decision log.",
      "evidence": "PASSED: mandatory producer contains no paid endpoint or provider-key surface; external reviewer release test passes 14 criteria and five mutation guards while API_REVIEWER.md names it optional paid operator infrastructure only.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_external_reviewer_release.py",
      "reviewEvidence": {
        "claim": "Paid review remains a separately consented operation outside the mandatory route.",
        "impactClasses": [
          "security"
        ],
        "oracleIds": [
          "external-reviewer-release-regression"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-003-AC7",
      "criterion": "Every active acceptance criterion carries a closed evidence-first mapping from generic impact classes to exact-tree machine oracle IDs; missing, failed, duplicate, foreign-tree or uncovered evidence is UNVERIFIED before any model can return PASSED.",
      "source": "User-approved methodology-wide independent-review effectiveness correction on 2026-08-03.",
      "evidence": "The frozen structural canaries cover missing, failed, duplicate, foreign-tree, missing-reviewer and uncovered-evidence mutations against the same coverage module embedded in the producer packet; the semantic corpus is measured separately from these deterministic mechanics.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_independent_review_efficacy.py --expected-keyring-sha256-file .itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256",
      "reviewEvidence": {
        "claim": "Evidence coverage is closed, exact-tree bound and measured before semantic review.",
        "impactClasses": [
          "correctness",
          "reconciliation",
          "numerical-stability",
          "generated-artifact-freshness"
        ],
        "oracleIds": [
          "independent-review-efficacy"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-003-AC8",
      "criterion": "Medium, high and unknown risk require one clean fresh opposite-GPT provider/model/session identity after exact-tree machine evidence. The host preserves every machine, unit, integration and reviewer finding or unverified contour; a clean integration response cannot erase an earlier blocker.",
      "source": "ADR-006 user-approved single opposite-GPT route and the A29 aggregation finding.",
      "evidence": "Producer, route, broker and Verification Loop mutations cover single-reviewer phase-one v2, missing-reviewer denial, unit/integration finding retention, opposite-model identity, enrollment authorization and route-bound adjudication.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_free_reviewer_producer.py",
      "reviewEvidence": {
        "claim": "The signed receipt, prompt bundle and adjudication preserve the complete reviewer union.",
        "impactClasses": [
          "correctness",
          "error-handling",
          "security"
        ],
        "oracleIds": [
          "free-reviewer-producer",
          "mandatory-keyless-review-regression",
          "broker-orchestration",
          "verification-loop-commit-bridge-regression"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-003-AC9",
      "criterion": "Independent-review efficacy is a release gate: critical/high blocker detection, missing-evidence detection and unit-finding retention are 100%; medium detection is at least 90%; clean-control false blocks are at most 10%; the same focused suite passes on WSL and native Windows.",
      "source": "User-approved measurable reviewer-effectiveness target.",
      "evidence": "PASSED from fresh real keyless gpt-5.6-terra reports on WSL and native Windows: critical/high detection 100%, medium detection 100%, clean-control false blocks 0%, structural missing-evidence detection 100% and unit-finding retention 100%. Every host-derived result is an Ed25519-signed envelope with runtime version, executable/proxy hashes and distinct sessions; the host label is measured, not caller supplied. Critical/high detection additionally requires a BLOCKED verdict, while medium requires BLOCKED or PASSED_WITH_WARNINGS. The frozen eval manifest keeps hidden expected faults outside the reviewer prompt. The separate live-model workflow fixture is explicitly not reviewer-isolation evidence.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_independent_review_efficacy.py --expected-keyring-sha256-file .itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256",
      "reviewEvidence": {
        "claim": "Reviewer effectiveness and false-block calibration are executable release criteria.",
        "impactClasses": [
          "repository-hygiene",
          "host-parity"
        ],
        "oracleIds": [
          "independent-review-efficacy",
          "host-adapter-regression"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-003-AC10",
      "criterion": "The mandatory OpenAI subscription reviewer transport has a current official source, a freshness record no older than 30 days and a pinned minimum runtime version. It requires host-derived live opposite-model proof on WSL and native Windows before release acceptance. Optional transports cannot satisfy or block the selected route; unit protocol tests alone cannot prove mandatory availability.",
      "source": "2026-08-04 methodology correction after the legacy Gemini CLI individual backend retired while its local adapter oracle remained green.",
      "evidence": "The freshness record and dual-host live evidence bind the installed OpenAI subscription runtime plus Sol/Terra model/session provenance. Anthropic, Copilot, Antigravity and retired backends remain documented as optional, unavailable or retired without entering mandatory acceptance.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_reviewer_provider_freshness.py",
      "reviewEvidence": {
        "claim": "Mandatory provider availability is externally fresh rather than inferred from adapter unit tests.",
        "impactClasses": [
          "compatibility",
          "generated-artifact-freshness",
          "host-parity"
        ],
        "oracleIds": [
          "reviewer-provider-freshness",
          "copilot-reviewer"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-003-AC11",
      "criterion": "A portable v2 local-review profile may publish an intentionally unadopted product checkout only through the guarded exact-HEAD path and a current exact independent adjudication. The host-owned registry must pin the authorized phase-one producer keyring SHA-256, so candidate code/evidence cannot introduce its own signing authority. The route does not require an adoption contract or machine preflight, while App-backed and legacy profiles retain both controls.",
      "source": "2026-08-04 OneOfS publication blocker and bounded repair 7e80b5b folded into the active WIP=1 unit.",
      "evidence": "PASSED focused replay: profile registry 24 checks, profile doctor 28 checks, Verification Loop 78 checks, Git hooks 30 checks, guarded CLI 62 checks and host adapters all passed. Mutations prove local-review rejects non-HEAD, invalid adjudication and a foreign producer keyring before publication, never calls adoption/machine preflight, and App-backed/legacy paths still require their original controls.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_gate_registry_profiles.py",
      "reviewEvidence": {
        "claim": "Portable local-review remains exact and fail-closed without coupling product PRs to adoption contracts.",
        "impactClasses": [
          "compatibility",
          "correctness",
          "repository-hygiene",
          "security"
        ],
        "oracleIds": [
          "gate-registry-profiles-regression",
          "gate-profile-doctor-regression",
          "guarded-pr-cli-regression",
          "global-git-gate-regression"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PA1",
      "criterion": "The Verification Loop mints an ADJUDICATED adjudication receipt only from a PASSED machine receipt plus a BLOCKED checker receipt whose findings are each covered by a human disposition (accepted-trade-off | refuted-by-evidence | fixed) with a rationale and an explicit recorded human confirmation bound to the exact candidate digest and the checker receipt sha256. Any uncovered finding, disposition naming no finding, foreign unit, stale or foreign candidate, or missing confirmation refuses to mint. Checker acceptedVerdicts stays [\"PASSED\"] and a checker verdict is never rewritten, downgraded or re-labelled.",
      "source": "docs/adr/ADR-007-human-adjudication-of-independent-review.md; .itd/DECISIONS.md 2026-08-09 (compressed reviewer-policy unit, phase A).",
      "evidence": "RED-first: the pre-channel deadlock (BLOCKED checker receipt with fully dispositioned findings cannot satisfy the gate) is reproduced by execution, then turns GREEN only through the channel; mutation checks kill every minting guard individually.",
      "verificationCommand": "python3 tests/verify_adjudication_channel.py",
      "reviewEvidence": {
        "claim": "A human can adjudicate independent-review findings through a fail-closed machine channel without bypassing or weakening the mandatory reviewer.",
        "impactClasses": [
          "correctness",
          "error-handling",
          "security"
        ],
        "oracleIds": [
          "adjudication-channel"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PA2",
      "criterion": "The commit review gate (hooks/check-review-before-commit.sh via skills/review/scripts/itd_review_cache.py) accepts PASSED and ADJUDICATED adjudication receipts for the exact candidate, records the honest ADJUDICATED label distinct from PASSED, and continues to reject prose verdicts, missing or foreign receipts, and BLOCKED checker evidence that lacks complete human adjudication.",
      "source": "docs/adr/ADR-007-human-adjudication-of-independent-review.md.",
      "evidence": "Gate-level tests exercise accept and reject paths for both outcomes against the exact candidate context; the recorded outcome preserves the ADJUDICATED label end to end.",
      "verificationCommand": "python3 tests/verify_adjudication_channel.py --gate",
      "reviewEvidence": {
        "claim": "The commit gate distinguishes a clean review from a humanly adjudicated one and stays fail-closed for everything else.",
        "impactClasses": [
          "correctness",
          "repository-hygiene",
          "security"
        ],
        "oracleIds": [
          "adjudication-channel-gate"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PA3",
      "criterion": "On the exact phase-A candidate: the full test suite passes, a fresh independent route reviews the candidate, any findings are adjudicated through the channel itself, and the phase-A commit passes the review gate without --no-verify or any environment bypass.",
      "source": ".itd/SCOPE_LOCK.md acceptance boundary.",
      "evidence": "Full run-all output, the route evidence directory, the ADJUDICATED (or PASSED) receipt for the exact staged phase-A tree, and the committed result are recorded together.",
      "verificationCommand": "bash tests/run-all.sh",
      "reviewEvidence": {
        "claim": "Phase A is accepted end to end on its own exact candidate through the machinery it introduces.",
        "impactClasses": [
          "correctness",
          "repository-hygiene",
          "security"
        ],
        "oracleIds": [
          "adjudication-channel",
          "adjudication-channel-gate"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PA4",
      "criterion": "The combined candidate carries the machine-accepted bounded-process/resumability slice unchanged: producer bytes stay identical to the frozen slice (both signed benchmark legs remain bound to the same producer sha256), the dual-host efficacy legs hold host parity, and the live-model artifact evidence is the fresh PASS run pinned by latest.json. The slice's oracle evidence therefore remains exact on this tree.",
      "source": ".itd/DECISIONS.md 2026-08-09 (combined slice+channel commit); .itd-memory/session_2026-08-09_3.md.",
      "evidence": "Every cited oracle passes exactly on this candidate tree inside the bound machine receipt; the efficacy verifier revalidates both signed legs against the host-owned keyring pin on every run.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_independent_review_efficacy.py --expected-keyring-sha256-file .itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256",
      "reviewEvidence": {
        "claim": "The bounded-process slice inside the combined candidate keeps its full machine acceptance: bounded transport containment, dual-host parity, fresh generated-artifact evidence, and reviewer-transport regressions.",
        "impactClasses": [
          "bounded-output",
          "compatibility",
          "generated-artifact-freshness",
          "host-parity",
          "numerical-stability",
          "performance",
          "reconciliation",
          "scale"
        ],
        "oracleIds": [
          "copilot-reviewer",
          "free-reviewer-producer",
          "gate-profile-doctor-regression",
          "gate-registry-profiles-regression",
          "host-adapter-regression",
          "independent-review-efficacy",
          "mandatory-keyless-review-regression",
          "reviewer-provider-freshness"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PB1",
      "criterion": "The Verification Loop check subcommand gains an explicit opt-in --accept-adjudicated-route flag. Default behavior is byte-preserved: --require-mandatory-route without the new flag accepts only a PASSED adjudication whose checker carries a valid signed phase-one clean-pass route. With the flag: a PASSED outcome still requires the signed route, while an ADJUDICATED outcome is authorized by the ADR-007 human channel (PASSED machine receipt, BLOCKED checker validated on the exact tree with artifact and identity checks, complete human adjudication bound to the checker sha256) without a phase-one route, because the producer structurally never mints phase-one for a BLOCKED verdict. Missing human adjudication, tampered checker binding, foreign unit or candidate, and PASSED-without-route all stay fail-closed under the flag.",
      "source": ".itd/DECISIONS.md 2026-08-09 (push through the guarded gate is honestly impossible today); .itd/SCOPE_LOCK.md push-gate unit.",
      "evidence": "RED-first: the honest ADJUDICATED committed-head chain (fixture modeled on receipts adf40ca3f6d504c9) fails check --require-mandatory-route today; it turns GREEN only under --accept-adjudicated-route; mutation checks kill each guard individually and prove the default path is unchanged.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_push_gate_adjudicated.py",
      "reviewEvidence": {
        "claim": "An honestly adjudicated independent review can satisfy the push layer without weakening the signed-route requirement for clean passes.",
        "impactClasses": [
          "correctness",
          "error-handling",
          "security"
        ],
        "oracleIds": [
          "push-gate-adjudicated"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PB2",
      "criterion": "validate_local_adjudication passes --accept-adjudicated-route, and the local-review profile doctor reports the honest route evidence label (human-adjudication vs signed-keyless-route) for the validated receipt without elevating the LOCAL_REVIEWED claim; stale, foreign, unbound and fixture receipts still fail closed.",
      "source": ".itd/SCOPE_LOCK.md push-gate unit design.",
      "evidence": "Extended profile-doctor regression covers accept and reject paths for both route-evidence labels on committed-head candidates.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_gate_profile_doctor.py",
      "reviewEvidence": {
        "claim": "The push-layer claim surface stays honest about which authority backed the review.",
        "impactClasses": [
          "correctness",
          "repository-hygiene",
          "security"
        ],
        "oracleIds": [
          "gate-profile-doctor-regression"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PB3",
      "criterion": "Tests and rehearsals cannot write the live default gate registry: the guarded registry writer refuses a row whose checkout lies under the system temp directory unless the write targets an explicit non-default ITD_GATE_REGISTRY path, and the isolation test proves the live registry file stays byte-identical across the gate suites while the same write succeeds against an explicit isolated target.",
      "source": ".itd/DECISIONS.md 2026-08-09 incident: live ~/.config/itd/gates.json overwritten by the /tmp/itd_gate_local_review_commit fixture.",
      "evidence": "RED-first: the incident write (fixture row into the default path) is reproduced and then fails closed; the explicit isolated write passes; a live-registry hash pin brackets the suite run.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_gate_registry_isolation.py",
      "reviewEvidence": {
        "claim": "Registry isolation is enforced by the writer and pinned by a regression, not by convention.",
        "impactClasses": [
          "repository-hygiene",
          "security"
        ],
        "oracleIds": [
          "gate-registry-isolation"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PC1",
      "criterion": "Reviewer selection is governed by a closed maker/reviewer independence class over the vendor set {anthropic (Claude), openai (Codex Sol/Terra)}: for every supported maker identity the policy resolves at least one cross-vendor independent reviewer route; an anthropic maker no longer dead-ends with the typed UNAVAILABLE 'maker is not a supported Sol/Terra model'; unknown or unsupported maker vendors stay fail-closed typed UNAVAILABLE; and the maker identity can never be selected as its own reviewer.",
      "source": "Compressed reviewer-policy plan 2026-08-09 (ladder remainder); live typed-UNAVAILABLE proof in skills/_shared/itd_free_reviewer_producer.py (select_openai_reviewer_model / require_opposite_openai_model).",
      "evidence": "PASSED 2026-08-09: the RED pin producer/cross-vendor-anthropic-maker-accepted (typed UNAVAILABLE reproduced by execution) turned GREEN only through the approved PC-S3 producer batch; the 42-check policy suite kills same-vendor selection, unknown-vendor acceptance and self-review individually; producer 128, mandatory-route 83 and Verification Loop 78 checks stayed green on the batched tree.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_reviewer_independence_policy.py",
      "reviewEvidence": {
        "claim": "Cross-vendor reviewer independence is a closed machine-checked class, not a hard-coded convention.",
        "impactClasses": [
          "compatibility",
          "correctness",
          "security"
        ],
        "oracleIds": [
          "reviewer-independence-policy"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PC2",
      "criterion": "A same-vendor-different-model fallback reviewer is selectable only after the cross-vendor route returns a typed unavailability; the honest independence label ('same-vendor-different-model' distinct from 'cross-vendor') is carried by the signed phase-one receipt (independenceLevel, minted by the producer and verified fail-closed) and by the check --accept-adjudicated-route output (routeIndependence); the fallback is never selected silently, never permits a same-model pair, and never upgrades the recorded independence claim. Surfacing the label in the profile doctor is a recorded BACKLOG follow-up outside this unit.",
      "source": "Compressed reviewer-policy plan 2026-08-09, point 2 (flagged fallback, honest label, not silent).",
      "evidence": "PASSED 2026-08-09: policy-suite mutations cover silent fallback without typed unavailability, same-model pairs, label laundering to 'cross-vendor' and claim elevation; the producer mints independenceLevel into phase-one (optional on verification so pre-batch chains keep validating — Verification Loop 78 checks green), verify_phase_one rejects a dishonest label, and check --accept-adjudicated-route reports routeIndependence from the signed route only.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_reviewer_independence_policy.py",
      "reviewEvidence": {
        "claim": "Degraded independence is possible only as an explicitly labeled state, never as a silent substitute.",
        "impactClasses": [
          "correctness",
          "repository-hygiene",
          "security"
        ],
        "oracleIds": [
          "reviewer-independence-policy"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PC3",
      "criterion": "An explicit HUMAN_OVERRIDE_NO_INDEPENDENT_REVIEW outcome exists for the case where no independent reviewer route is available at all: the mint-override CLI channel records an explicit human confirmation bound to the exact candidate digest with typed cross-vendor and fallback unavailability evidence, under its own honest label — never PASSED, never ADJUDICATED, never any form of independent review. The record is structurally not an adjudication receipt, so every downstream gate that requires an independent review continues to treat the candidate as review-absent and the doctor keeps reporting the weakest claim.",
      "source": "Compressed reviewer-policy plan 2026-08-09, point 3; ADR-007 honest-label precedent.",
      "evidence": "PASSED 2026-08-09: module mutations cover missing confirmation, foreign candidate digest, wrong outcome literal, extra keys and missing typed unavailability; the mint-override CLI channel mints the audited record fail-closed (happy path, bad-digest rejection) and the record is structurally not an adjudication receipt, so review gates keep treating the candidate as review-absent (adjudication-channel 42 and push-gate 17 checks green).",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_reviewer_independence_policy.py",
      "reviewEvidence": {
        "claim": "A human can consciously proceed without independent review only through an audited, honestly labeled, non-review outcome.",
        "impactClasses": [
          "error-handling",
          "repository-hygiene",
          "security"
        ],
        "oracleIds": [
          "reviewer-independence-policy"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PC4",
      "criterion": "The minimumIndependentReviewers contract is enforced structurally: the restored low-reviewer and high-quorum cases in the efficacy suite prove that fewer distinct independent reviewer identities than the contract requires is UNVERIFIED, and quorum counting deduplicates by provider/model/session identity so one reviewer cannot satisfy a higher quorum.",
      "source": "BACKLOG deferred item from the bounded-process slice: cardinality cases removed with the ported matcher because the contract belongs to this policy unit.",
      "evidence": "PASSED 2026-08-09: RED (low-reviewer reviewer cardinality was accepted under the weakened 0..3 matcher) turned GREEN by restoring the exact-equality contract; the restored low-reviewer/high-quorum canaries and the duplicate-identity quorum canary run inside the signed efficacy harness — final run EXIT 0, 48 PASS / 0 FAIL, both signed host legs valid. Re-confirmed 2026-08-13 on the U12 candidate: after PR #191 (da42644) changed producer bytes, the three legs were live re-signed with the current bytes and the same canaries ran green inside the re-signed harness (verifier status PASSED, hostParityVerified true).",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_independent_review_efficacy.py --expected-keyring-sha256-file .itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256",
      "reviewEvidence": {
        "claim": "Reviewer cardinality is an executable contract, not an advisory count.",
        "impactClasses": [
          "correctness",
          "scale",
          "security"
        ],
        "oracleIds": [
          "independent-review-efficacy"
        ]
      },
      "status": "passed"
    },
    {
      "id": "GPG-004-PC5",
      "criterion": "U12: the independence ladder is measured, not asserted — same-vendor-different-model and cross-vendor reviewer legs run over the frozen seeded benchmark corpus under the same manifest, the comparison (per-class detection and clean-control false-block rates) is published as signed host-derived evidence, the measurement never edits the corpus expectations, and the result is recorded honestly even if same-vendor is not measurably worse.",
      "source": "Compressed reviewer-policy plan 2026-08-09, point 5 (U12, deferred from session_2026-08-08_7).",
      "evidence": "PASSED 2026-08-09: signed legs — wsl same-vendor (terra-maker/sol-reviewer) 9/9 attempts=1, u12 cross-vendor (opus anthropic-subscription maker/sol-reviewer) 9/9 attempts=1, windows same-vendor on stable codex 0.146.0 (user-authorized transport replacement after the 0.147-alpha deterministic tool-use aborts) 9/9 attempts=1. Recorded honest comparison u12IndependenceLadder over the shared frozen manifest: sameVendor {criticalHigh 1.0, medium 1.0, cleanFalseBlock 0.0} vs crossVendor {criticalHigh 1.0, medium 1.0, cleanFalseBlock 0.0} — measured identical, not asserted; hostParityVerified true. Re-signed 2026-08-13 (this candidate): PR #191 (da42644) changed producer bytes after the prior recording, so all three legs were live re-recorded with the current producer/runner/manifest bytes - wsl same-vendor 9/9 attempts=1, u12 cross-vendor 9/9 attempts=1, windows 9/9 attempts=1 (one typed UNAVAILABLE transport drop resumed from the signed checkpoint, typed-exit-3 retry precedent). Measured u12IndependenceLadder unchanged: sameVendor 1.0/1.0/0.0 == crossVendor 1.0/1.0/0.0; outcome recorded in the ADR-007 addendum.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_independent_review_efficacy.py --expected-keyring-sha256-file .itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256",
      "reviewEvidence": {
        "claim": "The cost of degraded independence is a measured quantity with signed evidence, not an assumption.",
        "impactClasses": [
          "generated-artifact-freshness",
          "host-parity",
          "numerical-stability"
        ],
        "oracleIds": [
          "independent-review-efficacy"
        ]
      },
      "status": "passed"
    },
    {
      "id": "R1-SCRUB-1",
      "criterion": "_safe_review_text hands the reviewer the SCRUBBED text and refuses only on detector hits (high-confidence secret, residual credential, high-entropy token, and per-line whitespace-collapsed variants); a public no-reply contact is redacted, never a reason to refuse; the three signed efficacy legs are re-minted over the patched producer with hostParityVerified true.",
      "source": "retro R1 2026-08-10 (route findings r33-r35) + independent route finding r2",
      "evidence": "verify_free_reviewer_producer 140 checks PASSED (both-direction mutation coverage incl. split-credential composite); verify_independent_review_efficacy PASSED, hostParityVerified true, detection 1.0/1.0, cleanFalseBlockRate 0.0 both hosts; receipts .itd-memory/verification-loop/receipts/r1-staged/.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_free_reviewer_producer.py",
      "status": "passed"
    },
    {
      "id": "U16:general-review-1",
      "criterion": "/deploy is gated fail-closed: for a deploy candidate whose derived risk class is data-sensitive, irreversible or monetary, a recognised deploy-transport command (rsync/scp/ssh/tar-non-read-only/curl/wget/aws/docker-push/kubectl/terraform/... or a statically opaque command that could hide one — command/process substitution, eval/source/xargs, case/select, non-lexable) is blocked unless a fresh Verification Loop adjudication receipt bound to the exact deploy candidate validates; missing, stale or invalid receipts block with a typed JSON WHY/FIX and exit 2. Per ADR-008 the gate does not re-analyse the shipment form (a valid current pass authorises the reviewed deploy whatever its shape); ordinary local code execution and file operations are the documented out-of-scope honest limit (covered by /careful, the completion gate and human deploy review).",
      "source": "GPG-004 ladder point U16 (plan 2026-08-09), user-approved unit plan 2026-08-10; skills/deploy/scripts/itd_predeploy_gate.py + Step 0 item 5b wiring in skills/deploy/SKILL.md.",
      "evidence": "PASSED 2026-08-12 on the exact candidate: tests/verify_predeploy_independent_review.py exit 0, 103/103 assertions under the ADR-008 shipment-scoped receipt gate (the S1 redesign that ended the r53–r89 shipment-FORM arms race: the hook no longer parses the shipment form — a gated candidate's recognised deploy transport, or a statically opaque command that could hide one, is denied without a valid current pass; a valid pass authorizes the reviewed deploy regardless of form; local code execution and file operations are the documented ADR-008 out-of-scope honest limit — design and boundaries in .itd/SCOPE_LOCK.md 'ADR-008 amendment'); tests/verify_predeploy_gate.py 11/11 (real PreToolUse subprocess); the exact-candidate machine oracle runs this verifier directly (deterministic, self-proves CORE registration — the full-suite interference flake is a queued S2 follow-up, see SCOPE_LOCK 'Machine-oracle shape'). Deny/allow/receipt-core branches and both mutation directions exercised at module, hook and CLI level. The `oracleIds` value `quick-suite-with-u16-verifier` is an OPAQUE machine-oracle run label (the isolated single-command verifier run, retained for receipt continuity); it is NOT a claim that the full run-all.sh --quick suite is this unit's oracle — the accepted oracle is verifier-only (see SCOPE_LOCK 'Machine-oracle shape').",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_predeploy_independent_review.py",
      "reviewEvidence": {
        "claim": "The pre-deploy gate blocks a gated candidate's recognised deploy transport (and transport-hiding opaque commands) without a valid exact-candidate adjudication receipt, allows the reviewed deploy once a valid pass exists regardless of shipment form (ADR-008), leaves ordinary local execution untouched, refuses the unsigned override bypass for every gated class (cannotWeaken), and stays quiet-pass for routine candidates — the single narrow exception being a content-shipping command that changes to a statically-unresolvable directory (`cd \"$VAR\" && rsync …`), which is fail-closed regardless of launch class because it may reach a gated checkout (r65 cd-escape guard).",
        "impactClasses": [
          "bounded-output",
          "correctness",
          "error-handling",
          "security"
        ],
        "oracleIds": [
          "quick-suite-with-u16-verifier"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U16:general-review-2",
      "criterion": "The criterion's only bypass class, the audited HUMAN_OVERRIDE_NO_INDEPENDENT_REVIEW record, requires a recorded reason AND a signature; since mint-override records carry no signature today, the gate refuses every override record for every gated class (strict fail-closed branch, typed WHY/FIX naming the missing signed channel), an override is never reported as PASSED and never counted as independent-review evidence (cannotWeaken), and the signed override channel is queued as an explicit BACKLOG follow-up; the gate-pass record itself is authenticated by a host-owned MAC outside the checkout, so a hand-written record is not a pass (r51).",
      "source": "Unit U16 criterion cannotWeaken clause; itd_predeploy_gate.py evaluate_gate/load_override; mint-override producer shape in skills/_shared/itd_verification_loop.py.",
      "evidence": "PASSED 2026-08-12 on the exact candidate: tests/verify_predeploy_independent_review.py exit 0, 103/103 assertions under the ADR-008 shipment-scoped receipt gate (the S1 redesign that ended the r53–r89 shipment-FORM arms race: the hook no longer parses the shipment form — a gated candidate's recognised deploy transport, or a statically opaque command that could hide one, is denied without a valid current pass; a valid pass authorizes the reviewed deploy regardless of form; local code execution and file operations are the documented ADR-008 out-of-scope honest limit — design and boundaries in .itd/SCOPE_LOCK.md 'ADR-008 amendment'); tests/verify_predeploy_gate.py 11/11 (real PreToolUse subprocess); the exact-candidate machine oracle runs this verifier directly (deterministic, self-proves CORE registration — the full-suite interference flake is a queued S2 follow-up, see SCOPE_LOCK 'Machine-oracle shape'). Deny/allow/receipt-core branches and both mutation directions exercised at module, hook and CLI level. The `oracleIds` value `quick-suite-with-u16-verifier` is an OPAQUE machine-oracle run label (the isolated single-command verifier run, retained for receipt continuity); it is NOT a claim that the full run-all.sh --quick suite is this unit's oracle — the accepted oracle is verifier-only (see SCOPE_LOCK 'Machine-oracle shape').",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_predeploy_independent_review.py",
      "reviewEvidence": {
        "claim": "The bypass class cannot weaken the gate: unsigned override records are refused outright for every gated class until an authenticated signed channel exists; never PASSED, never review evidence.",
        "impactClasses": [
          "correctness",
          "security"
        ],
        "oracleIds": [
          "quick-suite-with-u16-verifier"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U16:general-review-3",
      "criterion": "Mutation coverage holds in both directions: removing or unwiring the gate turns tests/verify_predeploy_independent_review.py red (block cases + SKILL.md Step 0 wiring assertion), and an unconditional gate turns the routine negative control red; the verifier is registered in the run-all CORE list and green in the isolated snapshot.",
      "source": "Unit U16 mutation clause; tests/verify_predeploy_independent_review.py; tests/run-all.sh CORE registration.",
      "evidence": "PASSED 2026-08-12 on the exact candidate: tests/verify_predeploy_independent_review.py exit 0, 103/103 assertions under the ADR-008 shipment-scoped receipt gate (the S1 redesign that ended the r53–r89 shipment-FORM arms race: the hook no longer parses the shipment form — a gated candidate's recognised deploy transport, or a statically opaque command that could hide one, is denied without a valid current pass; a valid pass authorizes the reviewed deploy regardless of form; local code execution and file operations are the documented ADR-008 out-of-scope honest limit — design and boundaries in .itd/SCOPE_LOCK.md 'ADR-008 amendment'); tests/verify_predeploy_gate.py 11/11 (real PreToolUse subprocess); the exact-candidate machine oracle runs this verifier directly (deterministic, self-proves CORE registration — the full-suite interference flake is a queued S2 follow-up, see SCOPE_LOCK 'Machine-oracle shape'). Deny/allow/receipt-core branches and both mutation directions exercised at module, hook and CLI level. The `oracleIds` value `quick-suite-with-u16-verifier` is an OPAQUE machine-oracle run label (the isolated single-command verifier run, retained for receipt continuity); it is NOT a claim that the full run-all.sh --quick suite is this unit's oracle — the accepted oracle is verifier-only (see SCOPE_LOCK 'Machine-oracle shape').",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_predeploy_independent_review.py",
      "reviewEvidence": {
        "claim": "Both mutation directions are executable checks, not narration.",
        "impactClasses": [
          "compatibility",
          "correctness",
          "repository-hygiene"
        ],
        "oracleIds": [
          "quick-suite-with-u16-verifier"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-PRODUCER-LEGIBILITY-AC1",
      "criterion": "Every reviewer prompt that carries candidate.diffBytes also states the exact review representation total that governs coverage, and states the rule that complete review coverage is reconciled against that representation total and never against candidate.diffBytes. This holds for the root prompt, for every hierarchical unit prompt and for the integration prompt.",
      "source": ".itd/SCOPE_LOCK.md (unit U-PRODUCER-LEGIBILITY); live run report .itd-memory/verification-loop/reports/U-NTFS-DIRSIZE-terra-r4.json (2026-08-11).",
      "evidence": "tests/verify_free_reviewer_producer.py asserts the property on a fixture whose two totals genuinely differ (an oversized gzip transparent blob: the candidate diff is the git-native binary patch, the review representation is the canonical JSONL text), and additionally on the direct transparent route. The vacuity guard in the same check requires candidate.diffBytes != the representation total, so a fixture where the totals coincide cannot satisfy the criterion. 142/142 cases pass on the exact reviewed tree.",
      "verificationCommand": "python3 tests/verify_free_reviewer_producer.py",
      "reviewEvidence": {
        "claim": "The reviewer is told which of the two byte totals governs coverage, so the reconciliation that produced BLOCKED with empty findings on run terra-r4 now resolves against the representation.",
        "impactClasses": [
          "correctness",
          "reconciliation"
        ],
        "oracleIds": [
          "producer"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-PRODUCER-LEGIBILITY-AC2",
      "criterion": "The regression test is bound to the defect rather than to the wording: removing the emitting line from the three prompt builders makes the targeted suite fail.",
      "source": ".itd/SCOPE_LOCK.md (unit U-PRODUCER-LEGIBILITY); live run report .itd-memory/verification-loop/reports/U-NTFS-DIRSIZE-terra-r4.json (2026-08-11).",
      "evidence": "Executable mutation proof: the candidate tree is copied outside the reviewed checkout, all three occurrences of the emitting line are deleted, and the targeted suite is re-run in the mutant. The oracle exits 0 only when the mutant fails. The governing total is additionally parsed as an exact delimited number rather than matched as a substring: a helper extracts every total the note states and the suite requires the extracted set to equal the representation total, and asserts that an emitted digits-superset (141147 -> 1411470) no longer satisfies the check. Suite after the addition: 143 checks PASSED, liveExternalCalls 0, paidApiCalls 0.",
      "verificationCommand": "python3 -c 'import pathlib,shutil,subprocess,tempfile; d=pathlib.Path(tempfile.mkdtemp())/\"mutant\"; shutil.copytree(\".\",d,symlinks=True,ignore=shutil.ignore_patterns(\".git\",\".itd-memory\")); p=d/\"skills/_shared/itd_free_reviewer_producer.py\"; s=p.read_text(encoding=\"utf-8\"); line=\"        f\\\"{_review_representation_note(packet)}\\\"\\n\"; assert s.count(line)==3, s.count(line); p.write_text(s.replace(line,\"\",3),encoding=\"utf-8\"); r=subprocess.run([\"python3\",\"tests/verify_free_reviewer_producer.py\"],cwd=d); raise SystemExit(0 if r.returncode!=0 else 1)'",
      "reviewEvidence": {
        "claim": "The test cannot pass on the pre-fix producer.",
        "impactClasses": [
          "correctness",
          "error-handling"
        ],
        "oracleIds": [
          "mutation"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-PRODUCER-LEGIBILITY-AC3",
      "criterion": "The two changed code files -- skills/_shared/itd_free_reviewer_producer.py and tests/verify_free_reviewer_producer.py -- are additions-only: no line is removed or rewritten there, so no existing binding, hash, review plan, partition, receipt or artifact schema can shift as a side effect, and the redaction-scrub contract merged in PR #188 is preserved verbatim. The claim is deliberately scoped to those two files. The candidate also rewrites .itd/SCOPE_LOCK.md and replaces activeFollowup.unitId in .itd/ACCEPTANCE_CONTRACT.json; that is this unit's own bookkeeping, explicitly authorized by its Allowed zones, and it is contract state rather than reviewed behaviour, so it is outside the additions-only claim.",
      "source": ".itd/SCOPE_LOCK.md (unit U-PRODUCER-LEGIBILITY); live run report .itd-memory/verification-loop/reports/U-NTFS-DIRSIZE-terra-r4.json (2026-08-11).",
      "evidence": "Two independent observations, both scoped to the two code files. (1) git diff --numstat against the merge base reports 27/0 and 21/0 for them: zero deletions, confirmable by the reviewer directly from the diff. (2) An executable oracle, runnable inside the isolated machine run where no git history exists, asserts that the three emitting lines are present AND that the three distinctive markers of the scrub contract merged in PR #188 survive verbatim -- the scrub call, the comment stating that redaction is not a finding, and the high-confidence-secret detector. Deleting or rewriting that work fails the oracle. The two contract files are expected to change and are not part of this criterion.",
      "verificationCommand": "python3 -c 'import pathlib; s=pathlib.Path(\"skills/_shared/itd_free_reviewer_producer.py\").read_text(encoding=\"utf-8\"); emit=\"        f\\\"{_review_representation_note(packet)}\\\"\\n\"; keep=[\"clean, _redactions = scrubber.scrub(text)\", \"Redaction is the scrubber doing its job, not a finding\", \"scrubber.contains_high_confidence_secret(text)\"]; missing=[k for k in keep if k not in s]; print({\"emitters\": s.count(emit), \"missing_pr188_markers\": missing}); raise SystemExit(0 if s.count(emit)==3 and not missing else 1)'",
      "reviewEvidence": {
        "claim": "Nothing that a receipt or a downstream checker binds to has changed, and the concurrently merged producer work is intact.",
        "impactClasses": [
          "compatibility",
          "generated-artifact-freshness"
        ],
        "oracleIds": [
          "additions"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-PRODUCER-LEGIBILITY-AC4",
      "criterion": "The helper is fail-closed: a malformed, absent, non-integer or non-positive representation total raises FreeReviewError('UNVERIFIED', ...) instead of emitting a prompt that names no governing total.",
      "source": ".itd/SCOPE_LOCK.md (unit U-PRODUCER-LEGIBILITY); live run report .itd-memory/verification-loop/reports/U-NTFS-DIRSIZE-terra-r4.json (2026-08-11).",
      "evidence": "Five degenerate representations (absent, empty, zero, string, None plan total) each raise UNVERIFIED. A silent fallback would reintroduce exactly the ambiguity this unit removes, so the absence of a default is the point.",
      "verificationCommand": "python3 -c 'import importlib.util; spec=importlib.util.spec_from_file_location(\"p\",\"skills/_shared/itd_free_reviewer_producer.py\"); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); bad=[{}, {\"reviewRepresentation\": {}}, {\"reviewRepresentation\": {\"reviewDiffBytes\": 0}}, {\"reviewRepresentation\": {\"reviewDiffBytes\": \"12\"}}, {\"reviewRepresentation\": {\"reviewPlan\": {\"fullDiffBytes\": None}}}]; raised=0\nfor case in bad:\n    try:\n        m._review_representation_note(case)\n    except m.FreeReviewError as exc:\n        raised += 1 if exc.status==\"UNVERIFIED\" else 0\nprint(raised); raise SystemExit(0 if raised==len(bad) else 1)'",
      "reviewEvidence": {
        "claim": "An unbound prompt is unreachable; the producer refuses instead.",
        "impactClasses": [
          "error-handling",
          "security"
        ],
        "oracleIds": [
          "failclosed"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-PRODUCER-LEGIBILITY-AC5",
      "criterion": "The note is trusted framing that precedes all untrusted material, and the trusted output contract remains the final instruction in every prompt; the added text is producer-authored and never interpolates untrusted candidate content.",
      "source": ".itd/SCOPE_LOCK.md (unit U-PRODUCER-LEGIBILITY); live run report .itd-memory/verification-loop/reports/U-NTFS-DIRSIZE-terra-r4.json (2026-08-11).",
      "evidence": "The suite asserts that every observed prompt ends with END TRUSTED OUTPUT CONTRACT and that END UNTRUSTED DIFF UNIT precedes BEGIN TRUSTED OUTPUT CONTRACT. The helper interpolates a single integer taken from the producer-computed representation, so no untrusted byte enters the trusted framing.",
      "verificationCommand": "python3 tests/verify_free_reviewer_producer.py",
      "reviewEvidence": {
        "claim": "Prompt-injection ordering and the closed output contract are preserved.",
        "impactClasses": [
          "security",
          "bounded-output"
        ],
        "oracleIds": [
          "producer"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-PRODUCER-LEGIBILITY-AC6",
      "criterion": "Unit and integration prompts still fit their transport bounds on the oversized transparent fixture, and unit prompts still carry the plan hash rather than the plan itself.",
      "source": ".itd/SCOPE_LOCK.md (unit U-PRODUCER-LEGIBILITY); live run report .itd-memory/verification-loop/reports/U-NTFS-DIRSIZE-terra-r4.json (2026-08-11).",
      "evidence": "The suite bounds every unit prompt by MAX_UNIT_PROMPT_BYTES and the integration prompt by MAX_INTEGRATION_PROMPT_BYTES on a 5000-record gzip fixture that selects the hierarchical plan, and asserts the serialized representation is absent from unit prompts. The addition is a fixed-length sentence plus one integer per prompt.",
      "verificationCommand": "python3 tests/verify_free_reviewer_producer.py",
      "reviewEvidence": {
        "claim": "Transport bounds and the hash-only unit binding are unaffected at scale.",
        "impactClasses": [
          "bounded-output",
          "scale"
        ],
        "oracleIds": [
          "producer"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-PRODUCER-LEGIBILITY-AC7",
      "criterion": "The helper performs no IO, no hashing and no arithmetic: it reads an integer already computed by the producer and formats it, so it adds no measurable cost and cannot introduce a numerical discrepancy between the stated total and the plan.",
      "source": ".itd/SCOPE_LOCK.md (unit U-PRODUCER-LEGIBILITY); live run report .itd-memory/verification-loop/reports/U-NTFS-DIRSIZE-terra-r4.json (2026-08-11).",
      "evidence": "An AST oracle over the helper body reports its only calls are isinstance, type and FreeReviewError. The emitted total is plan.fullDiffBytes when a hierarchical plan is present and representation.reviewDiffBytes otherwise, taken verbatim, which are the same integers the plan validator compares against len(diff.encode('utf-8')).",
      "verificationCommand": "python3 -c 'import ast,pathlib; tree=ast.parse(pathlib.Path(\"skills/_shared/itd_free_reviewer_producer.py\").read_text(encoding=\"utf-8\")); fn=[n for n in ast.walk(tree) if isinstance(n,ast.FunctionDef) and n.name==\"_review_representation_note\"][0]; calls={n.func.id for n in ast.walk(fn) if isinstance(n,ast.Call) and isinstance(n.func,ast.Name)}; print(sorted(calls)); raise SystemExit(0 if calls<={\"isinstance\",\"type\",\"FreeReviewError\"} else 1)'",
      "reviewEvidence": {
        "claim": "The stated total is the plan's own integer, not a recomputation.",
        "impactClasses": [
          "performance",
          "numerical-stability"
        ],
        "oracleIds": [
          "noio"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-PRODUCER-LEGIBILITY-AC8",
      "criterion": "The surrounding review contour stays green on this host and the working tree carries no stray artifacts: the producer suite, the review broker, the keyless review route, the reviewer independence policy, the provider freshness check and the verification loop all pass, with no live or paid model calls.",
      "source": ".itd/SCOPE_LOCK.md (unit U-PRODUCER-LEGIBILITY); live run report .itd-memory/verification-loop/reports/U-NTFS-DIRSIZE-terra-r4.json (2026-08-11).",
      "evidence": "Measured on WSL 2026-08-11 on the exact candidate tree, rebuilt on main a2ce0a3 which already contains PR #188: producer 142 checks PASSED (was 130 before #188 added 12), review broker 741, mandatory keyless review 83, reviewer independence policy 42, copilot reviewer 49, provider freshness PASSED, verification loop 78 passed / 0 failed. git status lists only the four files of this unit.",
      "verificationCommand": "python3 tests/verify_free_reviewer_producer.py && python3 tests/verify_review_broker.py && python3 tests/verify_verification_loop.py",
      "reviewEvidence": {
        "claim": "No adjacent contract that reads these prompts regressed, including the one merged in #188.",
        "impactClasses": [
          "host-parity",
          "repository-hygiene"
        ],
        "oracleIds": [
          "producer",
          "broker",
          "loop",
          "keyless",
          "independence",
          "copilot",
          "freshness"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-PRODUCER-LEGIBILITY-AC9",
      "criterion": "The live-model evidence is re-observed on this exact candidate tree instead of being carried over stale: the recorded methodology-tree content pin equals the pin computed live, the candidate run is a real external headless model that exited zero, and the deterministic snapshot oracle rather than the model self-report returns PASS.",
      "source": ".itd/SCOPE_LOCK.md (unit U-PRODUCER-LEGIBILITY, Allowed zones); tests/verify_live_model_benchmark.py:105-123,214 (methodology tree pin over AGENTS.md, .codex-plugin, .claude-plugin, skills, agents, hooks, docs/templates/itd*, docs/HOST_ADAPTER_CONTRACT.md, docs/host-adapters.json).",
      "evidence": "Re-minted on WSL 2026-08-11 by tests/run-live-model-benchmark.py --provider openai on this exact candidate tree: run 20260811T112211Z-68af56ad, observedAt 2026-08-11T11:22:11Z, provider openai, commandFamily 'codex exec --json --ephemeral --repository-local-itd', candidate exitCode 0, attemptCount 1, transcript 48861 bytes sanitized with 0 redactions. The deterministic snapshot oracle tests/verify_snapshot.py returned PASS with exitCode 0 and candidateSelfReportAccepted false. sourcePins.methodologyTree = sha256:d848c6564b5e47f12b37cb7a48957612dda15b382801b710acead16924bccaf1, which equals the tree hash computed live (the pre-run evidence pinned sha256:5cfc3ce7820881f1ebd2002d39cb1e15aa04bee283abd7979d683a3638027e1b and was stale by construction). tests/verify_live_model_benchmark.py --require-evidence: 98 passed, 0 failed, including the mutation checks that reject a tampered pin and stale external evidence.",
      "verificationCommand": "python3 tests/verify_live_model_benchmark.py --require-evidence",
      "reviewEvidence": {
        "claim": "This unit edits a file inside the pinned methodology tree, so the previous evidence pin is stale by construction; it is re-minted by running the unmodified runner, never by editing a pin.",
        "impactClasses": [
          "evidence-freshness",
          "repository-hygiene"
        ],
        "oracleIds": [
          "live-model-benchmark"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-NTFS-DIRSIZE-AC1",
      "criterion": "Both plain-stat identity builders (_plain_source_identities and _plain_ancestor_identities) drop the directory st_size from the entry identity through the single shared _plain_identity helper, while a regular file keeps its size, so an untouched tree yields an identical identity map across consecutive scans and a resized declared file is still detected.",
      "source": ".itd/SCOPE_LOCK.md (unit U-NTFS-DIRSIZE); commit message of this candidate (measurement 2026-08-10).",
      "evidence": "tests/verify_verification_loop.py models the NTFS resident/non-resident flip deterministically through Path.lstat and covers both identity functions, the end-to-end guarded fallback of secure_copy_declared_source, and two controls proving the guard still fires on a resized declared file. 83/83 cases pass on the exact reviewed tree.",
      "verificationCommand": "python3 tests/verify_verification_loop.py",
      "reviewEvidence": {
        "claim": "The declared-input guard reconciles a before/after observation of the same untouched tree as unchanged, and still refuses a genuinely changed input.",
        "impactClasses": [
          "correctness",
          "reconciliation"
        ],
        "oracleIds": [
          "verification-loop-regression",
          "full-suite"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-NTFS-DIRSIZE-AC2",
      "criterion": "The regression test is bound to the defect rather than to the implementation shape: restoring the directory st_size into the identity tuple makes the targeted suite fail.",
      "source": ".itd/SCOPE_LOCK.md (unit U-NTFS-DIRSIZE); commit message of this candidate (measurement 2026-08-10).",
      "evidence": "Executable mutation proof: the candidate tree is copied outside the reviewed checkout, the single line binding the directory size is reverted to the pre-fix expression, and the targeted suite is re-run in the mutant. Observed 80 passed / 3 failed; the oracle exits 0 only when the mutant fails.",
      "verificationCommand": "python3 -c 'import pathlib,shutil,subprocess,sys,tempfile; d=pathlib.Path(tempfile.mkdtemp())/\"mutant\"; shutil.copytree(\".\",d,symlinks=True,ignore=shutil.ignore_patterns(\".git\",\".itd-memory\")); p=d/\"skills/_shared/itd_verification_loop.py\"; s=p.read_text(encoding=\"utf-8\"); old=\"    size = 0 if stat.S_ISDIR(info.st_mode) else info.st_size\"; new=\"    size = info.st_size\"; assert s.count(old)==1, s.count(old); p.write_text(s.replace(old,new),encoding=\"utf-8\"); q=subprocess.run([sys.executable,\"tests/verify_verification_loop.py\"],cwd=d,capture_output=True); print(\"mutant exit:\",q.returncode); sys.exit(0 if q.returncode!=0 else 1)'",
      "reviewEvidence": {
        "claim": "Removing the fix reintroduces the failure, so the green result is caused by the change and not by a test that cannot fail.",
        "impactClasses": [
          "correctness"
        ],
        "oracleIds": [
          "directory-size-mutation-proof"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-NTFS-DIRSIZE-AC3",
      "criterion": "Identity stability holds at the scale that produced the defect and the scan stays within a bounded wall-clock budget: a 2000-directory tree is scanned twice with no write in between and both identity maps are equal.",
      "source": ".itd/SCOPE_LOCK.md (unit U-NTFS-DIRSIZE); commit message of this candidate (measurement 2026-08-10).",
      "evidence": "Executable oracle builds a synthetic 2000-directory tree (4001 entries) outside the reviewed checkout, scans it twice through the changed _plain_source_identities and asserts equality plus a 60-second bound on the second scan. Observed 4001 entries, about 0.8 s per scan, maps equal.",
      "verificationCommand": "python3 -c 'import sys,pathlib,tempfile,time; sys.path.insert(0,\"skills/_shared\"); import itd_verification_loop as m; base=pathlib.Path(tempfile.mkdtemp()); root=base/\"t\"; root.mkdir(); N=2000; [ ((root/(\"d%04d\"%i)).mkdir(), (root/(\"d%04d\"%i)/\"f\").write_bytes(b\"x\"*(i%97))) for i in range(N) ]; t0=time.monotonic(); a=m._plain_source_identities(root,base,\"t\"); t1=time.monotonic(); b=m._plain_source_identities(root,base,\"t\"); t2=time.monotonic(); print(\"entries\",len(a),\"scan1\",round(t1-t0,3),\"scan2\",round(t2-t1,3)); assert len(a)>=2*N, len(a); assert a==b, \"identity unstable across two scans of an untouched tree\"; assert (t2-t1)<60, t2-t1'",
      "reviewEvidence": {
        "claim": "The changed scan is stable and bounded on a tree of the order that exposed the defect, so the fix is not a small-input artefact.",
        "impactClasses": [
          "scale",
          "performance"
        ],
        "oracleIds": [
          "identity-scan-scale-performance"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-NTFS-DIRSIZE-AC4",
      "criterion": "The identity remains a fixed-arity 6-tuple of integers for every entry, and the size component of a directory identity is pinned to 0 independently of the directory size reported by the filesystem.",
      "source": ".itd/SCOPE_LOCK.md (unit U-NTFS-DIRSIZE); commit message of this candidate (measurement 2026-08-10).",
      "evidence": "Executable oracle scans a tree with files of widely different sizes and asserts arity == {6}, component types == {int} (timestamps are st_mtime_ns/st_ctime_ns nanosecond integers, so no floating-point member can drift between observations), and directory size components == {0}.",
      "verificationCommand": "python3 -c 'import sys,pathlib,tempfile,stat; sys.path.insert(0,\"skills/_shared\"); import itd_verification_loop as m; base=pathlib.Path(tempfile.mkdtemp()); root=base/\"t\"; root.mkdir(); [ ((root/(\"d%d\"%i)).mkdir(), (root/(\"d%d\"%i)/\"f\").write_bytes(b\"x\"*(i*1000))) for i in range(5) ]; ids=m._plain_source_identities(root,base,\"t\"); ar={len(v) for v in ids.values()}; ty={type(x) for v in ids.values() for x in v}; dirs=[p for p in ids if stat.S_ISDIR((base/p).stat().st_mode)]; sizes={ids[p][3] for p in dirs}; print(\"entries\",len(ids),\"arity\",ar,\"types\",ty,\"dir-size\",sizes); assert ar=={6}, ar; assert ty=={int}, ty; assert sizes=={0}, sizes'",
      "reviewEvidence": {
        "claim": "Identity output is constant-size per entry and carries no floating-point component, so comparison cannot drift with input magnitude.",
        "impactClasses": [
          "bounded-output",
          "numerical-stability"
        ],
        "oracleIds": [
          "identity-bounded-arity"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-NTFS-DIRSIZE-AC5",
      "criterion": "The defect is Windows/NTFS-specific but the fix is host-neutral: the hard-gate host-parity contract is unchanged and still passes on the reviewed tree.",
      "source": ".itd/SCOPE_LOCK.md (unit U-NTFS-DIRSIZE); commit message of this candidate (measurement 2026-08-10).",
      "evidence": "tests/verify_all_hard_gate_host_parity.py passes on the exact reviewed tree, and the full repository suite reports DONE fails:none, so no host-specific gate behaviour was altered by removing the directory size from the identity.",
      "verificationCommand": "python3 tests/verify_all_hard_gate_host_parity.py",
      "reviewEvidence": {
        "claim": "A Windows-only observation defect was repaired without introducing a host-dependent branch in the identity oracle.",
        "impactClasses": [
          "host-parity"
        ],
        "oracleIds": [
          "host-parity",
          "full-suite"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-NTFS-DIRSIZE-AC6",
      "criterion": "POSIX behaviour is preserved: a regular file still binds its size, the identity field order and arity are unchanged, and the cross-platform runtime contract still passes.",
      "source": ".itd/SCOPE_LOCK.md (unit U-NTFS-DIRSIZE); commit message of this candidate (measurement 2026-08-10).",
      "evidence": "tests/verify_cross_platform_runtime.py passes on the reviewed tree and the targeted suite asserts that a resized declared file is still rejected, so the change is backward compatible for every input class other than a directory.",
      "verificationCommand": "python3 tests/verify_cross_platform_runtime.py",
      "reviewEvidence": {
        "claim": "The identity contract stays compatible for existing callers and input classes; only the directory size component changed.",
        "impactClasses": [
          "compatibility"
        ],
        "oracleIds": [
          "cross-platform-runtime",
          "verification-loop-regression"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-NTFS-DIRSIZE-AC7",
      "criterion": "The guarded fallback still fails closed on a real change and the isolation of the machine oracle is unaffected: a weakened identity must not let a mutated tree pass as unchanged.",
      "source": ".itd/SCOPE_LOCK.md (unit U-NTFS-DIRSIZE); commit message of this candidate (measurement 2026-08-10).",
      "evidence": "tests/verify_fail_closed_hygiene.py and tests/verify_machine_oracle.py pass on the reviewed tree; the targeted suite contains two controls in which the guard fires on a resized declared file, so the narrowed identity did not become permissive.",
      "verificationCommand": "python3 tests/verify_fail_closed_hygiene.py",
      "reviewEvidence": {
        "claim": "Narrowing the identity did not weaken the security property it exists to enforce, and error paths still refuse rather than degrade.",
        "impactClasses": [
          "error-handling",
          "security"
        ],
        "oracleIds": [
          "fail-closed-hygiene",
          "machine-oracle-isolation",
          "verification-loop-regression"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U-NTFS-DIRSIZE-AC8",
      "criterion": "The candidate leaves the repository clean: the whole test suite is green and no generated or documented artefact went stale behind the change.",
      "source": ".itd/SCOPE_LOCK.md (unit U-NTFS-DIRSIZE); commit message of this candidate (measurement 2026-08-10).",
      "evidence": "bash tests/run-all.sh reports DONE fails:none on the exact reviewed tree with the host-owned efficacy keyring pin declared as an input, and tests/verify_harness_docs_freshness.py passes, so no suite that was green before the candidate is red after it.",
      "verificationCommand": "bash tests/run-all.sh",
      "reviewEvidence": {
        "claim": "No regression and no stale generated artefact was introduced anywhere in the repository.",
        "impactClasses": [
          "repository-hygiene",
          "generated-artifact-freshness"
        ],
        "oracleIds": [
          "full-suite",
          "harness-docs-freshness"
        ]
      },
      "status": "passed"
    },
    {
      "id": "S2-FLAKE:general-review-1",
      "criterion": "The session-hygiene runner is fail-closed under host spawn pressure: a transient fork-level OSError inside itd_hygiene can no longer crash `close` into an rc=1/empty-stdout state (the 2026-08-11 flake signature); git() retries a spawn-level failure boundedly and degrades to a structured rc=127 result so every caller reports its own diagnosable error; cleanup_manifest deletes only with positive proof of untracked-ness (ls-files --error-unmatch rc=1) and treats any other git rc as the fail-closed error 'tracking state could not be proven'. Both properties are pinned by tests that are red on the pre-fix runner.",
      "source": "S2 (PLAN-CLOSEOUT-2026-08-11) closing BACKLOG P0 items 'machine-oracle interference' and 'verify_session_hygiene_quality flake'; root cause in tests/ROOT_CAUSE-s2-oracle-nondeterminism.md.",
      "evidence": "PASSED 2026-08-12 on the exact candidate: tests/verify_session_hygiene_quality.py exit 0, 50 checks ALL PASS including test_close_survives_spawn_pressure (RLIMIT_NPROC=1: structured fail-closed report, no traceback, non-empty stdout) and test_cleanup_requires_tracking_proof (git shim rc=2: deletion refused, artifact preserved); both pins verified red on the pre-fix runner via stash runs (rc=1 stdout='' Traceback, and silent rc=0 deletion respectively). Operator series: 5 consecutive green hygiene runs and 5 consecutive green U16-verifier runs on this tree.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_session_hygiene_quality.py",
      "reviewEvidence": {
        "claim": "Under transient host spawn failure the hygiene close/cleanup gates fail closed with structured diagnosable errors instead of crashing with empty output or silently deleting paths whose tracking state git could not prove; absent spawn pressure their behaviour is unchanged (the full pre-existing suite stays green).",
        "impactClasses": [
          "correctness",
          "error-handling",
          "security"
        ],
        "oracleIds": [
          "hygiene-suite"
        ]
      },
      "status": "passed"
    },
    {
      "id": "S2-FLAKE:general-review-2",
      "criterion": "The candidate keeps the repository's static review rubric green (docs/code consistency, registration and counts, gate taxonomy) — the diagnosis documents (ROOT_CAUSE, BACKLOG closures) and the runner change introduce no meta-review Critical or Important findings.",
      "source": "tests/meta_review.py binary rubric (review skill machine oracle for this repo).",
      "evidence": "PASSED 2026-08-12 on the exact candidate: tests/meta_review.py exit 0, FINAL STATUS: PASSED (0 Critical, 0 Important).",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/meta_review.py",
      "reviewEvidence": {
        "claim": "Repository-wide static consistency holds on the exact candidate tree: the S2 fix and its documentation do not drift docs from code and keep every registration/count/table rubric check green.",
        "impactClasses": [
          "repository-hygiene",
          "correctness"
        ],
        "oracleIds": [
          "meta-review"
        ]
      },
      "status": "passed"
    },
    {
      "id": "S3-ADVOCATE:general-review-1",
      "criterion": "The live benchmark's adversarial review is the REAL agents/devils-advocate.md definition executed in a harness-orchestrated fresh session of the same transport after the snapshot oracle (never an inline self-critique inside the blueprint session): the runner's phase 2 writes and validates DEVILS_ADVOCATE_REVIEW.md (min bytes + Challenge structure), archives it hash-pinned, records the devilsAdvocate block (agent digest, artifact digest, session isolation), and keeps attemptCount/recovery semantics for blueprint attempts while the phase entry preserves exact transcript segment coverage. verify_evidence enforces all of this fail-closed under --require-evidence.",
      "source": "S3 (PLAN-CLOSEOUT-2026-08-11) closing the BACKLOG live-model fixture-hardening defect 'the run substituted the Devil's Advocate subagent invocation with inline self-critique'; headless transports cannot spawn Claude-native subagents (claude -p 401 account review; codex has no subagent mechanism).",
      "evidence": "Static harness suite green on the exact candidate: verify_live_model_benchmark (no flag) 39 passed 0 failed; against the OLD pinned evidence --require-evidence fails exactly on the new advocate contract + changed source pins (10 FAIL), proving the checks are fail-closed; full green arrives with the re-recorded evidence in the follow-up commit of this same unit.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_live_model_benchmark.py",
      "reviewEvidence": {
        "claim": "The benchmark scenario is reproducible headlessly with a real, isolated devils-advocate execution whose artifact and agent definition are hash-bound into the run report, and the replay verifier rejects runs that lack or fake that phase.",
        "impactClasses": [
          "correctness",
          "repository-hygiene"
        ],
        "oracleIds": [
          "live-benchmark-static"
        ]
      },
      "status": "passed"
    },
    {
      "id": "S3-ADVOCATE:general-review-2",
      "criterion": "The candidate keeps the repository's static review rubric green (docs/code consistency, registration and counts).",
      "source": "tests/meta_review.py binary rubric.",
      "evidence": "PASSED 2026-08-13 on the exact candidate: tests/meta_review.py exit 0, FINAL STATUS: PASSED.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/meta_review.py",
      "reviewEvidence": {
        "claim": "Repository-wide static consistency holds on the exact candidate tree.",
        "impactClasses": [
          "repository-hygiene",
          "correctness"
        ],
        "oracleIds": [
          "meta-review"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U12:general-review-1",
      "criterion": "The independence ladder is measured, not asserted: both independence classes run over the SAME frozen seeded-defect corpus with signed host-derived results bound to the current producer/runner/manifest bytes (re-signed after PR #191 changed producer bytes), the verifier replays them fail-closed and reports u12IndependenceLadder, and the restored reviewer-cardinality canaries (low-reviewer, high-quorum, duplicate-reviewer-quorum; exact-equality minimumIndependentReviewers contract) are green. The measured outcome is recorded in the benchmark results and in the ADR-007 addendum.",
      "source": "U12 (GPG-004 unit plan) = S4 in PLAN-CLOSEOUT-2026-08-11.",
      "evidence": "exit 0 on the exact candidate: status PASSED, hostParityVerified true; sameVendor criticalHigh 1.0 / medium 1.0 / cleanFalseBlock 0.0 == crossVendor 1.0 / 1.0 / 0.0 (parity, not superiority; ladder order stays cross-vendor-first on the correlated-blind-spots argument). Legs: wsl 9/9 attempts=1; u12 cross-vendor 9/9 attempts=1; windows 9/9 attempts=1 with one typed UNAVAILABLE drop resumed from the signed checkpoint (typed-exit-3 retry precedent 2026-08-08).",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_independent_review_efficacy.py --expected-keyring-sha256-file .itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256",
      "reviewEvidence": {
        "claim": "Same-vendor and cross-vendor reviewer legs are honestly re-signed over the shared frozen corpus and the measured ladder is recorded without overclaiming.",
        "impactClasses": [
          "correctness",
          "repository-hygiene"
        ],
        "oracleIds": [
          "efficacy-oracle"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U12:general-review-2",
      "criterion": "The candidate keeps the quick regression suite green on the exact tree.",
      "source": "tests/run-all.sh --quick.",
      "evidence": "DONE fails:none on the exact candidate tree, 2026-08-13.",
      "verificationCommand": "bash tests/run-all.sh --quick",
      "reviewEvidence": {
        "claim": "Repository-wide quick regression holds on the exact candidate tree.",
        "impactClasses": [
          "repository-hygiene",
          "correctness"
        ],
        "oracleIds": [
          "quick-suite"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U17:general-review-1",
      "criterion": "An opt-in, advisory design-provenance reviewer exists on top of the Devil's Advocate debate in /blueprint: DESIGN_PROVENANCE.md records each architectural claim with a provenance source, the stdlib validator surfaces unsourced claims advisorily, and the cannotWeaken invariants hold - exit 0 with findings, with a clean report, with an absent report and with a malformed report; JSON output has no verdict-shaped field and never the acceptance token; the tool is read-only and a quiet no-op without arguments; no hook or gate script references the reviewer (gate outcome unchanged).",
      "source": "U17 (GPG-004 unit plan) = S5 in PLAN-CLOSEOUT-2026-08-11.",
      "evidence": "RED first: rc=2 before implementation (verifier file absent). GREEN on the exact candidate: python3 tests/verify_blueprint_provenance_reviewer.py exit 0, ALL CHECKS COMPLETED - skill-text invariants, five fixture runs (sourced 4/0, unsourced 4 claims / 2 unsourced findings / assumption-without-reference note, absent, malformed, no-args quiet no-op), fixtures byte-identical after runs, gate sweep over all hooks plus review-cache/verification-loop/producer clean.",
      "verificationCommand": "python3 tests/verify_blueprint_provenance_reviewer.py",
      "reviewEvidence": {
        "claim": "The design-stage reviewer is advisory by construction and cannot block or mint acceptance evidence.",
        "impactClasses": [
          "correctness",
          "repository-hygiene"
        ],
        "oracleIds": [
          "u17-verifier"
        ]
      },
      "status": "passed"
    },
    {
      "id": "U17:general-review-2",
      "criterion": "The candidate keeps the quick regression suite green on the exact tree; the only pre-declared red is verify_live_model_benchmark --require-evidence, inherent until the post-commit live re-record (two-commit acceptance, precedent PR #193/#195).",
      "source": "tests/run-all.sh --quick; .itd/SCOPE_LOCK.md two-commit acceptance section.",
      "evidence": "DONE fails:none on the exact candidate tree 2026-08-13 (includes the newly registered verify_blueprint_provenance_reviewer). verify_live_model_benchmark default mode rc=0, 0 FAIL; --require-evidence red exactly on the burned tree/dirty pins as declared.",
      "verificationCommand": "bash tests/run-all.sh --quick",
      "reviewEvidence": {
        "claim": "Repository-wide quick regression holds on the exact candidate tree.",
        "impactClasses": [
          "repository-hygiene",
          "correctness"
        ],
        "oracleIds": [
          "quick-suite"
        ]
      },
      "status": "passed"
    },
    {
      "id": "S6-SCRUBBER:general-review-1",
      "criterion": "The residual-credential detector separates code from credentials: both recorded U16 false-positive classes (an ordinary parser assignment such as a token-named variable assigned from a subscript or call expression, and prose quoting such a line verbatim) no longer match, while every benign exclusion keeps a literal true-positive antipair that still matches (quoted and bare literals, values that merely contain interpolation syntax, call-lookalikes with embedded quotes, newline-continued quoted values). The high-confidence and entropy detectors are untouched. RED-first: the verifier failed on the pre-fix detector.",
      "source": "S6-SCRUBBER (BACKLOG residual-credential item, U16 2026-08-11); root cause in .itd-memory/contracts/S6-SCRUBBER.md.",
      "evidence": "tests/verify_scrubber_precision.py exit 0, 30 checks PASSED on the exact candidate (11 benign code/prose samples, 14 true-positive antipairs including the r5 quoted call-lookalikes and the r6 expression-wrapper attacks, 1 high-confidence regression pin, 3 producer clean-text source pins, 1 end-to-end scrub check). RED-first at each step: the original detector failed on the benign parser assignment; the r5 detector failed on the quoted call-lookalike; the pre-r6 grammar failed on the no-space wrapper attacks. Machine run s6-verifier exit 0 in the isolated staged-tree receipt.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_scrubber_precision.py",
      "reviewEvidence": {
        "claim": "Ordinary code and prose about it no longer refuse a review route while every literal credential shape the detector previously caught is still caught.",
        "impactClasses": [
          "correctness",
          "security"
        ],
        "oracleIds": [
          "s6-verifier"
        ]
      },
      "status": "passed"
    },
    {
      "id": "S6-SCRUBBER:general-review-2",
      "criterion": "The free-reviewer producer runs all three detectors on the SCRUBBED text, matching the broker and build_candidate routes and its own recorded contract: a credential scrub() neutralises reaches the reviewer only in redacted form and the route proceeds; a credential scrub() cannot neutralise (the bare-value gap where scrub stops at '#' and the detector does not) still refuses fail-closed; the whitespace-split composite and the no-reply smuggling shape never reach the reviewer unneutralised.",
      "source": "S6-SCRUBBER scope; producer contract comment «redaction is not a finding» (route findings r33-r35, 2026-08-10).",
      "evidence": "tests/verify_free_reviewer_producer.py exit 0, 144 checks PASSED on the exact candidate (re-pinned: sk-/AKIA/entropy payloads redacted+proceed, unneutralisable literal refuses, smuggling shape neutralised before egress); tests/verify_review_broker.py exit 0, 741 checks (clean-text parity unchanged). Machine runs producer-suite and review-broker exit 0 in the isolated staged-tree receipt.",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_free_reviewer_producer.py",
      "reviewEvidence": {
        "claim": "Detection on scrubbed text neither leaks an unneutralised credential to a reviewer nor over-refuses a fully neutralised one.",
        "impactClasses": [
          "security",
          "error-handling",
          "correctness"
        ],
        "oracleIds": [
          "producer-suite",
          "review-broker"
        ]
      },
      "status": "passed"
    },
    {
      "id": "S6-SCRUBBER:general-review-3",
      "criterion": "All three signed efficacy legs are re-minted live on the final producer bytes (wsl same-vendor, u12 cross-vendor, windows via native interop), each 9/9 with attempts=1 and no resampling; the efficacy verifier passes with hostParityVerified true against the host-owned keyring pin; the quick regression suite is green on the exact tree, the only pre-declared red being verify_live_model_benchmark --require-evidence, inherent until the post-commit live re-record (two-commit acceptance, precedent PR #193/#195/#199).",
      "source": "S6-SCRUBBER scope (producer edit burns producerSha256 of all three legs); leg retry policy A21.",
      "evidence": "Legs re-minted 2026-08-13: wsl 9/9 attempts=1 (15:09:04Z), u12-cross-vendor 9/9 attempts=1 (15:14:29Z, maker claude-fable-5/anthropic-subscription), windows 9/9 via powershell interop. verify_independent_review_efficacy exit 0: status PASSED, hostParityVerified true, criticalHigh/medium 1.0, cleanFalseBlock 0.0 on both hosts. Machine runs efficacy and quick-suite exit 0 in the isolated staged-tree receipt (quick DONE fails:none).",
      "verificationCommand": "sh skills/_shared/itd_py.sh --itd-isolated tests/verify_independent_review_efficacy.py --expected-keyring-sha256-file .itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256",
      "reviewEvidence": {
        "claim": "The signed efficacy evidence is current for the candidate producer bytes and the repository-wide quick regression holds on the exact tree.",
        "impactClasses": [
          "repository-hygiene",
          "correctness"
        ],
        "oracleIds": [
          "efficacy",
          "quick-suite"
        ]
      },
      "status": "passed"
    },
    {
      "id": "S7-EVIDENCE:general-review-1",
      "criterion": "The bounded-process transport rejects non-finite timeouts, anchors a relative Windows-wrapper cwd at the caller, and reaps setsid-escaping POSIX descendants; the producer oracle proves all three on the exact tree, including a test that actually daemonizes.",
      "source": "S7-U1/U2/U3 unit contracts; BACKLOG GPG-004 adjudication debts.",
      "evidence": "verify_free_reviewer_producer.py PASSED checks=153 on the exact candidate (machine run producer-oracle exit 0); units verified with adjudication receipts (U3 risk high, full cross-model checker).",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_free_reviewer_producer.py",
      "status": "passed",
      "reviewEvidence": {
        "claim": "Transport hardening holds on the exact candidate tree.",
        "impactClasses": [
          "correctness",
          "error-handling",
          "security"
        ],
        "oracleIds": [
          "producer-oracle"
        ]
      }
    },
    {
      "id": "S7-EVIDENCE:general-review-2",
      "criterion": "The sync surface installs and polices the plugin manifest and no longer reads bytecode as skill drift; the sync oracle proves dry-run plan, apply-mode install, re-run no-op and bytecode-not-drift against an isolated CLAUDE_HOME.",
      "source": "S7-U4 unit contract; BACKLOG sync-manifest and __pycache__ debts.",
      "evidence": "verify_sync_manifest.py PASSED checks=9 (machine run sync-oracle exit 0); live sync run reports 'manifest: .claude-plugin/plugin.json unchanged'.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_sync_manifest.py",
      "status": "passed",
      "reviewEvidence": {
        "claim": "The install surface is aligned and drift reads are noise-free.",
        "impactClasses": [
          "repository-hygiene",
          "correctness"
        ],
        "oracleIds": [
          "sync-oracle"
        ]
      }
    },
    {
      "id": "S7-EVIDENCE:general-review-3",
      "criterion": "The three signed efficacy legs are current for the final producer bytes (wsl and u12 from WSL, windows from the native Windows host), round-1 red is archived rather than discarded, and the efficacy verifier passes against the host-owned keyring pin.",
      "source": "S7 evidence commit; DECISIONS 2026-08-13/14 re-mint entries.",
      "evidence": "Efficacy verifier exit 0 with host pin; round-1 artifacts in .itd-memory/efficacy-evidence/s7-round1/; round-2 legs 9/9 with one checkpoint resume each where transport dropped.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_independent_review_efficacy.py --expected-keyring-sha256-file .itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256",
      "status": "passed",
      "reviewEvidence": {
        "claim": "Signed efficacy evidence is current and honestly recorded for this candidate.",
        "impactClasses": [
          "repository-hygiene",
          "security"
        ],
        "oracleIds": [
          "efficacy"
        ]
      }
    },
    {
      "id": "S7-EVIDENCE:general-review-4",
      "criterion": "The methodology's own meta-review rubric holds on the exact candidate tree with zero Critical and zero Important findings.",
      "source": "Methodology self-review floor (every S7 commit passed it).",
      "evidence": "tests/meta_review.py FINAL STATUS: PASSED (machine run meta-review exit 0).",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/meta_review.py",
      "status": "passed",
      "reviewEvidence": {
        "claim": "The methodology rubric holds on the candidate.",
        "impactClasses": [
          "repository-hygiene"
        ],
        "oracleIds": [
          "meta-review"
        ]
      }
    },
    {
      "id": "S8-1-hostpin",
      "criterion": "The efficacy verifier's required host input is supplied by every caller, and the git-ignored pin is never declared as a trusted verifier path.",
      "source": ".itd-memory/contracts/S8-U1-HOSTPIN.md (commit 3d501dd).",
      "evidence": "Oracle gate-registry-binding = python3 -I tests/verify_gate_registry_binding.py and oracle machine-oracle = python3 -I tests/verify_machine_oracle.py, both exit 0 on the reviewed tree; the CORE aggregator reports DONE fails:none.",
      "verificationCommand": "python3 -I tests/verify_gate_registry_binding.py",
      "status": "passed",
      "reviewEvidence": {
        "claim": "Both callers pass the host-owned keyring digest, and the aggregator table cannot drift from run-all.sh.",
        "impactClasses": [
          "correctness",
          "repository-hygiene"
        ],
        "oracleIds": [
          "gate-registry-binding",
          "machine-oracle"
        ]
      }
    },
    {
      "id": "S8-2-treepin",
      "criterion": "The methodology tree pin excludes exactly what Git ignores, identically in the producer and the verifier, and fails loudly when Git cannot answer.",
      "source": ".itd-memory/contracts/S8-U2-TREEPIN.md (commit 53eb3c2).",
      "evidence": "Oracle tree-pin-debris = python3 -I tests/verify_tree_pin_debris.py, exit 0: 14 checks including the H4 debris reproduction, a non-ignored probe that must still move the pin, producer/verifier agreement, and all three Git-failure paths.",
      "verificationCommand": "python3 -I tests/verify_tree_pin_debris.py",
      "status": "passed",
      "reviewEvidence": {
        "claim": "Harness debris cannot enter the tree pin, and a broken Git raises instead of silently widening it.",
        "impactClasses": [
          "correctness",
          "repository-hygiene"
        ],
        "oracleIds": [
          "tree-pin-debris"
        ]
      }
    },
    {
      "id": "S8-3-a19",
      "criterion": "A Codex error item is classified as a transport failure rather than a reviewer tool call, while the isolation refusal stays fail-closed and names what it saw.",
      "source": ".itd-memory/contracts/S8-U3-A19.md (commit e69431f), BACKLOG item A19.",
      "evidence": "Oracle producer-oracle = python3 -I tests/verify_free_reviewer_producer.py, exit 0: 158 checks, five of them new and each demonstrated red against HEAD individually.",
      "verificationCommand": "python3 -I tests/verify_free_reviewer_producer.py",
      "status": "passed",
      "reviewEvidence": {
        "claim": "The transport's own failure is no longer reported as the reviewer breaking isolation, and no unknown item type is silently accepted.",
        "impactClasses": [
          "correctness",
          "error-handling",
          "security"
        ],
        "oracleIds": [
          "producer-oracle"
        ]
      }
    },
    {
      "id": "S8-4-efficacy",
      "criterion": "The three signed semantic-efficacy legs bind the producer bytes of this candidate and hold host parity.",
      "source": ".itd-memory/contracts/S8-EVIDENCE.md (commit 0c640ea).",
      "evidence": "Oracle efficacy = python3 -I tests/verify_independent_review_efficacy.py --expected-keyring-sha256-file .itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256, exit 0: status PASSED, hostParityVerified true, criticalHighDetection 1.0, mediumDetection 1.0, cleanFalseBlockRate 0.0 on both hosts.",
      "verificationCommand": "python3 -I tests/verify_independent_review_efficacy.py --expected-keyring-sha256-file .itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256",
      "status": "passed",
      "reviewEvidence": {
        "claim": "The independent-review efficacy claim is signed against this exact producer, on both hosts.",
        "impactClasses": [
          "host-parity",
          "security"
        ],
        "oracleIds": [
          "efficacy"
        ]
      }
    },
    {
      "id": "S8-5-rerecord",
      "criterion": "The live H4 workflow-quality evidence was recorded on this tree, so its runner, adapter and methodology-tree source pins bind.",
      "source": ".itd-memory/contracts/S8-RERECORD.md (commit 3a0a820).",
      "evidence": "Oracle evidence-replay = python3 -I tests/verify_live_model_benchmark.py --require-evidence, exit 0: 108 passed, 0 failed.",
      "verificationCommand": "python3 -I tests/verify_live_model_benchmark.py --require-evidence",
      "status": "passed",
      "reviewEvidence": {
        "claim": "The published live-quality evidence is current for this candidate rather than inherited from a tree that no longer exists.",
        "impactClasses": [
          "correctness",
          "generated-artifact-freshness"
        ],
        "oracleIds": [
          "evidence-replay"
        ]
      }
    }
  ],
  "completion": {
    "status": "verified",
    "selectedDeploymentProfile": "local-submission",
    "selectedProtectionProfile": "local-review",
    "implementationMerge": "234752828d463821814020bebf2cc3dc40399beb",
    "releaseMerge": "8c2bb1b0689ce68282a3ef10a2edc6143a097f8f",
    "releaseVersion": "1.95.0",
    "durableEvidence": ".itd/GPG-001_COMPLETION_EVIDENCE.json",
    "unclaimed": [
      "live App/broker enrollment",
      "organization-workflow",
      "PROTECTED",
      "credential rotation"
    ]
  },
  "activeFollowup": {
    "unitId": "S9",
    "status": "in_progress",
    "selectedClaim": "LOCAL_REVIEWED",
    "rootCause": ".itd-memory/contracts/S9-U2-DOCTOR.md",
    "reviewPolicy": {
      "mode": "evidence-first",
      "riskTier": "medium",
      "requiredImpactClasses": [
        "correctness",
        "error-handling",
        "repository-hygiene",
        "security"
      ],
      "minimumIndependentReviewers": 1,
      "explorer": "isolated-machine-oracle",
      "adjudicator": "sealed-host-union"
    },
    "openedAt": "2026-08-15T18:00:00Z",
    "scopeNote": "Four harness debts of one class, one committed unit each, in order U4 (scripts/itd.py no-op push) -> U3 (completion-ledger producer schema) -> U2 (doctor independence label) -> U1 (producer committed-head candidate). U1, U2 and U3 touch METHODOLOGY_TREE_ROOTS and burn the live-evidence pin; a single live re-record runs at the end on a clean tree."
  },
  "doneRule": "A criterion status of passed means only that its verification command passed. GPG-001 and GPG-002 remain verified under their durable completion evidence. GPG-003 additionally requires one exact high-risk adjudication, merged implementation and patch-release PRs, and clean WSL/native-Windows installed-host proof; it never upgrades LOCAL_REVIEWED to PROTECTED. The GPG-004 push-gate unit is closed only after its commit is followed by: the pin-clean-live-evidence follow-up, a fresh committed-head ADJUDICATED chain on the final HEAD, live registry repair through the guarded register flow (the incident fixture row replaced; load_registry validates), and guarded publication — the pre-unit receipts in adf40ca3f6d504c9 are RED-test fixtures only and never authorize a later push. The GPG-004 ladder remainder (PC1..PC5) lands as one combined candidate: producer edits only inside the approved PC-S3 batch with both signed benchmark legs re-run exactly once, and GOAL/STATE closure of GPG-004 requires the /goal verification pass after the remainder is accepted."
}
