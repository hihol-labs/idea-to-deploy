# BROKER-ISOLATION

Current unit BROKER-ISOLATION, medium risk. Owner-approved order (2026-09-10): BROKER-ISOLATION -> RSI-DEBT-2 -> RSI-DEBT-3 -> the separate unit that splits the ledger into internal quality and external validation. One plan item is one session.

Measured cause, replacing the one recorded in BACKLOG P1 2026-09-10: `tests/verify_review_broker.py` was not isolation-dependent and not mirror-order-dependent. It was a roughly 8% flake reproducible on the host checkout too (1 red in 12 standalone runs; 1 red in 26 and 1 in 47 in-process iterations). The host wall clock steps backwards - two steps, -2.478 s and -5.366 s, inside a 45-second measurement of `datetime.now(utc)`, which is WSL2 resynchronising with Windows - and the free two-phase receipt required a strict ordering of stamps.

Reconciliation with the sealed criterion, stated so the ledger is not ambiguous. The unit criterion in `.itd-memory/GOAL.json` is sealed and is NOT rewritten by this candidate: an implementation does not edit its own exam. Its second clause offers two ways out - "the mirror-order dependency of free_review_phase is either declared as an input or removed" - and this candidate takes the second: there is no mirror-order dependency to declare, and the nondeterminism that produced the appearance of one is removed at its source. The diagnosis wording inside the criterion therefore stands as the history of what was believed on 2026-09-10; what was measured is recorded here, in `BACKLOG.md` and in `.itd/DECISIONS.md`, and the acceptance rows of this unit name the oracles that hold the new guarantee.

Allowed: the clock-skew tolerance in `skills/_shared/itd_free_reviewer_producer.py` (a declared constant plus the four chronology comparisons that use it, including the mint-time `phase two predates live observation` guard); the deterministic clock-step regressions in `tests/verify_review_broker.py`; the new oracle `tests/verify_isolated_candidate_mirror.py` and its `mirror-runner` declaration in `tests/OUT_OF_MIRROR.json`; the regenerated `.itd/IMPACT_GRAPH.json`; the re-minted efficacy legs under `benchmarks/independent-review-efficacy/` with their archive folder, manifest rows and the matching `HISTORY_PINNED_SNAPSHOTS` pins in `tests/verify_independent_review_efficacy.py`; the BACKLOG correction and the DECISIONS entries; canonical unit ledgers and the acceptance rows of this unit.

Required: both legs of the unit's sealed verificationCommand exit 0 - `tests/verify_review_broker.py` PASSED and `tests/verify_isolated_candidate_mirror.py` PASSED with the mirror's last line inside the isolated candidate exactly `DONE fails: verify_reviewer_provider_freshness`. RED-first is observed on the pre-fix bytes for the same candidate, twice in a row, before the fix is accepted. The efficacy suite is green on the host pin against the changed producer bytes. A `/review` route before the multi-file commit, then the cross-vendor producer; existing accepted evidence stays immutable.

Forbidden: touching `PE5-008` or `PE5-009` (blocked until real external adoption evidence); weakening `MAX_LIVE_AGE_SECONDS` or any other freshness window; widening the skew tolerance beyond the 60 seconds the shipped broker policy already grants JWT validation; deleting or rewriting an existing live-model run or an already accepted efficacy leg; adding the new oracle to the mirror's own suite list; declaring the isolated candidate green while a suite other than `verify_reviewer_provider_freshness` is red; `--no-verify` (if a gate refuses, the exact command goes to the owner); starting RSI-DEBT-2 or RSI-DEBT-3 in this session.
