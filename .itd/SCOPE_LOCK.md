# ROUTE-REPAIR-3 — the route metric tells three kinds of `verified` apart

Unit `ROUTE-REPAIR-3` of the goal in `.itd-memory/GOAL.json`, risk tier `low`,
checker mode `machine_only`. Review claim id:
`ROUTE-REPAIR-3:general-review`.

Criterion (sealed in the ledger): `itd_unit_lifecycle.build` assigns every
verified lifecycle to exactly one named route class - `verifiedIndependent`,
`verifiedMachineOnly`, `verifiedOwnerRoute` - `itd_metrics.py` surfaces all
three counters, their sum equals `unitsVerified`, `vcr` keeps its current value
and definition, spelling variants of the route label (`owner` / `owner-route`)
and the literal string `none` in `checkerReceipt` are normalised rather than
trusted, and each class is a RED-first regression proved by mutation.

## In scope

- `skills/_shared/itd_unit_lifecycle.py`: `route_class`, its two normalisers,
  the lifecycle field `routeClass` and the three summary counters.
- `scripts/itd_metrics.py`: the three counters summed across memory dirs.
- `tests/verify_route_metric.py`: the new oracle (fixture classes, sum, `vcr`,
  metrics CLI, six in-oracle mutations, read-only live integration).
- `tests/run-all.sh`: the oracle joins the mirror `CORE` set.
- `.itd/IMPACT_GRAPH.json`: regenerated by `tests/build_impact_graph.py` so the
  new suite is attached (the mirror is red without it).
- `CHANGELOG.md` (`[Unreleased]` / Added), `.itd/DECISIONS.md` (precedence
  decision), `BACKLOG.md` (the writer gap found on the way), this file.
- `.itd-memory/{GOAL,STATE}.json`, `.itd-memory/events.jsonl`: harness-written
  activation of the unit, nothing hand-edited.

## Required evidence

- `sh skills/_shared/itd_py.sh tests/verify_route_metric.py && sh skills/_shared/itd_py.sh tests/verify_retro_scan.py`
  exits 0 (the unit's `verificationCommand`, run by the goal harness).
- RED-first: the oracle was red on the untouched tree (38 failed, 7 passed).
- `bash tests/run-all.sh --quick` ends with `DONE fails:none`, run with the
  host pin `.itd-memory/host-inputs/GPG-003_REVIEW_EFFICACY_KEYRING.sha256`.

## Declared limit

- `verifiedIndependent` stays 0 for harness transitions: `transition_event` in
  `skills/goal/scripts/itd_goal_verify.py` does not write a checker receipt
  into the event. Recorded in `BACKLOG.md`, not fixed here.
- The classifier reads the event log only; it does not check that a named
  receipt file exists.

## Out of scope

`skills/goal/scripts/itd_goal_verify.py` and every other event writer;
`skills/retro` / `itd_retro_scan.py` output; `tests/verify_route_debts.py`
(unit `ROUTE-DEBTS-ORACLE-1`); the release unit `REL-1.105.0`.
