# RSI-DEBT-1

Current unit RSI-DEBT-1, medium risk. Owner-approved RSI route debt (2026-09-08): the review scrubber must stop blinding the independent reviewer to ordinary code.
Allowed: `skills/_shared/itd_external_reviewer.py` (the two assignment-shaped rules in `SECRET_PATTERNS` and `scrub()` only; the bare-run character class is one definition shared by the assignment rule and `RESIDUAL_CREDENTIAL_RE`, so a boundary change lands in both or in neither - DECISIONS 2026-09-08 Sol-r5), `tests/verify_scrubber_precision.py` (RED-first regressions for both shapes: a call expression such as `token = self.w.HANDLE()` stays intact; every real credential of the existing corpus is still redacted), any test that pins the scrubber's exact output and must follow by construction; canonical unit ledgers, the acceptance contract rows of this unit, generated impact metadata and DECISIONS/BACKLOG entries (including the deferred DECISIONS record of the 2026-09-08 owner decision that `/review` subagent returns to the ordinary-unit route).
Required: RED on the pre-fix bytes and GREEN on the candidate for both shapes, observed rather than asserted; every guarantee proved lethal by mutation; the unit oracles (`verify_scrubber_precision`, `verify_free_reviewer_producer`) exit 0; `/review` subagent runs before the commit (population of the blind protocol), then the cross-vendor producer; existing accepted evidence stays immutable.
Live-model evidence, stated precisely: rewriting any recorded run or efficacy observation is forbidden and every existing run stays byte-identical. Because this unit changes `skills/`, CI Gate 1's pin needs one fresh `fixture-03-cli-tool` run; that run is captured on the CLEAN committed tree and lands as its own follow-on commit (a new immutable run directory plus the moved pointer), because the benchmark records the tree's dirty state and a capture inside this staged candidate would not match the committed tree. So this code candidate is complete WITHOUT the run; the re-pin commit is reviewed separately, and the generated output inside it is the benchmarked model's own work product, not a claim about this unit.
Forbidden: new sites beyond those above (surface treadmill), relaxing redaction of real credentials (every credential of the existing corpus stays redacted; by the unit criterion a call expression such as `fetch(name)` is kept, and the residual named in DECISIONS - a letters-only argument of at most fourteen characters - is a criterion consequence awaiting the owner's ADR-007 disposition, not a silent relaxation), new secret classes, new provider routes, lowered gates, changes to the Q6 campaign or the blind protocol, pilot decisions, later unit activation, release or rollout inside this unit.
