# SCOPE_LOCK — ORACLE-DEBT (открыт 2026-08-28)

## Предмет
N7: два предсуществующих красных оракула (BACKLOG P1, найдено в GENG-S05
2026-08-22) + подготовка данных и вариантов по классу «сьюты вне зеркала
run-all» (26 сьютов) и по 2 группам идентичного покрытия с их участием
(остаток LPD-003-4). Решение по классу выносит владелец — само-авторизация
запрещена.

Измеренная переклассификация (эта сессия, воспроизводимо):
`tests/verify_operating_loops_release.py --phase candidate` ЗЕЛЁНЫЙ на своём
пиненном кандидате `172fca79` (v1.94.0) и красный на любом другом дереве по
построению — это не «стейл-пин», а candidate-bound сьют того же класса, что
`verify_harness_demo_pilots` / `verify_semantic_navigation_demand`.

## Разрешённые области
- `tests/verify_harness_demo_absorption.py` — ТОЛЬКО маршрут валидации
  исторического v2-ремонта: недостижимые git-объекты -> запечатанная фикстура
  `docs/harness-demo-pilots/HISTORICAL_REPAIR_FIXTURE.json` (+ её `.sha256`),
  прецедент — `tests/verify_harness_demo_portable.py:255`. Гарантии не
  ослабляются: те же base/accepted tree, те же changedPaths, те же
  `acceptedRepairSeal`-дайджесты; git-маршрут остаётся ТОЛЬКО как усиление,
  когда объекты достижимы. Требует контракта v5 + владельческого одобрения
  (changePolicy: `new-version-and-human-approval`).
- `docs/HARNESS_DEMO_ABSORPTION_CONTRACT.json` + `.sha256` — версия 5 и
  `absorptionRepairApprovalProvenance` (или эквивалент по форме v2/v3/v4) с
  хешем точной владельческой формулировки. Правка ТОЛЬКО после явного
  одобрения владельца.
- Новый машиночитаемый реестр внезеркального класса и его оракул —
  только по решению владельца (вариант из пакета решений), пути объявляются
  в момент выбора варианта.
- `.itd-memory/measurements/ORACLE-DEBT-n7.json` — замеры этой сессии
  (26 сьютов: rc, длительность, причина внезеркальности; пары идентичного
  покрытия; поимённое состояние входа), `git add -f`.
- `.itd-memory/measurements/ORACLE-DEBT-check.py` — сверка этого замера с
  деревом (конвенция коммитнутых замеров N4/N6: `LPD003-REMEASURE-check.py`),
  `git add -f`. Добавлен в разрешённые области по находке ревьюера r3: файл
  создавался раньше, чем скоуп его называл.
- `.itd/ACCEPTANCE_CONTRACT.json` — критерии `ORACLE-DEBT-*` и
  `activeFollowup` на этот юнит.
- `.itd-memory/STATE.json` — активация/verified юнита (события пишет
  `itd_unit_log`), `nextAction` -> N8.
- `.itd/DECISIONS.md` — append записи «N7 / ORACLE-DEBT» (решения владельца).
- `BACKLOG.md` — ТОЛЬКО секции P1 про два красных оракула и про сьюты вне
  зеркала: статусы закрытия/переклассификации.
- `LAUNCH_PLAN.md` — ТОЛЬКО строка N7 в блоке остатка плана.
- `.itd/IMPACT_GRAPH.json` — регенерация карты, если менялись tests/.
- `.itd/SCOPE_LOCK.md` — этот файл.

## Запрещено
- Правка `skills/`, `hooks/`, `agents/`, `scripts/`, `.github/` — юнит не
  меняет поведение методологии.
- Правка `tests/verify_operating_loops_release.py`: измерением показано, что
  сьют зелёный на своём кандидате; «ремонт пина» задним числом переписал бы
  историческую релизную квитанцию.
- Ослабление гарантий: маршрут через фикстуру обязан проверять ТЕ ЖЕ
  дайджесты, что git-маршрут; тихая деградация (git недостижим -> проверка
  пропущена) запрещена.
- Само-авторизация решений владельца: судьба 26 внезеркальных сьютов,
  2 групп идентичного покрытия и одобрение контракта v5 — владельческие.
- Удаление сьютов и любое сокращение покрытия без доказанной идентичности
  покрытия и без решения владельца.
