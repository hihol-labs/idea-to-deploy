# ROUTE-REPAIR-1

Current unit ROUTE-REPAIR-1, medium risk. Owner-approved order (2026-09-12, superseding the 2026-09-08 order): route repair BEFORE RSI-DEBT-3, because the three recorded route defects are paid on every unit and the dominant one was measured at about three hours of wall clock on one medium-risk unit. Queue: ROUTE-REPAIR-1 -> ROUTE-REPAIR-2 (claim-id circle + live-benchmark pin cascade) -> ROUTE-REPAIR-3 (the metric sees the route) -> RSI-DEBT-3. One plan item is one session.

Measured defect, attributed 2026-09-12 from the per-round reports of the RSI-DEBT-2 series. Ten consecutive live cross-vendor rounds produced zero false positives and no clean PASS. The per-round reports were read while they still existed in the producing session's working directory: 16 finding rows, 14 distinct file-and-line pairs, across four files. The earlier session's record says twelve findings were closed; the two counts disagree, the source reports are gone with that session's directory, and the discrepancy is recorded rather than reconciled. Round 1 named line 74 of one file, round 2 line 69, round 3 line 72. The candidate was 230 KB, so the review ran hierarchically over sixteen bound ranges plus one integration call, and the aggregation unions every unit's findings without short-circuiting. So the cost was not a growing surface and not a coverage gap: the yield of one review call was one to two findings while the bound material held nine distinct ones over rounds 1-7 alone. The prompt contract never asked for more - `PASSED requires findings=[]` means a single high finding already justifies BLOCKED, and listing the rest earns the call nothing. Ten serial rounds paid for that silence.

Allowed: one module constant `ENUMERATION_REQUIREMENT` in `skills/_shared/itd_free_reviewer_producer.py`, emitted from the single trusted tail `_trusted_json_output_contract`, so the direct, unit and integration prompts inherit it verbatim; the new RED-first oracle `tests/verify_review_enumeration.py`; the DECISIONS entry recording the superseded order; the two BACKLOG P1 candidates opened at the close of session 2026-09-12 and the amendment that corrects the recorded cause of the non-convergence P1; canonical unit ledgers and the acceptance rows of this unit. Pin outcomes are measured, not assumed: the live benchmark pin does NOT cover this change - `BENCHMARK_PIN_ROOTS` holds 60 files, the producer is not among them, and the pin digest did not move - while the efficacy pin does, so its three legs are re-minted live and archived. The stale authority snapshot predating this branch is re-minted from merged main.

Required: both legs of the sealed verificationCommand exit 0 - `tests/verify_review_enumeration.py` and `tests/verify_free_reviewer_producer.py`. RED-first is observed on the pre-fix bytes for the same candidate before the fix is accepted, twice in a row. The criterion requires four lethal mutations, and they are the four it names: the clause removed; the clause moved into the prompt header ahead of the untrusted material; the prompt kinds diverging, realised as one kind building a private copy instead of inheriting the tail; the unit kind losing the clause. Six additional mutations are measured and recorded beyond the criterion: the clause emitted outside the trusted output block, because the placement guarantee has two distinct failure modes and only one of them is the prompt header; the severity sentence inverted so that rank does license omission; the invention sentence inverted so that guessing is instructed; an untrusted section appended after the trusted tail; the unit clean example given a non-empty findings list; and the completeness duty directly negated. They exist because independent review found two holes in the checks themselves: keyword presence accepts its own inversion, since `severity` and `invent` appear in both polarities (round 1), and a direct negation carries every keyword the positive form carries, since `do not list every defect` passes any presence test (round 5). The content checks were rebuilt around polarity, each duty now rejects its own inverted clause, and marker offsets were replaced by the stronger invariant that the trusted tail is the prompt's suffix. The recorded set is a superset of the required four, named here as a superset so the ledger, this scope and the acceptance contract cannot be read as three different requirements. The full mirror's ACTUAL last line is recorded with every red suite attributed to this candidate or to pristine main by a clean-worktree run, and the measured outcome of every pin this change touches is recorded, instead of either being asserted. The two clauses that asserted a sealed mirror line and a benchmark-pin re-record were amended out by the owner on 2026-09-12 after the stop rule returned REDESIGN_OR_DISCARD; both premises were measured false. A `/review` route before the multi-file commit, then the cross-vendor producer; existing accepted evidence stays immutable.

Forbidden: touching `PE5-008` or `PE5-009` (blocked until real external adoption evidence); changing `VERDICT_SCHEMA` or `UNIT_VERDICT_SCHEMA`; weakening the closed output contract or the empty-list condition for `PASSED`; asking the reviewer to guess, since the measured false-positive count of this route is zero and guesses would buy that class back; introducing a round ceiling, which `.itd/STOP_RULE_POLICY.json` forbids by construction with its own measurement; building the delta-review mechanism here (it belongs to ROUTE-REPAIR-2 and, on this series' attribution, would have hidden the head findings that lie away from the fixed lines: rounds 3, 4, 7 and 10 named lines 72, 93, 35 and 96 while the lines actually being fixed were 74 and 69); `--no-verify` (if a gate refuses, the exact command goes to the owner); starting RSI-DEBT-3 in this session.
