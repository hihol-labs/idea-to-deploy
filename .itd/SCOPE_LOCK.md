# ROUTE-DEBTS-FOLLOWUP-A13

Current unit ROUTE-DEBTS-FOLLOWUP-A13, high risk. Frozen surface: the four Sol-a13 findings deferred by owner decision on 2026-09-07 (BACKLOG P1), nothing else.
Allowed: skills/task/scripts/itd_unit_log.py (state_write_lock through the anchored no-follow namespace helper, regular single-link file), tests/verify_independent_review_efficacy.py (validate_current_result_archive_binding through the anchored no-follow reader), skills/_shared/itd_safe_atomic_windows.py (private append owner SID check mirroring the POSIX st_uid rule), skills/_shared/itd_review_evidence.py (active_criteria fails closed on an explicit row with matching unitId and a non-string id); matching RED-first regressions in the existing suites; canonical unit ledgers, the acceptance contract rows of this unit, generated impact metadata and DECISIONS/BACKLOG entries.
Also allowed, and named here after Sol-fa3 raised it as a scope finding: skills/_shared/itd_safe_atomic.py, strictly for the shared anchored no-follow primitive the first criterion requires (`open_private_lock_fd`) and the label parameter that lets it name its own refusal. The criterion approved at activation says the lock must be taken *through the anchored no-follow namespace helper*, and that helper is this module; the earlier wording enumerated the call sites and omitted the helper's own home, so the surface is clarified rather than widened. Existing callers of that module keep their behaviour and their messages.
Required: every fix lands with a regression that is RED on the pre-fix bytes and GREEN on the candidate, and every guarantee is proved lethal by mutation; the ROUTE-DEBTS regression oracle and portable host-metadata gates exit 0; existing accepted evidence stays immutable; publication remains evidence-gated through the local-review registry.
Forbidden: new sites beyond those above (surface treadmill), unrelated features, new provider routes, lowered gates, edits to live-model evidence content or efficacy history, changes to the Q6 campaign, pilot decisions, later unit activation, release or rollout inside this unit.
