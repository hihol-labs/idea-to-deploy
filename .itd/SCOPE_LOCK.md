# RSI-ROUTE-P1

Current unit RSI-ROUTE-P1, medium risk. Owner-approved order (2026-09-08): P1 producer fix -> release v1.104.0 with rollout -> RSI-DEBT-2 -> RSI-DEBT-3.
Allowed: `skills/_shared/itd_free_reviewer_producer.py` (`_transparent_review_representation` and the chunk builder it uses; the chunks must be cut from the same scrubbed diff text the function returns, the way `_raw_review_file_chunks` already cuts them from `_safe_review_text` output), `tests/verify_free_reviewer_producer.py` (RED-first regression on a sealed fixture: one declared `.jsonl.gz` transparent file plus one redactable line, refused on the pre-fix bytes with "hierarchical review unit coverage is invalid" and partitioned on the candidate), any test that pins the producer's transparent representation and must follow by construction; canonical unit ledgers, the acceptance contract rows of this unit, generated impact metadata and DECISIONS/BACKLOG entries.
Required: RED on the pre-fix bytes and GREEN on the candidate, observed rather than asserted; the raw-bytes mutation lethal; the unit oracles (`verify_free_reviewer_producer`, `verify_scrubber_precision`) exit 0; `/review` subagent before the commit (population of the blind protocol; the final message carries a prose `Verdict:` line), then the cross-vendor producer; existing accepted evidence stays immutable.
Live-model evidence, stated precisely: this unit changes `skills/`, so CI Gate 1's pin needs one fresh `fixture-03-cli-tool` run captured on the CLEAN committed tree as its own follow-on commit; every existing run stays byte-identical.
Route constraint carried from RSI-DEBT-1: the producer that reviews this candidate is the INSTALLED authority (1.103.1) and still has the defect this unit fixes, so a staged candidate must not carry a `.jsonl.gz` change together with a redactable line; the re-pin therefore lands after the code commit, and the committed-head claim may again be served by the staged receipt on the same HEAD tree (registry precedent PR #272).
Forbidden: new sites beyond those above (surface treadmill), any change to `itd_external_reviewer.py` or `itd_review_broker.py`, relaxing the sensitive-material refusal, new provider routes, lowered gates, changes to the Q6 campaign or the blind protocol, pilot decisions, release or rollout inside this unit (REL-1.104.0 is the next unit), later unit activation.
