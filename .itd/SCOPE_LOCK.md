# SCOPE_LOCK — LPD003-REMEASURE (открыт 2026-08-27)

## Предмет
N6: запись результата перемера машинного слоя (разрез GATE G0, эры до/после
merge LPD-003-1) и владельческого вердикта «NO-GO по GENG-B подтверждён» в
durable-леджеры репо. Кода нет; сам замер живёт вне репо
(`~/.claude/geng/N6/{remeasure.py,BASELINE_N6.md}`, конвенция S02-S05).

## Разрешённые области
- `.itd/DECISIONS.md` — ТОЛЬКО append одной записи «N6 / LPD-003-REMEASURE»
  (числа обеих эр, вердикт владельца, ограничения честности).
- `.itd-memory/STATE.json` — активация/verified юнита LPD003-REMEASURE
  (события пишет itd_unit_log), nextAction -> N7.
- `LAUNCH_PLAN.md` — ТОЛЬКО Block J: чекбоксы закрытых LPD-003-1/2/4 и
  абзац результата N6 (числа + вердикт + остаток плана).
- `.itd/ACCEPTANCE_CONTRACT.json` — критерий LPD003-REMEASURE-1 и переключение
  activeFollowup на этот юнит (однокандидатный жизненный цикл записи:
  открытие и закрытие в одном кандидате, что явно объявлено в scopeNote).
- `.itd-memory/measurements/LPD003-REMEASURE-n6.json` (замороженные числа
  замера, срез createdAt < 2026-08-27T18:45Z) и
  `.itd-memory/measurements/LPD003-REMEASURE-check.py` (fail-closed пересчёт
  из квитанций) — конвенция коммитнутых замеров N4/N5. Сверка исполняется
  ЛОКАЛЬНО на чекауте с квитанциями; в изолированной машинной ноге она
  неисполнима по построению — движок Verification Loop запрещает собственные
  улики (verification-loop/) как вход ноги, поэтому квитанции в изолят не
  поставляются ни через --input, ни иначе.
- `.itd/SCOPE_LOCK.md` — этот файл.

## Запрещено
- Любая правка tests/, skills/, hooks/, agents/, scripts/, docs/ (кроме
  перечисленного), .github/ — юнит не меняет поведение.
- Пересчёт или правка чисел задним числом: числа в записи обязаны совпадать
  с воспроизводимым выводом `~/.claude/geng/N6/remeasure.py` на квитанциях
  обеих локаций .itd-memory/verification-loop/.
- Само-авторизация решений по GENG: вердикт в записи — владельческий
  (2026-08-27), исполнителю запрещено его расширять или сужать.
