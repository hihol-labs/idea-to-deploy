# SCOPE_LOCK — PILOT-P02-CLOSE (открыт 2026-09-04; предмет PILOT-P02, открыт 2026-09-02)

## Предмет
Слепой семантический протокол приёмки из `.itd-memory/ADVISORY-RSI-2026-08-27-v4.md`
§5: владелец категоризует 10 случайных НОВЫХ находок по тексту находки,
авторская метка скрыта, инструкция — описания значений словаря; совпадение
>= 8/10 принимает пилот. Это smoke-приёмка, НЕ статистическая гарантия
точности 80%.

## Замер ДО правки (первый шаг, выполнен 2026-09-02)
`.itd-memory/review-findings.jsonl`: 260 записей, 286 findings.
- Со штампом `taxonomy_version`: **0/260**. Популяция «новых находок» ПУСТА.
- Установленный runtime (`~/.claude/`) не содержит `VERDICT_TAXONOMY.json`,
  `itd_verdict_taxonomy.py`; в установленном `verdict-contract.sh` слово
  taxonomy не встречается ни разу. PR #253 в main, но не раскатан.
Вывод: до раскатки популяция не пополняется, вердикт вынести не на чем.

## Разрез (утверждён владельцем 2026-09-02, паттерн §5)
1. **PILOT-P02 (этот юнит, SETUP)** — замороженная машинерия протокола:
   популяция, воспроизводимая выборка, слепой лист, скоринг с порогом,
   честная маркировка, fail-путь, tamper-evidence, одна попытка на печать.
   Машинный оракул на запечатанных фикстурах.
2. **Наблюдение** — накопление новых записей после раскатки, БЕЗ WIP.
3. **Decision-юнит (отдельный)** — печать, разметка владельцем, скоринг,
   вердикт. Он и есть гейт для Q2.
Основание: §5 прямо запрещает держать WIP на календарном ожидании данных.

## Допущения владельца
- Порог 8/10, размер выборки 10 и состав словаря берутся из advisory как есть.
- Слепота обеспечивается составом ЛИСТА, а не криптографией: леджер лежит
  рядом и читаем. Это записано в самом протоколе как ограничение.
- Согласие считается между владельцем и моделью-автором записи, НЕ между
  двумя людьми; evidentiary claim ослаблен соответственно.

## Разрешённые области
- `skills/_shared/BLIND_PROTOCOL.json` — новый замороженный контракт замера.
- `skills/_shared/itd_blind_protocol.py` — машинерия и CLI (seal/worksheet/score).
- `tests/verify_blind_protocol.py` — оракул на запечатанных фикстурах.
- `tests/run-all.sh`, `scripts/itd_install_runtime.py` — регистрация сьюта и
  файлов в манифесте установленного runtime.
- **Разблокировка (объявлена явно):** `tests/verify_runall_drift.py` — сверка
  замороженного замера консолидации LPD003-4 с ТЕКУЩИМ деревом требовала
  точного равенства числа сьютов, то есть запрещала добавить сьют вообще.
  Заменено на монотонность (дерево не имеет права упасть ниже замеренного);
  отмену самой консолидации по-прежнему ловят проверки доноров, кипера и
  узлового множества. Изменение минимально и не трогает других гарантий.
- Леджерная бухгалтерия юнита: `.itd-memory/STATE.json`,
  `.itd/ACCEPTANCE_CONTRACT.json`, `.itd/SCOPE_LOCK.md`, `.itd/DECISIONS.md`,
  `BACKLOG.md` (факты маршрута и долги при ledger-close — прецедент STOPRULE-1/2;
  заход close1 продюсера назвал отсутствие файла в этом списке),
  `.itd-memory/contracts/PILOT-P02-CLOSE.md`, `.itd/IMPACT_GRAPH.json` (производный:
  регенерируется `tests/build_impact_graph.py`, руками не правится),
  `.itd-memory/events.jsonl` (append-only журнал: каждый переход юнита
  дописывает в него событие, поэтому он неотделим от бухгалтерии закрытия;
  объявлен в `ledgerFiles` базового STATE).

## Явно вне скоупа
- **Вынесение вердикта** — нет популяции; это decision-юнит.
- **Раскатка таксономии на хост** — релизная работа, отдельный юнит.
- **Q1-Q9** discovery-backlog: не активируемы, пока контракт не заполнен.
- Класс враждебного содержимого словаря (BACKLOG P2 из PILOT-P01).

## Закрытие (PILOT-P02-CLOSE, 2026-09-04)
Кандидат PILOT-P02 был заморожен в индексе параллельной сессии (дерево `64d6b4bc`,
оракул 153/0, 20 заходов / 46 находок, r58/r59 закрыты) без коммита: серия ревью
не имела критерия остановки. Критерий теперь есть (STOPRULE-1/2, PR #255/#257):
код кандидата перенесён на свежую ветку от main `75bd05c` без правок по существу,
далее — заход продюсера; при BLOCKED стоп-правило (`scripts/itd_stop_rule.py`)
решает по истории серии, при терминале решения владельца правило составляет
диспозиции (`--emit-dispositions`), владелец подписывает, adjudicated-маршрут, PR.

## Статус 2026-09-04 — ЗАКРЫТ
PR #259 merged `bf692ab` (дерево `af3d71b9`). Серия: r1-r3 отказы предусловий (префикс id
критериев, детектор энтропии на длинных id, oracleIds без команды квитанции), r4-r6 BLOCKED
(7 находок: 5 закрыты кодом/леджером с летальными мутациями, 2 low подписаны владельцем как
accepted-trade-off и закрыты в ledger-close). Стоп-правило 1.2.0 остановило серию на r6
(REDESIGN_OR_DISCARD: `contracts/PILOT-P02-CLOSE.md::specification-compliance` в r4 и r6).
Второй живой прогон маршрута «правило составляет, человек подписывает».
