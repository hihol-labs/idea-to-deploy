# ROUTE-DEBTS

Current unit ROUTE-DEBTS, high risk. Approved RSI route repair only.
Allowed: relevant shared verification/reviewer/gate modules, risk hook, goal/task writers, stop-rule CLI, matching tests/docs and registration/impact metadata; current real efficacy results and their immutable history; release manifests/runbook outputs and canonical unit ledgers.
Required: all ten invariant groups in .itd-memory/verification-loop/reports/ROUTE-DEBTS-contract-check.md. Existing accepted evidence stays immutable. Publication remains evidence-gated.
The approved contract requires current live efficacy evidence when the producer footprint changes. Preserve the exact protected-base result bytes and subsequent diagnostic observations in the tracked, hash-checked history archive before refreshing the current `results/` views. New results must be new real signed observations; never edit, re-date or re-sign an archived observation. Historical snapshots cannot satisfy current-producer applicability checks.
The matching-test repair for EF8 is limited to the percentile example's implementation and regression assertions, supported by its executable counterexample and retained original corpus bytes/hash. The cohort, labels, thresholds and original cohort freeze date stay fixed. Corrected-corpus runs are separate observations and do not erase EF8 or claim improved model effectiveness.
Forbidden: unrelated features, new provider routes, lowered gates, reviewer freshness date-only edits, changed CMP formula/instrument/frozen campaign, pilot decision or later unit activation.
