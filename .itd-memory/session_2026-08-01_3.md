---
name: session_2026-08-01_3
description: "GPG-001 free isolated reviewer accepted; live App/ruleset/provenance protection is next"
type: project
---

## Дата

2026-08-01

## Проект

`/home/hihol/projects/idea-to-deploy`, ветка
`codex/harness-lifecycle-trust`, HEAD `7d463b6`, four commits ahead of origin.
WIP remains exactly one: `GPG-001`. No new GOAL was created.

## Резюме

The bounded free isolated reviewer producer slice is implemented and accepted.
The staged exact candidate is
`e2ab8afbe092bc3bb00b7c376ef72411a7bc738c`. It includes the new producer,
review-broker and server integration, deployment/policy contracts, TDD tests,
and AC15 evidence.

The implementation provides:

- fresh ephemeral `gpt-5.6-terra` through the native Codex subscription
  transport, with pinned executable content and strict subscription auth;
- only exact candidate, frozen scope/acceptance, and machine evidence as model
  input; no inherited development context, repository access, secrets,
  mutation tools, or enabled network tools;
- content-pinned proxy handling and rejection of conflicting ambient proxy or
  auth state;
- strict output schema and zero observed tool calls;
- signed Ed25519 phase-one and phase-two exact receipts;
- App revalidation of repository, PR, base/head/tree/parent/merge/check,
  scoped producer keys, maker provenance, pre-PATCH race closure, and durable
  publication recovery;
- fail-closed findings, unavailability, stale/forged evidence, and errors;
- no automatic paid fallback. Paid review requires separate user consent plus
  an explicit operation request, which the webhook path does not provide.

## Ключевые решения

- The free subscription reviewer remains the primary transport because the
  required property is fresh-context independence plus exact binding, not a
  paid request. Paid API stays disabled unless the user separately approves a
  specific operation and budget.
- Checkpoint documents remain unstaged because changing them after the clean
  adjudication must not silently redefine the accepted exact candidate. They
  must be included deliberately and re-reviewed before commit.
- Live App/ruleset/provenance evidence is the next phase because a local signed
  receipt proves only the local producer path, not GitHub-side protection.

These decisions preserve the durable reviewer-transport decision already
recorded in `.itd/DECISIONS.md`; no new durable policy was introduced here.

## Изменённые файлы

- Producer: `skills/_shared/itd_free_reviewer_producer.py`.
- Broker/runtime: `itd_review_broker.py`, `itd_review_broker_primitives.py`,
  `services/review_broker/server.py` and deployment files.
- Contracts/policy: AC15, verification contract, broker policy and schema.
- Tests: producer, broker, server, deployment and quick-suite wiring.
- Checkpoint only, unstaged: `HANDOFF.md`, `.itd-memory/STATE.json`; ignored
  session memory and active-session lock.

## Verification

- `tests/verify_free_reviewer_producer.py`: 53 passed.
- Broker: 721 passed; server: 50 passed; deployment: 25 passed.
- High-risk tool trust: PASS; operational cold start: all pass;
  meta-review: PASSED; host adapters: PASS.
- `bash tests/run-all.sh --quick`: `DONE fails:none`.
- Machine receipt:
  `.itd-memory/verification-loop/receipts/1dd8d0697e2f5434/GPG-001-general-review-machine-e5847e026cd1dd5b.json`.
- Clean report:
  `.itd-memory/verification-loop/reports/GPG-001-final-e2ab8afb-terra.json`.
- Checker receipt:
  `.itd-memory/verification-loop/receipts/1dd8d0697e2f5434/GPG-001-general-review-checker-757b490721eaca3c.json`.
- Final adjudication:
  `.itd-memory/verification-loop/receipts/1dd8d0697e2f5434/GPG-001-general-review-adjudication-a1.json`.
- Fresh reviewer: `openai-codex-subscription` / `gpt-5.6-terra`, session
  `019fbeaf-e1e2-7da1-a543-dfdd14071572`.

The exact staged tree remains accepted. This session checkpoint updates
tracked `HANDOFF.md` and `.itd-memory/STATE.json` only in the working tree,
intentionally unstaged, so it does not silently change the accepted staged
candidate. Before a later commit, stage the intended checkpoint documents and
run a new exact freeze/checker/adjudication.

## Блокеры / открытые вопросы

Draft PR #177 remains open and unmodified. Live base is
`c52eca984ae61ce474fa1151a894bde2382e064d`; remote head is
`e0384d62b356064b12e336e92ffc775001cdf7b2`.

`/home/hihol/.config/itd/gates.json` is absent, so guarded push is correctly
blocked and live protection is not proven. Current `gh` auth lacks
`admin:org`; organization ruleset access requires it, and App installation
enumeration is unavailable under the current token. A stable HTTPS broker URL
also has not been selected. Docker is installed; `cloudflared` and `caddy` are
not. No live GitHub or publication mutation was performed during discovery.

The local exact receipt must never be described as proof of the live GitHub
App/ruleset boundary.

## Следующие шаги

1. Open this exact WSL directory after ending the old session.
2. Read `AGENTS.md`, `HANDOFF.md`, `.itd-memory/STATE.json`, this file, the
   nine-point plan, AC15/AC16, and the verification contract.
3. Read-only verify branch/HEAD/status, `git write-tree == e2ab8afb...`, the
   final adjudication, PR #177 coordinates, missing gates registry, and current
   GitHub scopes.
4. The checkpoint edits to tracked `HANDOFF.md` and `STATE.json` reopened the
   general risk bucket. Include them deliberately in a newly frozen candidate
   and obtain a new successful bound machine/checker/adjudication receipt
   before any further implementation, live mutation, or commit.
5. Continue the already authorized live slice: choose/provision a stable HTTPS
   broker host, perform the user-interactive GitHub App/admin authorization,
   configure ruleset/provenance and negative canaries, then require
   `itd gate doctor --all = PROTECTED`.
6. Only after live protection: freeze and revalidate the final exact candidate,
   commit, then publish exclusively through `itd pr create` to update Draft
   PR #177.

Do not use raw `git push`, `--no-verify`, paid API, merge, release, repository
secrets, or installed-plugin-cache edits. Any step requiring browser login or
secret material should pause for the user's interactive action without
printing or storing the secret.

## Неочевидный контекст

- Draft PR #177 must be updated only through `itd pr create`; the existing
  remote branch is intentionally stale while the global gate registry is
  absent.
- The full live-PR base-to-candidate diff is about 1.42 MB, above the 1.2 MB
  producer cap. It must not be truncated; the accepted receipt covers the
  bounded staged delta over the previously accepted base.
- A fresh Codex desktop session should open the same WSL filesystem path. The
  active lock belongs to the previous Codex process and is only a WIP warning;
  after that process stops it expires automatically.

## Long-goal snapshot

`.itd-memory/GOAL.json` remains unchanged at 36/38 verified; PE5-008 and
PE5-009 remain blocked by the previously paused external outcome evidence.
This historical long goal is not the current WIP and must not be replaced.

## Branch finish

Keep branch mid-task. No commit, publication, PR update, merge, release, or
methodology sync was performed in this checkpoint.
