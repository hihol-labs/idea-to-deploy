# Q6-SETUP task contract

Authority: owner continued the approved RSI plan on2026-09-05; Q6 is next after REL-1.103.0. Risk: medium. WIP=1.

Scope: preregistration and experimental stdlib freeze/validate/reconcile/read-only scoring under benchmarks/cmp. No D3 result or operational scoring integration. The final criterion and executable command are only in GOAL.json units[id=Q6-SETUP]. The real campaign is initialized after accepted merge, before verified; pre-commit criteria cover instrument behavior only.

Required preregistration: explicit prospective lineage and complete first-outcome inventory, source/event/receipt identity bindings, exposure/time cutoff, fixed outcome-independent cohort, ties/missing rules, blind human rubric and allowlist;90day window,20units, rho>=.6; statistical decision algorithms/attempt policy frozen but implementation deferred. Current vendor/model/major/prompt-policy context must be explicit, with drift rejected. Read-only is tested by preserving input files and excluding operational consumers.

Baseline: zero usable prospective CMP pairs;37 canonical GOAL verifications in previous90d (all units, no qualifying-flow forecast). Manual ranking baseline N/A because no comparable formal workflow exists; cannot claim ROI now. Expected benefit hypothesis30min/month after validation; six-month post-D3 payback target remains unmeasured. Budget16h setup,1h/month collection,90d observation,180d data timeout. Deterministic setup tests have no statistical CI (N/A); future D3 methods/limitations are frozen in PROTOCOL.json.

Rollback: stop using/archive the campaign and instrument; no original lifecycle ledgers, policy, reward or skills are automatically changed. Raw data/privacy follow advisory §7. Future decision uses the frozen protocol and one attempt per freeze, after maturity; no next unit is activated here.

Verification: focused positive/negative tests on real-schema synthetic fixtures plus normal host/CI/drift checks, exact medium receipt and fresh independent review. Fixture results are setup engineering evidence, not empirical CMP validation.

Recovery authorization (owner, 2026-09-05): after BLOCKED, confirmed Q6 defects and necessary tests may be repaired within current scope. Preserve all negative evidence; no publication or risk paydown before a successful exact-candidate review. This is not approval of the failed candidate.
