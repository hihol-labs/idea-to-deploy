# Task Contract: PRG-003 — installed exact-context closure

## Scope

- Add the one path-loaded exact-context review-cache dependency to the closed
  installed runtime inventory.
- Add an executable isolated-runtime regression, not a source-file existence
  assertion.
- Reinstall and canary the resulting runtime on WSL and native Windows.

## Verification Standards

- `python3 -I tests/verify_itd_runtime_install.py` on WSL and native Windows.
- Installed `itd_verification_loop.py` loads its review-cache dependency under
  Python `-I -B`.
- A fresh exact-candidate machine/checker/adjudication chain precedes PR
  publication.

## Exclusions

- No reviewer, receipt, keyring, gate, hook, version-parser, broker/App or
  server-enforcement semantic changes.
- No additional runtime files without a demonstrated executable dependency.
