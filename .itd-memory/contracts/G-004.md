# Task Contract: G-004 — /adopt и /project предлагают установить enforcement-хуки

## Scope
- Новый stdlib-хелпер `skills/_shared/itd_project_hooks.py`: `plan` (read-only, печатает, что добавится) и `apply --yes` (идемпотентный merge шаблона по `command`-пути в `<root>/.claude/settings.json`); без аргументов - no-op.
- `skills/adopt/SKILL.md` и `skills/project/SKILL.md` заканчиваются явным шагом: показать план хелпера, спросить подтверждение, при «да» - `apply --yes`, при «нет» - файл не трогать.
- `README.md` / `README.ru.md`: вместо «not auto-installed» - описание шага.

## Verification Standards
- `sh skills/_shared/itd_py.sh tests/verify_hooks_autoinstall.py && ! grep -q "not auto-installed" README.md README.ru.md` (verificationCommand юнита).
- Оракул RED на дофиксовых байтах (main 49a149e) и GREEN после; покрывает: создание файла, повторный apply побайтно идентичен (нет дублей), чужие хуки и прочие ключи сохранены, отказ/plan/no-arg не меняют байты и не создают файл, битый JSON -> ненулевой exit с WHY/FIX и файл не тронут, user-level `~/.claude/settings.json` не пишется, шаги в двух SKILL.md.
- `tests/meta_review.py --verbose` PASSED без Important; `bash tests/run-all.sh --quick` зелёный.

## Exclusions
- Состав хуков в шаблоне, сами хуки, user-level установка (`sync-to-active.sh`).
- Codex-адаптер. Фикс байткода `tier_exempt` (BACKLOG P2) - отдельный юнит.
