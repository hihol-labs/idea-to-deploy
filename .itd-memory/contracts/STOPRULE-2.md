# Task contract — STOPRULE-2 (medium)

## Scope
1. `--emit-dispositions <checker-receipt> --out <file>`: черновик диспозиций
   ADR-007 на терминалах решения владельца; дайджест и фраза подписи — из
   `itd_verification_loop`; класс/основание/подписант — плейсхолдер.
2. Политика 1.2.0: `status: decides-with-human-confirmation`, блок
   `humanConfirmation`; заморожены загрузчиком.
3. Оракул: черновик, валидатор маршрута (принимает заполненный, отвергает
   незаполненный), отказы, CLI; документация согласована.
4. Закрытие PILOT-P02 этим маршрутом после публикации кода (подпись владельца).

## Verification standards
- RED-first: политика 1.2.0 краснит старый оракул на статусе; код возвращает
  зелёный; каждая гарантия — своя проверка и своя мутация (10/10 летальны).
- Оракул `tests/verify_stop_rule.py`: 564 -> 580, 0 красных.
- Независимое ревью: cross-vendor продюсер (gpt-5.6-sol) по exact staged tree,
  затем committed-head для публикации.
