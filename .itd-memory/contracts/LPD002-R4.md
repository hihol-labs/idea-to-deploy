# Task Contract — R4 (LPD-002)

## Scope
Пять мелких трений маршрута, измеренных ретро 2026-08-18: (a) efficacy-оракул
запускается в изоляции, (b) раннер не обещает несуществующего ретрая и не
стирает чекпоинт, (c) `--risk-tier` при активации юнита, (d) канонический
`PASSED` в шаблоне checker-промпта, (e) названный и объяснённый цикл «verified
без активации». Полный скоуп и зоны правок — `.itd/SCOPE_LOCK.md`.

## Verification Standards
- Оракулы (имена — те же, что в `machine --command`):
  - `efficacy-isolation-oracle` = `python3 -I tests/verify_independent_review_efficacy.py --expected-keyring-sha256 dd4c6dea0fd05351ae5026d36635618573938c0fc1d8d0b49605866c18ddc5e2`
  - `unit-log-oracle` = `python3 -I tests/verify_unit_log.py`
  - `verification-loop-oracle` = `python3 -I tests/verify_verification_loop.py`
  - `ledger-reconciliation-oracle` = `python3 -I tests/verify_ledger_reconciliation.py`
  - `goal-tools-oracle` = `python3 -I tests/verify_goal_tools.py`
  - `retro-scan-oracle` = `python3 -I tests/verify_retro_scan.py`
  - `gate-registry-binding-oracle` = `python3 -I tests/verify_gate_registry_binding.py`
- RED до правки (замерено): (a) `AssertionError: review keyring pin path is not
  the host contract` — argparse разбирал `--expected-keyring-sha256` как
  аббревиатуру файлового флага; (b) `runner still exposes a retry knob it does
  not honour`; (c) `error: unrecognized arguments: --risk-tier`; (d)
  `docs/templates/CHECKER_PROMPT.md` не существует; (e) счётчика
  необъяснённых аномалий нет.
- Мутации в обе стороны (каждая валит именованную проверку):
  1. вернуть `--max-transport-attempts` -> `runner exposes no retry knob…`;
  2. `finalize_checkpoint` -> `unlink` -> `successful run preserves the
     checkpoint as <path>.done`;
  3. `TRANSPORT_ATTEMPT_BOUND = 3` -> `transport attempt bound is not the
     decided 1`;
  4. `--risk-tier` с `default="low"` -> `activate-without-risk-tier-refused`;
  5. запись STATE до отказа по тиру -> `refused-activation-writes-nothing`;
  6. расширить `RISK_TIERS` посторонним значением ->
     `risk-tiers-match-proportionality-policy`;
  7. заменить в шаблоне `PASSED` на `PASS` -> `checker prompt template ends
     with the canonical verdict block`;
  8. убрать из шаблона любой вердикт закрытого множества -> `checker prompt
     template names every allowed verdict`;
  9. считать объяснённую строку аномалией (снять фильтр `_explained`) ->
     `an explained row stays visible but stops being an anomaly`;
  10. принять запись реконсиляции с пустым `why` -> `an entry without why
      explains nothing`;
  11. считать аномалией цикл С активацией -> `a properly activated cycle is
      never flagged as an anomaly`;
  12. `parse_caller_pin` без проверки формы (принимает `A`*64) -> `malformed
      caller keyring pin was accepted`;
  13. снять пин keyring'а из `.itd/VERIFICATION_CONTRACT.json` -> `efficacy
      entry lost its required keyring pin flag`;
  14. запинить в контракте чужой дайджест -> `tracked keyring pin does not
      match .itd/REVIEW_EFFICACY_KEYRING.json`;
  15. вернуть `spec.loader.exec_module` в `load_module` -> `oracle judged
      cached bytecode, not the source`.
  Канарейки на ПЕРЕблокировку: терминалы (`verified`/`close`/
  `backfill-activation`) флага `--risk-tier` не требуют; форма-значение и
  форма-файл обе принимаются, но взаимоисключающи; правильно активированный
  цикл не помечается аномалией.
- Изоляция (критерий (a)) проверена фактом: в дереве только tracked-файлов
  (`git ls-files` -> копия) форма-значение даёт exit 0
  (`keyringAuthorization: caller-pin`), форма-файл невозможна — `host-inputs/`
  не tracked.
- Полный `bash tests/run-all.sh` -> `DONE fails:none`.
- Перечеканки на итоговом дереве отдельными коммитами (обе обязательны):
  три подписанные efficacy-ноги (правка раннера меняет `runnerSha256`) и
  live-model-benchmark (`skills/` входит в `methodology_tree_sha256`);
  `python3 tests/verify_live_model_benchmark.py --require-evidence` -> зелёный.
- Маршрут ревью на коммит (riskTier low = `checkerMode machine_only`,
  `checkerRequired false`, `minimumIndependentReviewers 0`):
  `itd_verification_loop.py machine` (claim `LPD002-R4:general-review`) ->
  `adjudicate` -> `itd_review_cache.py record`. Чекер по политике не требуется
  (продюсер отвечает `NOT_REQUIRED`).
- Перед PR — ОТДЕЛЬНЫЙ pre-PR claim противоположным вендором
  (`--candidate-mode committed-head`, продюсер из authority-снапшота
  `REL198-1b38d1c8-a1`).

## Exclusions
`tests/run-all.sh` остаётся на host-owned пине (fail-closed без него) — форма
-значение слабее по авторизации и добавлена рядом, а не вместо. Диапазон
`--max-transport-attempts` не расширяется (`.itd/DECISIONS.md:214`, `:447`).
`checker --dry-run` не вводится. Механизм пинов не пересматривается. Пункты
R5-R6 не открываются до `verified` R4.
