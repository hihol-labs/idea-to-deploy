# Task Contract — LPD002-A39 (сессия долгов LPD-002)

## Scope
Две маршрутные мелочи, обе с измеренными инцидентами:
- **A3** (чекер имеет write-доступ — R1: `git stash` при живом кандидате).
  Обе стороны привязки УЖЕ fail-closed в коде
  (`assert_checkout_matches_candidate` при чеканке; отказ
  `checker was not observed on the exact reviewed tree` при валидации), но
  validate-сторона не была покрыта оракулом — мутация сняла бы её незаметно.
  Добавлена проверка: чекер-квитанция с подделанным `inspectedTree` и
  ПЕРЕсчитанным receiptSha256 отвергается на adjudicate именно tree-гвардом.
  Остаток (истинный read-only для субагента) — harness-фича, вне контракта
  репозитория (best-effort invariant), зафиксирован в BACKLOG.
- **A9** (`tests/run-all.sh`): красный сьют печатает свои строки
  `FAIL/FAILED` (head -20) перед tail -6 — флейк класса
  `verify_state_hardening 90/1` становится воспроизводимым по имени
  проверки.

## Verification Standards
- `verification-loop-oracle` = `python3 -I tests/verify_verification_loop.py`
  (87 -> 88 проверок).
- `runall-drift-oracle` = `python3 -I tests/verify_runall_drift.py`.
- Мутации: подделка inspectedTree с валидным digest -> отказ (не
  digest-гвардом, а tree-гвардом); честная квитанция проходит (соседние
  зелёные проверки сьюта).

## Exclusions
- Истинное read-only исполнение чекера-субагента — enforcement уровня
  харнеса, не репозитория.
- Остальные долги — свои юниты.
