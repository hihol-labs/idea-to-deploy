# Task Contract: PRG-004 — Codex live write boundary

## Scope

- Add one explicit Codex-native `apply_patch` write instruction to the live
  blueprint fixture prompt.
- Require it in the runner's preflight and pin that enforcement with a
  positive check plus a real temporary-fixture mutation.
- Hand off to one separate clean-HEAD evidence follow-up; generated evidence
  is written only by the existing bounded runner.

## Exclusions

- No sandbox, approval, model fallback, snapshot oracle, pin policy,
  methodology skill, runtime gate or reviewer semantic changes. Runner change
  is limited to rejecting a prompt without the directive before model dispatch.
- No manual edits to generated live evidence.

## Verification

- `python3 -I tests/verify_live_model_benchmark.py`
- `python3 -I tests/verify_live_model_benchmark.py --evidence .itd-memory/PRG-004-no-evidence.json`
- Clean-HEAD follow-up: runner + `--require-evidence --max-age-days 30`
