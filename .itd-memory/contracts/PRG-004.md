# Task Contract: PRG-004 — Codex live write boundary

## Scope

- Name both host boundaries in the live prompt: Codex native `apply_patch`;
  Claude main-session direct skill execution with built-in Write/Edit and no
  context-losing native Skill fork.
- Require both in runner preflight and pin each with a positive check plus a
  real temporary-fixture mutation.
- Send the multiline Claude product prompt over stdin and pin that it never
  enters the Windows command argv.
- Run snapshot diagnostics through one UTF-8-pinned helper for both live and
  immutable reverify paths.
- Require at least one exact case-sensitive lowercase `unique-cardinality
  exhaustion` literal on a physical line before dispatch; allow additional
  correct occurrences.
- Exclude ambient Claude user settings/MCP before dispatch; keep the advocate
  workspace snapshot complete, including every trace-like path.
- Pin at least three PRD story lines to the oracle's exact `- As a ` prefix.
- Redact credential-like high-entropy capture tokens before persistence while
  covering URL-safe, base64 and dotted forms and preserving lowercase digests.
- For Claude, execute repository-local skill/reference files directly rather
  than claiming native plugin activation when manifest validation is absent.
- Hand off to one separate clean-HEAD evidence follow-up; generated evidence
  is written only by the existing bounded runner.

## Exclusions

- No sandbox, approval, model fallback, snapshot oracle, pin policy,
  methodology skill, runtime gate or reviewer semantic changes. Runner change
  is limited to rejecting a prompt without the directive before model dispatch.
- No manual edits to generated live evidence.

## Verification

- `python3 -I tests/verify_live_model_benchmark.py`
- `python3 -I tests/verify_live_model_benchmark.py --evidence .itd-memory/PRG-004-no-evidence.json`
- Clean-HEAD follow-up: runner + `--require-evidence --max-age-days 30`
