# Task Contract — LPD002-A8 (сессия долгов LPD-002)

## Scope
Authority-снапшот замораживает поведение гейта ревью на момент чеканки
(sys.path.insert(HERE)); процедура чеканки не была задокументирована, а
дрейф — не виден машинно. Доставлено:
- runbook-раздел «Authority snapshot: minting and byte-parity» в
  docs/VERIFICATION_LOOP.md (чеканка ТОЛЬКО из merged main; ключ/keyring
  переносятся без изменений; когда перечеканивать);
- scripts/itd_authority_check.py — fail-closed байт-паритет модулей/политик
  снапшота против skills/_shared (exit 0 тихий; exit 1 путь+WHY+FIX;
  exit 2 input-ошибки; ключи/keyring exempt; без аргументов — usage error);
- tests/verify_authority_check.py (12 проверок, обе стороны, включая MISSING repo-side и all-orphan exit 1) —
  зарегистрирован в tests/run-all.sh (drift-guard зелёный: локальный extra
  строже CI — допустим);
- живой replay: чек назвал 3 разошедшихся модуля старого снапшота; по новой
  процедуре перечеканен LPD002-A8-62eea6bb-a1 из origin/main (parity OK,
  keyring 1fa8afec… без изменений).

## Verification Standards
- `authority-check-oracle` = `python3 -I tests/verify_authority_check.py`.
- `runall-drift-oracle` = `python3 -I tests/verify_runall_drift.py`.

## Exclusions
- Автоматическое встраивание чека в гейт регистрации — кандидат следующего
  шага (гейт уже пинит keyring-sha; поведенческий паритет пока ручной шаг
  процедуры).
