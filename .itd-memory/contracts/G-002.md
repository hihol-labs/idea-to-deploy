# Task Contract: G-002 — CONTEXT-BUDGET-1 (pre-flight контекст <= 1 КБ на промпт)

## Scope
- `hooks/pre-flight-check.sh`: sentinel/state-файл на сессию+репозиторий; первый промпт -
  полный дамп как сегодня; далее только дельта (новые коммиты, появившееся
  предупреждение о параллельной сессии), <= 1024 байт, без дельты - пустой вывод.
- `tests/verify_preflight_budget.py` (новый, в `tests/run-all.sh`): реальный хук
  subprocess'ом в throwaway git-репо, изолированные HOME/TMPDIR/session id; проверка
  бюджета host-индекса памяти (если файл есть на хосте).
- Host `MEMORY.md` индекса памяти: <= 24 400 байт, строки <= 200 символов - через
  approval-diff (архив старых записей в topic-файл + короткие хуки).
- `.itd/IMPACT_GRAPH.json`, `CHANGELOG.md`, строка таблицы хуков в
  `docs/HARNESS_ENGINEERING_MAP.md`.

## Verification Standards
- `sh skills/_shared/itd_py.sh tests/verify_preflight_budget.py && test "$(wc -c < "$HOME/.claude/projects/-home-hihol-projects-idea-to-deploy/memory/MEMORY.md")" -le 24400`
  (verificationCommand юнита): RED на дофиксовых байтах хука, GREEN после; `--mutations`
  >= 3 летальных.
- `sh skills/_shared/itd_py.sh tests/verify_fresh_session_resume.py`,
  `tests/verify_host_neutral_memory.py`, `tests/verify_init_contracts.py`,
  `tests/meta_review.py --verbose` - зелёные (потребители хука).
- `bash tests/run-all.sh --quick` -> `DONE fails:none`; `/review` low-маршрут
  (machine receipt + adjudication, review-cache).

## Exclusions
- Ранний выход хуков по тиру (G-003), автоустановка хуков (G-004), внешний пилот (G-005).
- Состав и порядок секций ПЕРВОГО дампа не меняются.
- Проектный `.itd-memory/MEMORY.md` (54 КБ) - вне критерия G-002.
