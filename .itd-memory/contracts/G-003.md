# Task Contract: G-003 — HOOKS-TIER-EXIT-1 (advisory-хуки молчат на low-юните)

## Scope
- `hooks/TIER_EXEMPT.json` (новый): `exemptTier: "low"`, ровно четыре хука с причиной -
  context-aware, context-budget, stuck-detection, handoff-readiness.
- `hooks/tier_exempt.py` (новый stdlib-хелпер): проект - только из `cwd` payload'а; тир
  из `STATE.currentUnit.riskTier` АКТИВНОГО юнита, иначе из того же юнита активной цели
  `GOAL.currentUnitId`; без фоллбэка на defaultRiskTier; закрытый юнит, чужой/нет `cwd`,
  любая ошибка -> хук работает как раньше.
- Ранний выход в четырёх хуках до любого чтения/записи state и вывода; импорт хелпера
  защищён.
- `scripts/sync-to-active.sh`: ставит `hooks/TIER_EXEMPT.json` вместе с хуками.
- `tests/verify_hook_tier_exit.py` (новый, в `tests/run-all.sh`); `.itd/IMPACT_GRAPH.json`,
  `CHANGELOG.md`, строка в `docs/HARNESS_ENGINEERING_MAP.md`.

## Verification Standards
- `sh skills/_shared/itd_py.sh tests/verify_hook_tier_exit.py` (verificationCommand
  юнита): low -> exit 0, пустой stdout, без записи в TMPDIR/.itd-memory; medium/high/без
  тира -> stdout+exit байт в байт как у дофиксовых байтов хука (8d192dd) на той же
  фикстуре; hardGates и запретный список не в TIER_EXEMPT; RED на дофиксовых байтах,
  GREEN после; `--mutations` >= 3 летальных.
- `tests/meta_review.py --verbose`, `tests/verify_context_budget.py` (если есть) и
  прочие потребители четырёх хуков - зелёные; `bash tests/run-all.sh --quick` ->
  `DONE fails:none`.
- `/review` medium: машинная квитанция + targeted fresh-session checker + adjudication.

## Exclusions
- check-skills, pre-flight-check, любые гейты и enforcement-хуки; ранний выход в
  codex-dispatch.py.
- Автоустановка хуков (G-004), внешний пилот (G-005).
- Поведение четырёх хуков на medium/high/неизвестном тире не меняется.
