# Task Contract: G-001 — RISK-TIER-1 (пропорциональность по умолчанию)

## Scope
- `skills/_shared/PROPORTIONALITY_POLICY.json` (+`strictClasses`), новый
  `skills/_shared/itd_risk_classes.py`, `skills/task/scripts/itd_unit_log.py` (activate:
  forced high), `docs/templates/itd/COMPLETION_POLICY.json` (`defaultRiskTier: low`),
  `docs/adr/ADR-011-default-risk-tier-low.md`, `tests/verify_risk_tier_default.py` +
  `tests/run-all.sh`, `skills/task/SKILL.md` Step 3.5, `CHANGELOG.md` [Unreleased].
- Пункт (1) критерия уже выполнен в коде - только регрессионный пин.

## Verification Standards
- `sh skills/_shared/itd_py.sh tests/verify_risk_tier_default.py` (verificationCommand
  юнита): RED на дофиксовых байтах, GREEN после, >=3 летальных мутации внутри оракула.
- `sh skills/_shared/itd_py.sh tests/verify_unit_log.py`,
  `tests/verify_proportionality_benchmark.py`, `tests/verify_verification_profiles.py` -
  зелёные (потребители формы политики).
- `bash tests/run-all.sh --quick` -> `DONE fails:none`; `/review` medium-маршрут.

## Exclusions
- Ранний выход хуков по тиру (G-003), pre-flight sentinel (G-002), автоустановка (G-004).
- Встроенные дефолты `hooks/completion-gate.sh` и `itd_hygiene.py` остаются medium.
- Матчер strict-классов в `/goal --activate` - follow-up (тир там задаёт владелец).
