---
name: session_2026-09-22_4
description: "Аудит ITD (/advisor x3 перспективы) -> цель PROPORTIONALITY-DEFAULT G-001..G-005 записана в GOAL.json, старый леджер -> GOAL-2026-09-22.json; НЕ закоммичено; next: --activate G-001 RISK-TIER-1"
type: project
---

## Дата
2026-09-22

## Проект
/home/hihol/projects/idea-to-deploy, worktree `.claude/worktrees/itd-methodology-audit-9056ac`,
ветка `claude/itd-methodology-audit-9056ac` (от main `2487397`). Изменения в индексе/рабочем
дереве, НЕ закоммичено.

## Что сделано
- **Аудит методологии** через `/advisor` (три перспективы: замеры + business-analyst +
  devils-advocate). Ключевые факты: маршрут верификации - главная статья расхода (A/B
  RETRO-2026-07-08: x3.5 wall при одинаковой доле верифицированного завершения; медиана
  юнита 5.1 ч, 18 из 23 последних юнитов - дефекты собственного маршрута, 0 фич для
  целевых задач); хуки = 1.44% активного wall по трейсам (devils-advocate) - НЕ узкое
  место; `pre-flight-check.sh` 5.8 КБ на каждый промпт без sentinel; MEMORY.md 48.9 КБ
  против лимита 24.4 КБ (44/135 строк индекса обрезаны харнесом); `--risk-tier` в
  `skills/task/scripts/itd_unit_log.py:375` опционален, в леджерах 30 high/21 medium/5 low;
  `README.md:123` «hooks not auto-installed»; внешних outcome-записей нет (только README).
- **Отвергнуто по замеру:** слияние хуков в один процесс; потолок раундов ревью
  (`STOP_RULE_POLICY.json`); новые lite/strict-профили (дубль risk-таблицы).
- **Цель утверждена владельцем** («подтверждаю» x2) и записана: `.itd-memory/GOAL.json`
  (`status: active`, 5 юнитов pending, `currentUnitId: ""`, `nonGoals[]`), валидация
  `scripts/validate_state.py` -> `OK`. Прежний done-леджер -> `GOAL-2026-09-22.json`
  (`git mv`).
- `.itd/DECISIONS.md`: запись «2026-09-22: открыта цель "пропорциональность по умолчанию"».
- `BACKLOG.md`: P3 - архивный `GOAL-2026-09-20.json` PE5-001/002 verified без unit-событий
  (state-guard предупреждает на каждой мутации леджера).

## Ключевые решения
- **Пять юнитов, строгий порядок, WIP=1** (см. DECISIONS 2026-09-22): G-001 RISK-TIER-1
  (medium), G-002 CONTEXT-BUDGET-1 (low), G-003 HOOKS-TIER-EXIT-1 (medium), G-004
  HOOKS-AUTOINSTALL-1 (medium), G-005 PILOT-LOW-1 (medium). Почему такой порядок:
  обязательный тир нужен ДО пилота, чтобы пилот сразу шёл по правильному маршруту.
- **Envelope `runPolicy` не заводится, `--seal` не делается** - прогон не AFK; оракулы
  G-001..G-004 - новые `tests/verify_*.py`, пишутся внутри юнитов RED-first.
- **G-005 verificationCommand** опирается на `itd_external_pilot.py aggregate` и ключ
  `comparableUnits` (проверено по коду: `scripts/itd_external_pilot.py:194`); флага
  `--validate --min-units` у скрипта нет - формулировка из чата исправлена до записи.

## Изменённые файлы
- `.itd-memory/GOAL.json` (новый леджер, 5 юнитов)
- `.itd-memory/GOAL.json -> .itd-memory/GOAL-2026-09-22.json` (переименование, git mv)
- `.itd/DECISIONS.md` (append)
- `BACKLOG.md` (P3 вверху)
- `.itd-memory/session_2026-09-22_4.md` (этот файл) + зеркало в приватной памяти

## Блокеры
- Нет блокеров. Коммит кандидата не делался: по правилам репо leger-коммит идёт маршрутом
  (owner-adjudicated general claim + gate registry + `itd pr create`) - это работа
  следующей сессии вместе с активацией G-001.

## Следующие шаги
1. Свежая сессия в этом worktree: `sh skills/_shared/itd_py.sh skills/goal/scripts/itd_goal_verify.py --activate G-001`
   (харнес выставит `currentUnitId`, событие `activated`).
2. Переписать `.itd/SCOPE_LOCK.md` под G-001 (сейчас там REL-1.105.0 ledger closure).
3. `/task` -> RED-first `tests/verify_risk_tier_default.py` -> правки
   `skills/task/scripts/itd_unit_log.py` (`--risk-tier` required + strictClasses -> high),
   `docs/templates/itd/COMPLETION_POLICY.json` (`defaultRiskTier: low`) и
   `docs/templates/itd/itd_hygiene.py:172` (та же константа), `skills/_shared/PROPORTIONALITY_POLICY.json`
   (`strictClasses[]`), `docs/adr/ADR-011-*.md`; >=3 мутации; `bash tests/run-all.sh --quick`.
4. `/review` -> продюсер (дефолт gpt-5.6-sol) -> `itd_goal_verify.py G-001` -> коммит ->
   `itd pr create --timeout 3600` -> `gh pr ready` -> `/session-save`.

## Неочевидный контекст
- Владелец просил сначала объяснить «что именно реализовывать» - ответ: большая часть
  механики уже есть, юниты в основном заставляют её применяться (обязательность, дефолты,
  ранний выход), а не добавляют новую поверхность.
- Аудит проходил в worktree; параллельная сессия (pid 1511420/1595431/1668250) в это же
  время закрывала REL-1.105.0 на `main` - лок-файл в приватной памяти принадлежит ей.
- Контекст-сенсор показывал ~211% (грубая оценка) - сессия сознательно закрыта после
  записи леджера без активации G-001.
- `hooks/session-open-diagnostic.sh:48` `sentinel_path()` - образец для G-002.
- Выбор проекта для G-005 - за владельцем; блокер HDX-009 resolved, авторизованы
  product-factory-os, jogai, hihol-landing.

## Самоаудит скиллов
- Скиллы: `/advisor`, `/goal`, `/session-save`. Кода нет, миграций/auth нет; `/review` и
  `/test` не требовались (только леджер и журналы). Bypass-записей за сессию: см. ledger
  `/tmp/claude-skill-ledger-day20260922.jsonl` (общий дневной, не разделён по сессиям).

## Стоимость сессии
- Cost-леджер `cost-tracker.sh` синтетический (оценка по типам вызовов) - не приводится как
  метрика (вывод аудита). Субагенты advisor: ~102k + ~126k + ~192k токенов по отчётам.
