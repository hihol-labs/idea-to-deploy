{
  "version": 1,
  "goal": "Метрика маршрута различает независимую, машинную и owner-верификацию, атрибуция событий переживает архивирование леджера, и это выпущено релизом 1.105.0.",
  "status": "active",
  "createdAt": "2026-09-20T11:07:20Z",
  "updatedAt": "2026-09-21T10:20:35Z",
  "currentUnitId": "",
  "units": [
    {
      "id": "LEDGER-ARCHIVE-1",
      "riskTier": "low",
      "criterion": "itd_unit_lifecycle.attribute does not trust an explicit event ledger label that names a ledger which does not own the unit: when exactly one ledger in the memory directory owns the unit id the event is attributed to that owner, otherwise it is counted as unattributed; a label naming a ledger that owns the unit and the STATE pseudo-ledger keep their current behaviour; on the live memory directory the PE5-era lifecycles labelled GOAL.json are attributed to GOAL-2026-09-20.json with lifecyclesVerified, unattributedEvents and vcr unchanged; and the guarantee is a RED-first regression in tests/verify_ledger_reconciliation.py proved by mutation.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_ledger_reconciliation.py",
      "status": "verified",
      "verifiedAt": "2026-09-21T10:20:35Z",
      "evidence": "exit 0: 96 passed, 0 failed"
    },
    {
      "id": "ROUTE-REPAIR-3",
      "riskTier": "low",
      "criterion": "itd_unit_lifecycle.build assigns every verified lifecycle to exactly one named route class - verifiedIndependent (the verified event binds a checker/adjudication receipt), verifiedMachineOnly (a harness transition with a machine receipt and no checker receipt), verifiedOwnerRoute (the transition was recorded by the owner) - itd_metrics.py surfaces all three counters, their sum equals unitsVerified, vcr keeps its current value and definition, spelling variants of the route label (owner / owner-route) and the literal string none in checkerReceipt are normalised rather than trusted, and each class is a RED-first regression proved by mutation.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_route_metric.py && sh skills/_shared/itd_py.sh tests/verify_retro_scan.py",
      "status": "pending"
    },
    {
      "id": "ROUTE-DEBTS-ORACLE-1",
      "riskTier": "low",
      "status": "pending",
      "criterion": "tests/verify_route_debts.py exits 0 in its default mode: the validate_adjudication stubs the oracle injects accept the call shape skills/goal/scripts/itd_goal_verify.py actually makes (today a fifth positional argument raises TypeError at the first stub and aborts every later check), no production module is changed to make the oracle pass, the --installed-proof mode accepts .itd-memory/host-inputs/REL-1.104.0/INSTALLED.json, and the repair is RED-first: the suite is red on the parent commit and a mutation that restores a four-argument stub turns it red again.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/verify_route_debts.py && sh skills/_shared/itd_py.sh tests/verify_route_debts.py --installed-proof .itd-memory/host-inputs/REL-1.104.0/INSTALLED.json"
    },
    {
      "id": "REL-1.105.0",
      "riskTier": "high",
      "status": "pending",
      "criterion": "Release 1.105.0 ships the LEDGER-ARCHIVE-1 attribution fix and the ROUTE-REPAIR-3 route-class counters: the unit command exits 0, i.e. meta_review is clean, the quick mirror's last line is exactly 'DONE fails:none', the version 1.105.0 stands in every place the command enumerates, CHANGELOG.md has exactly one '## [1.105.0]' heading as the first version heading with '## [Unreleased]' above it, the tag v1.105.0 points at the first commit of the origin/main first-parent chain that carries the version, the GitHub release is published on that tag commit, the live benchmark evidence is re-recorded on the release tree (tests/verify_live_model_benchmark.py green), and the native rollout on WSL and Windows is proved by .itd-memory/host-inputs/REL-1.105.0/INSTALLED.json accepted by tests/verify_route_debts.py --installed-proof. This text is the single source of the criterion; contract, SCOPE_LOCK and HANDOFF reference it and do not restate it.",
      "verificationCommand": "sh skills/_shared/itd_py.sh tests/meta_review.py && bash tests/run-all.sh --quick | tail -1 | grep -qx 'DONE fails:none' && sh skills/_shared/itd_py.sh -c \"import json,pathlib,re,sys;v='1.105.0';e=re.escape(v);J=lambda p,k: json.load(open(p))[k];T=lambda p: pathlib.Path(p).read_text(encoding='utf-8');one=lambda pat,txt: len(re.findall(pat,txt,re.M))==1;mk=json.load(open('.claude-plugin/marketplace.json'));badge=lambda p: one(r'^\\[!\\[Version: '+e+r'\\]\\(https://img\\.shields\\.io/badge/Version-'+e+r'-purple\\.svg\\)\\]\\(CHANGELOG\\.md\\)$',T(p));places=[('plugin.json',lambda: J('.claude-plugin/plugin.json','version')==v),('codex-plugin',lambda: J('.codex-plugin/plugin.json','version')==v),('marketplace',lambda: [p.get('version') for p in mk.get('plugins',[]) if p.get('name')=='idea-to-deploy']==[v]),('docs-state',lambda: J('docs/HARNESS_DOCS_STATE.json','pluginVersion')==v),('README.md',lambda: badge('README.md')),('README.ru.md',lambda: badge('README.ru.md')),('conformance',lambda: (lambda d: len(d)==1 and d[0].rstrip().endswith('idea-to-deploy **v'+v+'**.'))([l for l in T('docs/HARNESS_CONFORMANCE_REPORT.md').splitlines() if 'idea-to-deploy **v' in l])),('rc-contract',lambda: J('docs/api-reviewer/RELEASE_CANDIDATE_CONTRACT.json','version')==v),('release-oracle',lambda: one(r'^VERSION = '+chr(34)+e+chr(34)+r'$',T('tests/verify_external_reviewer_release.py'))),('changelog',lambda: (lambda h: len(h)>1 and h[0]=='## [Unreleased]' and h[1]=='## ['+v+']' and len([x for x in h if x=='## ['+v+']'])==1)([l.split(' - ')[0].strip() for l in T('CHANGELOG.md').splitlines() if l.startswith('## [')]))];bad=[n for n,f in places if not f()];sys.exit('version drift in %d of %d places: %s' % (len(bad),len(places),', '.join(bad))) if bad else None;print('version consistent', v, '%d/%d places' % (len(places),len(places)), 'anchored')\" && sh skills/_shared/itd_py.sh -c \"import json,subprocess,sys;run=lambda a: subprocess.run(['git',*a],capture_output=True,text=True);v='1.105.0';r=run(['rev-parse','v'+v+'^{commit}']);sys.exit('tag v'+v+' does not exist yet') if r.returncode else None;tag=r.stdout.strip();g=lambda *a: (lambda x: x.stdout.strip() if x.returncode==0 else sys.exit('git '+' '.join(a)+' failed'))(run(list(a)));fp=g('rev-list','--first-parent','origin/main').split();sys.exit('tag v'+v+' is not on the first-parent chain of origin/main: '+tag) if tag not in fp else None;ver=lambda c: json.loads(g('show',c+':.claude-plugin/plugin.json'))['version'];sys.exit('tag commit does not carry '+v+': '+ver(tag)) if ver(tag)!=v else None;par=g('rev-list','--parents','-n','1',tag).split()[1:];sys.exit('first parent already carries '+v+': '+par[0]) if par and ver(par[0])==v else None;print('tag',tag,'first introduces',v,'on the first-parent chain; parents',len(par))\" && sh skills/_shared/itd_py.sh -c \"import json,subprocess,sys;v='1.105.0';r=subprocess.run(['gh','release','view','v'+v,'--json','tagName,isDraft,isPrerelease,publishedAt,targetCommitish'],capture_output=True,text=True);sys.exit('GitHub release v'+v+' is not published: '+(r.stderr.strip() or r.stdout.strip())) if r.returncode else None;d=json.loads(r.stdout);tag=subprocess.run(['git','rev-parse','v'+v+'^{commit}'],capture_output=True,text=True).stdout.strip();bad=[n for n,ok in (('tagName',d.get('tagName')=='v'+v),('isDraft',d.get('isDraft') is False),('isPrerelease',d.get('isPrerelease') is False),('publishedAt',bool(d.get('publishedAt'))),('targetCommitish',d.get('targetCommitish')==tag)) if not ok];sys.exit('GitHub release v'+v+' is not a published release on the tag commit: '+', '.join(bad)) if bad else None;print('GitHub release published',d['tagName'],d['publishedAt'],'target',d['targetCommitish'])\" && bash scripts/sync-to-active.sh --check && CLAUDE_HOME=/mnt/c/Users/Дмитрий/.claude bash scripts/sync-to-active.sh --check && sh skills/_shared/itd_py.sh -c \"import pathlib,sys;v='1.105.0';ok=lambda d: d.is_dir() and (d/'scripts'/'itd.py').is_file() and (d/'skills').is_dir();w=[p for p in pathlib.Path.home().joinpath('.local/share/itd/runtime').glob(v+'-*') if ok(p)];n=[p for p in pathlib.Path('/mnt/c/Users/Дмитрий/AppData/Local/ITD/runtime').glob(v+'-*') if ok(p)];sys.exit('rollout missing or invalid: wsl=%s windows=%s' % ([p.name for p in w],[p.name for p in n])) if not (w and n) else None;print('rollout',v,w[-1].name,n[-1].name)\" && sh skills/_shared/itd_py.sh tests/verify_route_debts.py --installed-proof .itd-memory/host-inputs/REL-1.105.0/INSTALLED.json && sh skills/_shared/itd_py.sh tests/verify_live_model_benchmark.py"
    }
  ]
}
