# External outcome evidence

`INDEX.json` is intentionally absent until PE5-008 receives consented real
pilot records satisfying `docs/external-validation/PROTOCOL.md`.

Do not place test fixtures, methodology-repository runs, author-only
self-reports or synthetic examples here. A missing index keeps the external
adoption axis red by design.
