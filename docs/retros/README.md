# Retro reports

Dated reports produced by `/retro` (v1.46.0) — the methodology's
self-improvement loop: scan output from `skills/retro/scripts/itd_retro_scan.py`
(FACTS) + ranked, evidence-cited improvement proposals (PROPOSALS). What enters
the next release backlog is decided by the maintainer (MERGE stays human —
`/retro` never edits the methodology itself).

File naming: `RETRO-YYYY-MM-DD.md`.
