# RETRO 2026-07-10 — «принцип чемодана»: замеры после v1.73.0 и кандидат v1.74.0

## FACTS — скан телеметрии (as-is)

Команда: `python3 skills/retro/scripts/itd_retro_scan.py .`

```
## Retro scan — факты из телеметрии (без интерпретации)

Проектов с `.itd-memory`: **1** (workspace: .)
**VCR глобально:** 1.0 (8/8 юнитов), регрессий: 0, проваленных верификаций: 1

**SKILL_BYPASS:** всего 0
**Cost-леджеры:** 6 сессий, 5779500 токенов, ~$173.39

**Реестр инструкций (lifecycle):** секций 7, записей 7, проблем 0
```

Абстенции реестра фич (`itd_ledger_abstentions.py`): F-14/F-15/F-17 — внешних
safe-use сигналов не появилось, остаются `abstain`.

## Внешние сигналы (замеры-упражнения 2026-07-10, вне скана)

1. **SNR-аудит** задеплоенного entry-блока (150 строк, 20 инструкций × 5 типов
   задач из живой истории): багфикс/фича/отчёт 0.85, релиз методологии 0.80,
   консультация 0.45. Секция «Harness-native features — best-effort invariant»
   (~19 строк прозы) релевантна **1 из 5** типов задач — главный оставшийся
   шум-для-большинства в entry-блоке.
2. **A/B-замер сжатия** (Completion Gate 60→18 строк, 5 вопросов × 5 прогонов
   × 2 варианта, sonnet): поведенческие ответы **20/20 паритет** — компрессия
   «правила на краю + детали в topic-док» поведения не теряет.
3. **Lost-in-the-middle замер** (316-строчный файл, правило на 1%/53%/100%,
   haiku): позиционный эффект на файле такого размера не воспроизвёлся
   (5/5, 5/5, 4/5), но 6 из 21 прогона не смогли прочитать topic-файл и
   **молча продолжили без критического правила** → поведенчески-критичные
   строки обязаны оставаться в entry, в доки уходит только rationale.

## PROPOSALS

| # | Сигнал | Предложение | Effort | Риск |
|---|---|---|---|---|
| 1 | SNR 1/5 (сигнал 1) + паритет A/B (сигнал 2) — два внешних сигнала | Сжать секцию «Harness-native features» в `docs/templates/global-claude-md.md` до поведенческих правил (transport-not-contract + egress/mutation guard остаются в entry — по сигналу 3), rationale → topic-док `docs/harness-best-effort.md`; обновить запись реестра (notes + свежий `review_by`) | S | Потеря поведения при сжатии — прибивается тем же A/B-квизом (сигнал 2 показал методику рабочей); реестр — `tests/verify_retro_scan.py` |
| 2 | Сигнал 3 (6/21 тихих продолжений без файла) | Заметка в бэклог: агентный контракт «не смог прочитать заявленный источник инструкций → скажи об этом явно, не продолжай молча» (кандидат в helpers/agents; нужен второй сигнал) | S | — (пока только заметка) |

Анти-Гудхарт: оба сигнала внешние (замеры на агентах и SNR по живым типам
задач), не собственные метрики методологии.

## Решение

Пользователь принял кандидат №1 и мандатировал релиз **v1.74.0** (этот же
день). Кандидат №2 — в бэклог до второго сигнала.
