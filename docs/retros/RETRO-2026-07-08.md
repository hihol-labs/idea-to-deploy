# RETRO 2026-07-08 — по итогам 3 упражнений (сессия OneOfS_tmp)

Контекст: упражнения «атомизация», «сравнительный эксперимент A/B (WIP=1)», «аудит
доказательств завершения» выполнены на реальных прогонах (scratchpad, два независимых прогона-исполнителя,
независимый перезамер). Дополняют упражнения 2026-07-06 (посеянные дефекты,
калибровка, конфиги гейта).

## Step 1: FACTS — скан телеметрии (вывод скрипта as-is)

Команда: `py -3 -X utf8 ~/.claude/skills/retro/scripts/itd_retro_scan.py C:/Users/Дмитрий/AI/OneOfS_tmp`

```
## Retro scan — факты из телеметрии (без интерпретации)

Проектов с `.itd-memory`: **1** (workspace: C:\Users\Дмитрий\AI\OneOfS_tmp)
**VCR глобально:** 0.9 (9/10 юнитов), регрессий: 0, проваленных верификаций: 0
**Проекты с VCR<1.0:** OneOfS_tmp

**SKILL_BYPASS:** всего 864 за 2 сессий (**432.0/сессия** — метрика трения, должна падать от релиза к релизу), по инструментам: Bash×864
Последние причины bypass:
- Bash: final verification of in-progress fixes)
- Bash: final canonical sync of in-progress fix)
- Bash: canonical sync of in-progress fix)
- Bash: verification of in-progress /task hardening)
- Bash: verifying just-deployed skill step)
- Bash: executing /session-save skill steps)
- Bash: recon for approved drift-hook work)
- Bash: recon within approved task)
- Bash: executing /session-save steps)
- Bash: xlsx-скилл уже активен; дамп структуры трекера в UTF-8 файл

**Cost-леджеры:** 561 сессий, 3804200 токенов, ~$114.13

| Проект | Юниты (verified/activated) | VCR | Регрессии | Незакрытые гейты |
|---|---|---|---|---|
| OneOfS_tmp | 9/10 | 0.9 | 0 | — |
```

Step 1b (абстенции): 6 abstain-строк (F-14, F-15, F-17, F-18, F-19, F-20), у всех
fallback на месте. Внешних safe-use сигналов в этой сессии нет → все остаются abstain.

Дополнительный факт, найденный при реализации P1: unwrap-регекс WSL-обёртки не
матчил форму `wsl.exe -- bash -lc '…'` (голый `--` сепаратор) — доминирующую в живых
сессиях, т.е. read-only exemption для неё молча не срабатывал.

## Внешние сигналы сверх скана (измерено в упражнениях, не по памяти)

- **A/B эксперимент** (одна спека, два независимых прогона): доля проверенного завершения
  100% у ОБОИХ (сильная модель тестирует сама); разница в хвостах — A (свободный):
  branch coverage 87.0%, line 95.65%, 7 строк мёртвого/непроверенного кода,
  scope-дрейф (+14% src, незапрошенный API); B (WIP=1): 100% line / 94% branch,
  0 мёртвых строк. Цена B: ×3.5 время (407с vs 117с), ×7 tool-вызовов (28 vs 4),
  +8% токенов; главный множитель — накопительная регрессия после каждого юнита.
- **Аудит доказательств** прогона A: при честном отчёте «6/6 готово» найдены
  1 каркас (`getUser` — публичный метод без вызывающих и без требования) и
  1 класс незакрытых поведений (error-ветки `loadFromFile` без тестов).

## Step 2: PROPOSALS (ранжировано; signal-gated)

- **P1 (релиз-кандидат; повторяющийся количественный сигнал — 864/2 сессии):**
  skill-гейт: sliding grace-window внутри активного скилла; hard-gated
  мутационные классы (git push / migration appliers / deploy scripts) — без
  fast-path всегда; честный леджер (ceremonial vs real bypass); unwrap `--` формы.
- **P2 (1 измеренный сигнал):** пункт «негативные сценарии прогнаны» в рубрику
  /review (I-code-12) + advisory-хвост L2 в completion-gate (не вето).
- **P3 (1 измеренный сигнал):** «scope-аудит диффа» в рубрику /review (I-code-13).
- **P4 (1 измеренный сигнал, n=1):** «микро-путь» каденции регрессии в /task +
  helpers §6 (≤1–2 юнитов → накопительная регрессия одним финальным прогоном).

Анти-Гудхарт: ни одно предложение не обосновано улучшением собственных метрик
методологии; сигналы — трение bypass-леджера и измеренные прогоны.

## Step 3: решение человека

Пользователь принял все четыре кандидата и заказал их одним релизом (v1.66.0,
строго по порядку P1→P4). Ядро методологии (WIP=1, лестница L1→L3,
completion-gate) по итогам эксперимента признано менять НЕ нужным: подтверждена
роль «гарантии нижней границы» (упр. 3 от 2026-07-06: без гейта 0/4 пойманных
преждевременных DONE, с гейтом 4/4).
