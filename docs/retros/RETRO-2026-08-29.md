# RETRO 2026-08-29 — долг по сессии 2026-08-23, снят в N9 (релиз v1.101.0)

Роли по контракту `/retro`: **FACTS** — харнес (`itd_retro_scan.py`),
**PROPOSALS** — модель (этот отчёт), **MERGE** — человек. Ни один файл
методологии этим прогоном не изменён; всё ниже — кандидаты в бэклог.

## Step 1 — FACTS (вывод скана как есть)

Команда: `python3 -I skills/retro/scripts/itd_retro_scan.py --json`

```
projectsScanned = 1
unitsActivated = 167
unitsVerified = 151
unitsBlocked = 12
unitsOpen = 0
unitsWip = 0
unitsVerifiedNoActivationUnexplained = 0
unattributedEvents = 0
vcrGlobal = 1.0
regressions = 0
failedVerifications = 8
bypassTotal = 8            (bypassByTool: {"Bash": 8})
bypassCeremonialTotal = 4
bypassSessionCount = 1     bypassPerSession = 8.0
costSessions = 8           costTokensTotal = 6 002 600   costUsdEstimate = 180.08
instructionRegistry: sectionsTotal 7, entriesTotal 7,
                     unregistered [], staleEntries [], brokenEnforcedBy [], reviewDue []
reviewFindings: reviewsLogged 104, findingsTotal 130, classesTotal 113,
                repeatClasses[0] = {"class": "~unclassified", "count": 7}
```

Step 1b (`itd_ledger_abstentions.py`): пять абстенций (F-14 artifact publishing,
F-15 background agents с mutation, F-17 transcript mining, F-19 on-disk
checkpoints, F-20 graduated trust). **Внешнего safe-use сигнала ни по одной
нет — все остаются `abstain`.**

Внешние сигналы этой сессии — боевой маршрут публикации релиза v1.101.0
(PR #245, merge `24698e2`), где каждый отказ воспроизводился машинно.

## Step 2 — PROPOSALS

Ранжировано: боевые инциденты -> повторяющиеся классы -> одиночные.

| # | Сигнал (evidence) | Предложение | Effort | Риск / чем прибиваем |
|---|---|---|---|---|
| P1 | **Боевой.** `itd pr create` -> `BLOCKED: current exact local independent review is not valid`; `itd gate doctor` из инсталла: `itdVersion 1.100.1`, `drift: ["local review: UNVERIFIED: local adjudication is stale, foreign, or invalid"]`, из репо на том же дереве — `LOCAL_REVIEWED`. Корень: pre-push судит квитанцию валидатором УСТАНОВЛЕННОГО runtime, у которого `methodologyVersion` читается из инсталлового `plugin.json`; на релизе версии по построению разные. LPD-002 R2 закрыл ровно этот класс для гейта коммита (repo-first через `.itd-install-source.json`), pre-push остался снаружи. | Распространить repo-first LPD-002 R2 на `itd_pre_push.require_profile_review`: для checkout'а, объявленного в `.itd-install-source.json`, грузить валидатор из репо. | M | Ослабление привязки к установленному runtime; прибивается `verify_push_gate_adjudicated` + `verify_itd_runtime_install` мутацией «чужой checkout не получает repo-first». |
| P2 | **Боевой.** `itd_install_cli.py --apply` -> `{"reason": "installed runtime directory inventory drifted", "status": "BLOCKED"}`; замер: 39 файлов против 38 в манифесте, лишний — `skills/review/scripts/__pycache__/itd_review_cache.cpython-312.pyc`, порождённый исполнением валидатора ИЗ инсталла. Reinstall «не чинится поверх» по построению, то есть релиз встаёт до ручного удаления. | Писать байткод мимо runtime (`PYTHONPYCACHEPREFIX` / `sys.dont_write_bytecode` на всех входах, читающих модули инсталла) ИЛИ объявить `__pycache__` известным нетронутым классом в сравнении inventory. | S | Ослабление inventory-гарантии; прибивается мутацией в `verify_itd_runtime_install` («подмена .py всё ещё блокирует, лишний .pyc — нет»). |
| P3 | **Боевой.** `CLAUDE_HOME=/mnt/c/Users/Дмитрий/.claude bash scripts/sync-to-active.sh` -> шаг 5/6 `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xc4 in position 868`; байты: `"C:/Users/\xc4\xec\xe8\xf2\xf0\xe8\xe9/AppData/..."` — Windows-`settings.json` в cp1251 из-за кириллического имени пользователя. Регистрация хуков не выполняется; дыры сегодня не было ТОЛЬКО потому, что набор хуков в релизе не менялся (33 unchanged). | Читать `settings.json` с фолбэком кодировки (utf-8 -> cp1251/`locale`) и писать обратно в исходной кодировке; в runbook — строка в «Грабли». | S | Порча файла при обратной записи; прибивается новым тестом на cp1251-фикстуре + `windows-verify`. |
| P4 | **Повторяющийся класс.** Три из четырёх заходов продюсера — отказы ПРЕДУСЛОВИЯ, не вердикты: `required impact class is unknown`, `REL1101-1-version-pins is not passed`, `... review evidence is not a closed object`. Исторически тот же класс: LPD-003-1 («критерии приёмки в статусе pending»), S10 («active unit and machine evidence differ»). Каждый отказ стоит полного цикла перечеканки (машинная нога -> adjudicate -> cache -> amend). | `itd contract lint` — pre-flight валидатор контракта перед запуском продюсера: закрытый словарь impact-классов, `minimumIndependentReviewers` по тиру, все активные критерии `passed`, наличие `reviewEvidence{claim,impactClasses,oracleIds}` с exact-PASS привязкой. Печатает ВСЕ нарушения за один прогон. | M | Дублирование логики гейта; прибивается тем, что линт импортирует `itd_review_evidence`, а не переписывает его (мутация: расхождение линта и гейта краснеет). |
| P5 | **Боевой.** Четвёртый заход продюсера: `{"reason": "low-risk candidate is machine-only", "status": "NOT_REQUIRED"}` — на low-tier продюсер не нужен ПО ПОСТРОЕНИЮ, но узнаётся это только после трёх циклов починки контракта. | Проверять риск-тир ПЕРВЫМ и возвращать `NOT_REQUIRED` до валидации контракта; строку про low-tier добавить в `docs/VERIFICATION_LOOP.md` и в маршрут публикации. | S | Нулевой (порядок проверок), прибивается `verify_free_reviewer_producer`. |
| P6 | **Боевой, процессный.** Первый `itd pr create` -> `BLOCKED: direct push to protected branch is forbidden: refs/heads/release/v1.101.0` (`itd_pre_push.py:87` защищает `refs/heads/release/`). RELEASE_RUNBOOK предписывает «ветка `feat/…` или `fix/…`», но не называет `release/*` запрещённым пространством имён. | Одна строка в RELEASE_RUNBOOK п.2: имя релизной ветки — `chore/release-vX.Y.Z`, пространство `release/*` защищено. | S | Нулевой. |
| P7 | **Пробел документа.** Сессия 2026-08-22 сверяла раскатку на ЧЕТЫРЁХ целях (Claude WSL/Windows, Codex WSL/Windows), а RELEASE_RUNBOOK п.6 называет только два `CLAUDE_HOME` + installers. Сегодня обе Codex-копии стояли на `1.100.1` и обновлены вручную копированием. | Дописать в п.6 обе Codex-цели (`~/plugins/idea-to-deploy`, `C:\Users\<user>\plugins\idea-to-deploy`) и способ их обновления. | S | Нулевой. |
| P8 | **Одиночный, гигиена.** Сверка целей: Windows-Claude содержит `hooks/require-independent-review-before-pr.sh`, которого в репо нет (реликт прошлых релизов). В `settings.json` не зарегистрирован -> не исполняется, поэтому дыры нет; но синк не удаляет исчезнувшие файлы, и такой реликт может стать активным при совпадении имён. | Режим `--prune` для `sync-to-active.sh` по манифесту синкнутых имён (по умолчанию — отчёт, удаление по явному флагу). | S | Удаление чужих файлов из `~/.claude`; прибивается allow-list'ом «только ранее синкнутые пути» + тестом на постороннем файле. |

Отклонено по анти-Гудхарту: ничего не предлагается по `vcrGlobal = 1.0`,
`bypassPerSession = 8.0` и `repeatClasses[0] = "~unclassified" (7)` — это
собственные метрики методологии; классификация находок уже разбиралась в
v1.84.0 и без внешнего сигнала повторно не открывается.

## Step 3 — решение человека

Кандидаты P1-P8 в бэклог не заведены: **что попадает в следующий релиз,
решает владелец.** Порядок по силе сигнала: P1 (валит публикацию каждого
релиза) -> P2 (валит установку) -> P3 (валит раскатку на Windows-профиле с
не-ASCII путём) -> P4/P5 (трение маршрута) -> P6/P7 (доки) -> P8 (гигиена).
