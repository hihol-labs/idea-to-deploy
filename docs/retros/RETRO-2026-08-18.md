# RETRO 2026-08-18 — трение маршрута публикации (S10-LEDGER, S11, релиз v1.98.0)

Роли: FACTS — `itd_retro_scan.py` (as-is ниже); PROPOSALS — модель; MERGE — человек.
Ничего в методологии этим ретро не изменено. Скилл-декомпозиция кандидатов — через
штатный конвейер (/task → ветка → маршрут → PR).

## Step 1 — FACTS (вывод скана как есть)

Команда: `sh skills/_shared/itd_py.sh skills/retro/scripts/itd_retro_scan.py .`

## Retro scan — факты из телеметрии (без интерпретации)

Проектов с `.itd-memory`: **1** (workspace: .)
**VCR глобально:** 1.0 (126 verified из 140 жизненных циклов; вне знаменателя: 14 закрытых явным решением (blocked 12), wip 0), регрессий: 0, проваленных верификаций: 8
**Аномалия учёта:** 1 юнит(ов) verified без записанного activation-события — писатель активаций теряет события (сигнал для /retro, не для VCR>1)

**Активные цели (/goal):**
- : Довести практическую эффективность Idea to Deploy до машинно подтверждённых 5,0… — 36/38 verified, давление 2; blocked: PE5-008 (External pilot paused by human decision on 2026-07-16. Gmail invitations to Max…); PE5-009 (PE5-008 external outcome evidence is blocked; therefore the frozen all-axis eva…)

**SKILL_BYPASS:** всего 27 за 1 сессий (**27.0/сессия** — метрика трения, должна падать от релиза к релизу), по инструментам: Bash×27; из них ceremonial (гейт уже был открыт — привычная аннотация, не реальный обход): 20
<details><summary>Последние причины bypass</summary>

- Bash: публикация релиза 1.98.0 через itd pr create (repo hihol-labs/idea-to-dep…
- Bash: retry itd pr create for release 1.98.0 (TLS flakes; repo hihol-labs/idea-…
- Bash: retry loop itd pr create release 1.98.0 (repo hihol-labs/idea-to-deploy) …
- Bash: edit + merge release PR https://github.com/hihol-labs/idea-to-deploy/pull…
- Bash: релизный runbook (мета-работа репо, скилл выбран не нужен) — restore STAT…
- Bash: релизный runbook — gh release create v1.98.0 в hihol-labs/idea-to-deploy …
- Bash: релизный runbook — gh release create v1.98.0 (full sha target) в hihol-la…
- Bash: релизный runbook — gh release create v1.98.0 (real merge sha) в hihol-lab…
- Bash: релизный runbook шаг 6 — sync-to-active на WSL-инсталл по смерженному mai…
- Bash: релизный runbook шаг 6 — sync-to-active на Windows-инсталл + verify-sync

</details>
**Cost-леджеры:** 8 сессий, 6598900 токенов, ~$197.97
**Токены субагентов (реальные, топ):** general-purpose(sonnet): 0, general-purpose(inherit): 0

**Реестр инструкций (lifecycle):** секций 7, записей 7, проблем 0

**Находки /review (леджер):** ревью 32, находок 52, классов 41
Повторяющиеся классы — кандидаты в автопроверки (пункт 4: review-находка → hook/тест; `~` = фингерпринт без явной category):
- `~unclassified` ×4 (high×1, low×2, medium×1) — напр. HANDOFF.md; .itd-memory/STATE.json
- `~graph-orchestration-runtime-прямо` ×2 (critical×2) — напр. /home/hihol/projects/idea-to-deploy/docs/OPERATING_LOOPS.md; /home/hihol/projects/idea-to-deploy/docs/OPERATING_LOOPS.md
- `~исполняемый-граф-либо-тащит` ×2 (critical×2) — напр. /home/hihol/projects/idea-to-deploy/docs/harness-best-effor…; /home/hihol/projects/idea-to-deploy/docs/harness-best-effor…
- `~гейт-читает-единственный-state` ×2 (critical×2) — напр. /home/hihol/projects/idea-to-deploy/hooks/wip-gate.sh; /home/hihol/projects/idea-to-deploy/hooks/wip-gate.sh
- `~контракт-построен-единственных-unitid` ×2 (critical×2) — напр. /home/hihol/projects/idea-to-deploy/.itd/VERIFICATION_CONTR…; /home/hihol/projects/idea-to-deploy/.itd/VERIFICATION_CONTR…
- `~enforcement-привязан-pretooluse-posttooluse` ×2 (important×2) — напр. /home/hihol/projects/idea-to-deploy/hooks/hooks.json; /home/hihol/projects/idea-to-deploy/hooks/hooks.json
- `~источники-предложения-сами-противоречат` ×2 (important×2) — напр. /home/hihol/projects/idea-to-deploy/docs/OPERATING_LOOPS.md; /home/hihol/projects/idea-to-deploy/docs/OPERATING_LOOPS.md
- `~solo-мейнтейнер-граф-слой` ×2 (important×2) — напр. /home/hihol/projects/idea-to-deploy/hooks/hooks.json; /home/hihol/projects/idea-to-deploy/hooks/hooks.json
- `~реальный-пробел-вскрытый-graph` ×2 (minor×2) — напр. /home/hihol/projects/idea-to-deploy/docs/DESIGN_SPACE.md; /home/hihol/projects/idea-to-deploy/docs/DESIGN_SPACE.md

| Проект | Юниты (verified/activated) | VCR | Регрессии | Незакрытые гейты |
|---|---|---|---|---|
|  | 126/140 | 1.0 | 0 | branchFinish, deploymentReadiness, nextStepApproval |


### Внешние сигналы (измерены в сессиях 2026-08-17/18, источник — HANDOFF-S10-LEDGER §17–19, BACKLOG P1)

| # | Сигнал (боевой факт) | Где измерено |
|---|---|---|
| E1 | Ledger-close коммит (STATE `verified` + контракт) отвергнут evidence-first продюсером ДВАЖДЫ зеркально: открытый followup → «STATE и контракт расходятся»; закрытый → «coverage null» (`coverage_matrix` = None по коду). Владелец вынужден выбирать «не пушить» — STATE в main отстаёт до следующего delivery-коммита. | §17.10–17.12, `itd_review_evidence.py:48,105-111,145-155` |
| E2 | Продюсер классифицирует codex `Selected model is at capacity` как `UNVERIFIED failed without proven transport unavailability` (не UNAVAILABLE): 2 цикла ретраев ×(3+5) попыток, ~35 мин; корень виден только через обёртку `run_bounded_process` — stdout/stderr транспорта при провале не сохраняются. | §18.1, `itd_free_reviewer_producer.py:2519-2578` на `bfce460` |
| E3 | Коммит-гейт (`hooks/check-review-before-commit.sh`) валидирует receipt/кэш ИНСТАЛЛИРОВАННОЙ версией `itd_review_cache.py`/`itd_verification_loop.py` (1.97.0), а бамп 1.98.0 в рабочем дереве даёт `methodologyVersion` mismatch → два полных маршрута перечеканены инсталлированным loop (`REL198i-*`). | §19 |
| E4 | `tests/verify_independent_review_efficacy.py` требует host-input `.itd-memory/host-inputs/…sha256` (gitignored) → в изолированном machine-worktree exit 1 даже с абсолютным путём; efficacy-oracle не вошёл ни в одну машинную квитанцию S11. | §18 |
| E5 | GitHub API HTTPS TLS handshake timeout: `itd pr create` — 5+15 попыток до PR (ветка запушена с первой), `gh pr merge` ×2 (мерж состоялся при «ошибке»), `gh pr edit` ×6, `gh release create` ×5. | §18.2, §19.1 |
| E6 | Efficacy-нога wsl run1: 1/4 clean-кейсов ложно BLOCKED (`clean-bounded-reconciled-export`), run2 — 0/4; вся нога перечеканивалась заново (~15 мин), точечный перепрогон отвергнут как cherry-picking. | §18 |
| E7 | `run-independent-review-efficacy.py --max-transport-attempts` принимает только 1 (`retry bound is invalid`); чекпоинт удаляется при успехе. `itd_unit_log.py activate` без `--risk-tier` — STATE правился руками (S10, S11). | §18, §17.12 |
| E8 | Свежий sonnet-чекер написал в отчёт `"PASS"` вместо канонического `"PASSED"` (при верном финальном сообщении) → checker-binding UNVERIFIED, resume агента. | REL198-RR |
| E9 | `gh release create --target <short sha>` → `target_commitish is invalid`; прямой `git push` тега блокирован pre-push гейтом (правильно), обход — тег на стороне GitHub. Один додуманный «полный sha» — ошибка исполнителя, поймана API. | §19.1 |
| E10 | Скан: `SKILL_BYPASS` 27/сессию — все Bash hard-gated класса (push/PR/release/sync + ретраи одной и той же команды); ceremonial-шум, а не пропуск гейтов (v1.84.0 уже признал класс). | скан |
| E11 | Скан: «1 юнит verified без activation-события» — сигнал писателя (проверить по `itd_unit_lifecycle` какой). | скан |

## Step 1b — абстенции реестра фич
F-14, F-15, F-17, F-19, F-20 — внешних safe-use сигналов в этой сессии не появилось → остаются `abstain`.

## Step 2 — PROPOSALS (каждое с evidence; ранжировано: боевые → повторяющиеся → одиночные)

| # | Сигнал | Предложение | Effort | Риск / чем прибьём |
|---|---|---|---|---|
| P1 | E1 (боевой, 2 BLOCKED, решение владельца) | Определить в контракте/продюсере класс **ledger-close кандидата**: если диф ограничен `.itd-memory/STATE.json` + `activeFollowup.status/closedAt` и все критерии followup `passed`, `coverage_matrix` отдаёт покрытие ЗАКРЫТОГО юнита (не None) и unit-промпт получает явную фразу «closing commit — coverage inherited from the delivered unit». Альтернатива дешевле: `itd_unit_log.py verified` пишет followup-close атомарно и `itd_review_evidence` принимает `verified` followup как self-consistent. | M | Ревьюер может начать пропускать содержательные правки под видом close — ограничить класс точным составом дифа; RED-first в `verify_review_evidence`/producer-oracle + мутация «подмешать код в close-диф → не класс». |
| P2 | E2 (боевой, ~35 мин) | (a) `CLI_UNAVAILABLE_MARKERS` += `at capacity`; (b) при провале транспорта сохранять хвост stdout/stderr (≤4 КБ, скраббер) в `--report-output`-каталог как `transport-failure.txt`. | S | Ложная «доступность» маскирует настоящие сбои → маркер точный (`selected model is at capacity`), пин мутацией в producer-oracle. |
| P3 | E3 (2 лишних маршрута на релиз) | `check-review-before-commit.sh` в репо методологии грузит `skills/review/scripts/itd_review_cache.py` из **репо** (или `methodologyVersion` берётся из HEAD, а не рабочего дерева); задокументировать в RELEASE_RUNBOOK «Грабли». | S | Кандидат сам меняет валидатор — допустимо только для repo==methodology; verify_review_cache + мутация «версия из рабочего дерева». |
| P4 | E5 (4 инцидента за сессию) | Встроенный bounded retry с бэкоффом для GraphQL/REST в `itd pr create` / `itd gate` (3–5 попыток, 15–30 с) и подсказка «состояние могло примениться — перепроверь `gh pr view`». | S | Дубли действий при повторе (merge идемпотентен, create — проверять существующий PR перед созданием). |
| P5 | E4 | `verify_independent_review_efficacy.py --expected-keyring-sha256 <hex>` (значение, не файл) + tracked-пин в `.itd/` → оракул запускаем в изоляции, efficacy входит в машинную квитанцию. | S | Утечка ожидаемого sha в дерево — не секрет (публичный keyring). |
| P6 | E7 | `run-independent-review-efficacy.py`: убрать флаг или принимать 1..3; чекпоинт сохранять с суффиксом `.done` вместо удаления. `itd_unit_log.py activate --risk-tier` (обязателен для медиум/high). | S | Нулевой; тесты efficacy-runner + verify_goal_tools. |
| P7 | E8 (1 инцидент) | В шаблоне checker-промпта — буквальная строка первой строки отчёта и мини-валидатор `itd_verification_loop.py checker --dry-run` для агента перед возвратом. | S | Нулевой. Один сигнал → заметка в бэклог, не релиз-кандидат. |
| P8 | E9 | RELEASE_RUNBOOK «Грабли»: тег через `gh release create --target $(git rev-parse <merge>)`, никогда короткий/додуманный sha; pre-push гейт для тегов — ожидаемое поведение. | S (docs) | Нулевой. |
| P9 | E6 (1 наблюдение) | Записывать false-block ревьюера на clean-кейсах в ноге как метрику (`run1/run2`), не чинить: порог 0.1 адекватен, недетерминизм модели — внешний. Заметка в бэклог. | — | — |
| P10 | E10 | Наблюдение, не предложение (анти-Гудхарт: собственная метрика). Возможный класс: hard-gated аннотации считать отдельно от «пропусков» в скане — только если владелец считает метрику 27/сессию вводящей в заблуждение. | S | — |
| P11 | E11 | Проверить `itd_unit_lifecycle` на текущем логе: какой цикл `verified` без активации; если это исторический реконсилированный ряд — объяснить в LEDGER-RECONCILIATION, иначе — писатель. | S | — |

Отклонено (Гудхарт): любые предложения «смягчить продюсера, чтобы ledger-close проходил» без определения класса кандидата.

## Step 3 — решение за владельцем
Рекомендация в бэклог следующего релиза: **P1 + P2 + P3 + P4** (боевые, повторяющиеся); P5–P8 — S-довесок; P9–P11 — заметки.
