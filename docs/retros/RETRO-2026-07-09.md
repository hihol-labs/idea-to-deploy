# RETRO — 2026-07-09 (эксперимент «инженер с амнезией»)

Триггер: живой эксперимент из 3 упражнений (14 сессий-агентов, ~2,6M токенов,
fixture «Todo CLI» 5 юнитов; лог — memory OneOfS `session_2026-07-09_2.md` +
транскрипт сессии). Оба релиз-кандидата подкреплены боевыми инцидентами
эксперимента, а не собственными метриками (анти-Гудхарт соблюдён).

## FACTS — скан харнеса (as-is)

Команда: `py -3 ~/.claude/skills/retro/scripts/itd_retro_scan.py C:\Users\Дмитрий\AI\OneOfS_tmp`

> Проектов с `.itd-memory`: **1** (workspace: C:\Users\Дмитрий\AI\OneOfS_tmp)
> **VCR глобально:** 1.25 (15/12 юнитов), регрессий: 0, проваленных верификаций: 0
> **SKILL_BYPASS:** всего 877 за 2 сессий (**438.5/сессия**), по инструментам: Bash×877; из них ceremonial: 11
> **Cost-леджеры:** 561 сессий, 5 903 800 токенов, ~$177.11

Команда: `python3 skills/retro/scripts/itd_retro_scan.py .` (репо методологии)

> Проектов с `.itd-memory`: **1**; VCR 1.0 (8/8), регрессий 0, проваленных верификаций 1
> SKILL_BYPASS: 0; Cost-леджеры: 0

Абстенции реестра (`itd_ledger_abstentions.py`): F-14, F-15, F-17, **F-18 (defer)**, F-19 (defer), F-20 — 6 шт.

Внешние сигналы (замер amnesia-lab, та же дата):

1. **Инцидент cost-tracker**: агент ex3b остановлен HARD-ASK «2 000 700 токенов
   (100% потолка), 1635 вызовов» при собственном расходе **183 341 токен /
   8 вызовов** (usage-метаданные Agent-тула). Причина в коде: без
   `CLAUDE_SESSION_ID` (на Windows её нет — установлено ещё в v1.69.1)
   `session_id()` берёт ОБЩИЙ дневной якорь → все сессии и параллельные
   субагенты дня льются в один леджер.
2. **Инцидент лимита длинной сессии**: в упражнении 3 стратегия «всё одной
   сессией» — единственная, прерванная харнесом на полпути (тот же ложный
   потолок); правило «60% → /handoff» (v1.70.0) сработало бы, но датчика нет.
3. **Оверхед артефактов на микро-задачах**: трек B (PROGRESS/DECISIONS) платил
   +2–8 tool-вызовов и +5–15k токенов за сессию; recovery-разрыв на 3-файловом
   проекте мал (Σ14 vs Σ9 вызовов), окупаемость наступает с ростом кодовой базы.
4. Подтверждение философии Completion Gate: в трёх лаб-сессиях самоотчёт
   гласил «ARTIFACTS: нет» при фактически закоммиченных артефактах —
   самоотчёт ненадёжен, объективный сигнал (git) прав.
5. Продолжающийся сигнал трения: SKILL_BYPASS 438.5/сессию (было 432 в retro
   2026-07-08) — план v1.66.0 не добил; в этот релиз не входит, остаётся в
   бэклоге.

## PROPOSALS

| # | Сигнал (из FACTS) | Предложение | Effort | Риск |
|---|---|---|---|---|
| P1 | Инцидент №1 (боевой): ложный HARD-потолок 2,0M при собственных 183k; леджер 5,9M токенов — общий котёл | `hooks/cost-tracker.sh`: ключ леджера — `payload["session_id"]` (стабилен per агент; тот же класс фикса, что v1.69.1), fallback env → day-anchor. | S | Недоучёт при неверном ключе — прибит `tests/verify_cost_tracker.py` t1–t4 (t1/t2 red на старом коде — проверено) |
| P2 | Инцидент №2 + абстенция F-18 (defer) получила внешний safe-use сигнал | Частичный флип F-18: transcript-size датчик в cost-tracker — one-time hint «подготовь /handoff» при ≥60% окна (байты транскрипта/4 vs `ITD_CONTEXT_WINDOW_TOKENS`). Текстовое правило v1.70.0 остаётся контрактом; датчик — best-effort транспорт (исчез → ничего не ломается). Известное ограничение: после первой компакции оценка завышает (документировано). | S–M | False-positive шум — hint строго 1 раз на леджер + откл. `ITD_CONTEXT_HANDOFF_PCT=0`; гейт t5–t7 |
| P3 | Сигнал №3 (замер): артефакты не окупаются на микро | `/session-save` Steps 3.2/3.3 — микро-путь (≤1–2 юнита, нет durable-решений → пропуск, только session-файл + MEMORY.md), классификация по критерию helpers §6, не «по ощущению». | S | Потеря durable-решения — guard «есть решение → 3.2 обязателен» |

Отклонено/без действия: сигнал №4 — подтверждает существующую архитектуру
(Completion Gate), изменений не требует; сигнал №5 — один релизный слот, без
нового плана снижения bypass в этом релизе (бэклог).

## MERGE

Решение человека: пользователь явно распорядился «занеси обе находки в /retro
и закрой минус 0,5» — P1+P2+P3 идут в релиз v1.71.0 этим же PR (retro-отчёт +
реализация), через штатный конвейер (ветка → review → CI → merge → раскатка).
