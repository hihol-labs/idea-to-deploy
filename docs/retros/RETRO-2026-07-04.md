# RETRO — 2026-07-04 (первый боевой прогон /retro, v1.46.0)

Период: релизный марафон v1.44.0 → v1.46.0 (2026-07-03/04).
Команда скана:

```bash
python3 skills/retro/scripts/itd_retro_scan.py /home/hihol/projects \
  '/mnt/c/Users/Дмитрий/AI/OneOfS_tmp' \
  --tmp-dir '/mnt/c/Users/Дмитрий/AppData/Local/Temp'
```

## FACTS — вывод скана (as-is)

```
## Retro scan — факты из телеметрии (без интерпретации)

Проектов с `.itd-memory`: **1** (workspace: /home/hihol/projects, /mnt/c/Users/Дмитрий/AI/OneOfS_tmp)
**VCR глобально:** n/a (unit-событий нет) (0/0 юнитов), регрессий: 0, проваленных верификаций: 0

**SKILL_BYPASS:** всего 628, по инструментам: Bash×628
Последние причины bypass:
- Bash: генерация артефакта текущей задачи)
- Bash: read-only lookup формата)
- Bash: read-only lookup)
- Bash: read-only lookup для текущего фикса)
- Bash: read-only lookup)
- Bash: генерация артефакта текущей согласованной задачи)
- Bash: шаг скилла session-save)
- Bash: переходный шаг)
- Bash: шаг скилла session-save)
- Bash: середина одобренного релиза /retro v1.46.0 в репо методологии — единая за…

**Cost-леджеры:** 500 сессий, 654100 токенов, ~$19.62
```

Второй замер (WSL `/tmp`): bypassTotal 0, costSessions 0 — WSL-леджеры пусты
(tmpfs очищается ребутом; активные сессии идут на Windows-стороне).

## Внешние сигналы (за пределами скана, задокументированы в session-памяти)

- `careful.sh` **×3 за сутки** ложно матчил `git branch -d` (мягкое удаление
  СМЕРЖЕННОЙ ветки в релизном конвейере) как `git branch -D (force-deletes
  branch)` — предупреждение с требованием подтверждения на штатном шаге
  (сессии 2026-07-03_3, _4, 2026-07-04).
- Ревью-агент (code-reviewer) **×3 релиза подряд** останавливался на
  process-narration («Now check…») вместо финального вердикта — каждый раз
  требовался resume через SendMessage (v1.44/#106, v1.45/#107, v1.46/#108).
- Скилл-энфорсмент **заблокировал tool calls на счётчике 3/3 ПОСРЕДИ
  одобренного релиза** (2026-07-04, v1.46.0) — разблокировка потребовала
  холостого Bash с SKILL_BYPASS.
- Класс «потребитель построен по предположенному формату продюсера» — **2
  Important в одном ревью v1.46.0** (bypass-леджер: `skill` vs реальный
  `tool`; cost-леджер: USD vs реальный `total_tokens`).

## PROPOSALS — ранжированные кандидаты (сигнал → изменение → effort → риск)

| # | Сигнал (evidence) | Предложение | Effort | Риск / чем прибить |
|---|---|---|---|---|
| 1 | Боевой инцидент ×3/сутки: careful матчит «git branch -d» и «gh pr merge --delete-branch» как `-D` | Уточнить паттерн в `hooks/careful.sh`: предупреждать только на `branch -D` / `branch --delete --force`; `-d` и `--delete-branch` после merge — не деструктивны | S | Пропуск реального `-D` — закрыть pipe-тестами (positive `-D` / negative `-d`) в verify-сьюте careful |
| 2 | 628 SKILL_BYPASS, все Bash; ≥4 из 10 последних причин — «read-only lookup»; блокировка 3/3 посреди одобренного релиза | `check-tool-skill.sh`: авто-экземпция read-only Bash (git log/status/diff/show, ls/cat/grep/head/tail/wc/find) — CLAUDE.md УЖЕ декларирует их exempt, хук отстал от декларации; компаунд-команды с мутацией не экземптить | M | Мутация, спрятанная в read-only цепочке `&&` — парсить компаунд; кейсы в verify_skill_enforcement |
| 3 | Боевой проект (OneOfS): 0 unit-событий, VCR n/a при интенсивной работе (10+ PR в git-логе за неделю) | /task Step 3.5: автосоздать `events.jsonl` при существующем `.itd-memory`; pre-flight: мягкий hint, если `.itd-memory` есть, а unit-событий нет | S | Не Гудхарт: чинится СБОР данных, не метрика; шум hint'а — включать только при пустом events.jsonl |
| 4 | ×3 релиза ревью-агент кончает на «Now check…» → ручной resume | Усилить Final-message contract в `agents/code-reviewer.md`: явный анти-паттерн «фразы 'Now/Далее проверю' в финале = нарушение», плюс напоминание «если контекст кончается — выдай вердикт по собранному с пометкой Не успел» | S | Преждевременные вердикты — оговорка «Не успел проверить» уже в контракте v1.42 |
| 5 | 2 Important одного ревью = один класс «assumed producer shape» | В `/task` Step 3f и required-coverage code-reviewer: пункт «интеграция с существующим файлом/форматом — сначала процитируй код ПРОДЮСЕРА, тесты сей реальными образцами» | S | Нулевой — чисто чек-лист |
| 6 | 500 cost-леджеров / 654k токенов = ~1.3k токенов на «сессию» — дробление session-id; файлы копятся без ротации | Расследовать деривацию session-id в `cost-tracker.sh` (похоже, леджер плодится чаще, чем сессии); добавить чистку леджеров старше 14 дней в session-save Step 4.6 | S+расследование | Потеря истории затрат — чистить только старше порога |

Ранжирование: №1 (боевой, повторяющийся, S) → №2 (самый массовый сигнал) →
№3 (слепая зона телеметрии) → №4, №5 (повторившиеся классы) → №6 (одиночный).

Не предлагается (анти-Гудхарт-фильтр сработал): «смягчить условия verified
чтобы VCR появился» — отклонено, чинится сбор событий (№3), не метрика.

## Решение

За человеком: какие номера идут в бэклог следующего релиза (v1.47.0).
/retro на этом остановлен — реализация любого пункта только через штатный
конвейер (ветка → /review → PR).
