# RETRO-2026-07-11-followup — замер трёх сигналов после v1.82.0–v1.84.0

> Follow-up к [RETRO-2026-07-11.md](RETRO-2026-07-11.md): повторный аудит
> 2026-07-11 закончился на «4.9/5; последнюю 0.1 ставит следующее ретро:
> bypass вниз, аномалия учёта — ноль на новых юнитах, ноль шим-инцидентов».
> Этот документ — тот самый замер. Версия ITD: v1.84.0 (#156 смержен и
> раскатан на оба инсталла в тот же день).

## Step 1: FACTS (as-is)

### Скан репо методологии (WSL, через запускатель `itd_py.sh`)

```
Проектов с .itd-memory: 1 (workspace: .)
VCR глобально: 1.0 (18/18 юнитов), регрессий: 0, проваленных верификаций: 1
SKILL_BYPASS: всего 0 | Cost-леджеры: 0 сессий
```

Строки «Аномалия учёта» НЕТ: все 10 юнитов двух релизов дня (U1–U5 v1.83.0,
V1–V5 v1.84.0) имеют полные пары activated→verified.

### Скан OneOfS (Windows, задеплоенный сканер через `py -3`-запускатель)

```
VCR глобально: 0.941 (16/17 юнитов), регрессий: 0
Аномалия учёта: 4 юнит(ов) verified без записанного activation-события
SKILL_BYPASS: всего 1147 за 2 сессий (573.5/сессия), ceremonial: 271
```

### Дельта bypass после раскатки ceremonial-hint (v1.84.0 P4)

Hint сработал live в 15:40:38 (на аннотации автора релиза — первый боевой
прогон). Разрез леджера по mtime hint-sentinel:

```
до hint:    1147 аннотаций (ceremonial 271)
после hint: 0 аннотаций за 0.4 ч (ceremonial 0)
```

## Step 2: вердикты по трём сигналам

| Сигнал | Вердикт | Основание |
|---|---|---|
| **Аномалия учёта = 0 на новых юнитах** | ✅ ПОДТВЕРЖДЁН | репо: 18/18, строки аномалии нет; OneOfS: 4 — исторические юниты ДО фикса (U-2..U-5), число не растёт |
| **Ноль шим-инцидентов** | ✅ ПОДТВЕРЖДЁН | после перевода сниппетов на запускатель — десятки python-вызовов на Windows в тот же день (сканы, goal-report, хук-смоуки), 0 отказов; гейт `verify_no_bare_python3` держит 78 файлов / 0 голых вызовов |
| **Bypass вниз** | ◐ ПОДТВЕРЖДЁН ЧАСТИЧНО | после hint'а — 0 новых аннотаций (рост ceremonial остановлен: 271 заморожен); НО окно 0.4 ч и актор = автор механизма. Финальную точку ставит следующее полноценное ретро на чужой сессии: bypass/сессию < 573.5 |

## Честные оговорки (анти-Гудхарт)

1. Замер bypass сделан в день внедрения тем же агентом, который писал hint —
   эффект наблюдателя не исключён. Метрика зафиксирована (573.5/сессия,
   ceremonial 271) как базовая линия; критерий следующего ретро — падение
   на сессии, где механизм не «свежепостроен».
2. Проваленная верификация ×1 в репо-леджере — исторический артефакт
   (verification_failed до фикса, не регрессия дня).
3. Диагностический кандидат №5 (narration-финалы ревьюеров, evidence ×3 за
   день) этим ретро НЕ закрывается — требует live-probe wiring'а SubagentStop.

## Итог

Два сигнала из трёх подтверждены полностью, третий — поведенчески подтверждён
в коротком окне с зафиксированной базовой линией для независимой проверки.
Оценка компонентов не пересматривается этим документом (0.1 остаётся до
подтверждения bypass-тренда на независимой сессии) — по правилу «последний
балл ставит внешнее evidence».

## P4-final — независимая сессия подтвердила падение трения (2026-07-11, вечер)

Финальная точка bypass-тренда, которую этот документ оставлял открытой
(«0.1 остаётся до подтверждения на независимой сессии»):

- **Сессия-замер:** полностью независимая рабочая сессия (цель
  harness-layers-4.9, OneOfS workspace), ~85 Bash-вызовов, два корректно
  открытых скилл-гейта (/retro, /goal), инструментальные правки в двух репо.
- **Результат:** **0 bypass-аннотаций, 0 ceremonial** за всю сессию
  (леджер claude-skill-ledger-day20260708.jsonl: после 16:39Z — ни одной
  записи). Дневной срез 2026-07-11: 52 записи, все 49 ceremonial — из
  утренних сессий ДО этой.
- **База:** 573.5 записей/сессию (леджеры 20260624: 846; агрегат 0708: 304).
- **Вывод:** P4-hint (v1.84.0) на независимой сессии убирает ceremonial-шум
  полностью: 573.5 → 0. Слой «Калибровка/экономика» пятёрки Harness
  Engineering — evidence для 4.9-5.0 получено; сигнал закрыт.
